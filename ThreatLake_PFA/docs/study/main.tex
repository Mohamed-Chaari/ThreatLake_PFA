\documentclass[11pt,a4paper]{report}

% ---------------------------------------------------------------------
% Encoding / language plumbing - pdflatex, Latin-script Franco-Arabic
% (3andou, lezem, ye5dem...) mixed with English/French technical terms.
% No Arabic-script Unicode needed, so plain T1 + utf8 is enough.
% ---------------------------------------------------------------------
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[french,english]{babel}
\selectlanguage{english}
\usepackage{geometry}
\geometry{a4paper, margin=2.4cm}
\usepackage{xcolor}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage{listings}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}
\usepackage{parskip}
\usepackage{fancyhdr}
\usepackage{hyperref}

% ---------------------------------------------------------------------
% Colors
% ---------------------------------------------------------------------
\definecolor{amber}{HTML}{B8790C}
\definecolor{bodygray}{HTML}{2A2A2A}
\definecolor{analogybg}{HTML}{FDF6E8}
\definecolor{analogyborder}{HTML}{D4A93A}
\definecolor{jurybg}{HTML}{EEF3F7}
\definecolor{juryborder}{HTML}{4A6B8A}
\definecolor{codebg}{HTML}{F5F5F5}
\definecolor{codeframe}{HTML}{CCCCCC}
\definecolor{commentgray}{HTML}{6E6E6E}
\definecolor{stringteal}{HTML}{2E7D6E}

\color{bodygray}

% ---------------------------------------------------------------------
% Section / chapter styling - amber headers
% ---------------------------------------------------------------------
\titleformat{\chapter}[display]
  {\color{amber}\bfseries\Huge}{\chaptertitlename\ \thechapter}{10pt}{\color{amber}}
\titleformat{\section}
  {\color{amber}\bfseries\Large}{\thesection}{0.6em}{}
\titleformat{\subsection}
  {\color{amber}\bfseries\large}{\thesubsection}{0.6em}{}
\titleformat{\subsubsection}
  {\color{amber}\bfseries\normalsize}{\thesubsubsection}{0.6em}{}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\fancyhead[L]{\footnotesize\color{bodygray}ThreatLake PFA}
\fancyhead[R]{\footnotesize\color{bodygray}\leftmark}
\fancyfoot[C]{\small\color{bodygray}\thepage}

% ---------------------------------------------------------------------
% Code listings - light gray background, monospace, small font
% ---------------------------------------------------------------------
\lstdefinestyle{pystyle}{
  backgroundcolor=\color{codebg},
  basicstyle=\ttfamily\scriptsize,
  breaklines=true,
  breakatwhitespace=false,
  keywordstyle=\color{amber}\bfseries,
  commentstyle=\color{commentgray}\itshape,
  stringstyle=\color{stringteal},
  showstringspaces=false,
  frame=single,
  rulecolor=\color{codeframe},
  framerule=0.4pt,
  numbers=left,
  numberstyle=\tiny\color{commentgray},
  numbersep=6pt,
  language=Python,
  tabsize=4,
  columns=fullflexible,
  xleftmargin=1em,
  xrightmargin=0.4em,
  aboveskip=6pt,
  belowskip=6pt,
}
\lstset{style=pystyle}

% ---------------------------------------------------------------------
% Callout boxes
% ---------------------------------------------------------------------
\newtcolorbox{analogy}{
  colback=analogybg, colframe=analogyborder, colbacktitle=analogyborder,
  coltitle=white, fonttitle=\bfseries, title=Analogy,
  boxrule=0.8pt, arc=2pt, breakable, left=6pt, right=6pt, top=4pt, bottom=4pt,
}
\newtcolorbox{soual}{
  colback=jurybg, colframe=juryborder, colbacktitle=juryborder,
  coltitle=white, fonttitle=\bfseries, title=Soual (Jury),
  boxrule=0.8pt, arc=2pt, breakable, left=6pt, right=6pt, top=4pt, bottom=4pt,
}

\hypersetup{
  colorlinks=true,
  linkcolor=amber,
  filecolor=amber,
  urlcolor=amber,
  citecolor=amber,
  bookmarksopen=true,
  pdftitle={ThreatLake PFA - Line-by-line walkthrough},
  pdfauthor={ThreatLake PFA},
}

\DeclareRobustCommand{\code}[1]{\texttt{#1}}
\DeclareRobustCommand{\file}[1]{\texttt{\color{amber}#1}}

\setlist[itemize]{leftmargin=1.4em, itemsep=2pt, topsep=2pt}

\title{\color{amber}\Huge ThreatLake PFA\\[6pt]\LARGE Line-by-line Walkthrough (src/threatlake/)}
\author{Etude approfondie mte3 el codebase -- Franco-Arabe + English}
\date{\today}

\begin{document}

\maketitle
\thispagestyle{empty}

\begin{abstract}
\noindent Hedhi el document ya3mel walkthrough kamel, near line-by-line, mte3
kol file f\ \file{src/threatlake/} mte3 ThreatLake PFA -- la version PFA (cowrie
bark, batch bark, 2 gold tables, 2 detectors). Kol function 3andha subsection
mte3ha, kol ligne walla block muhim mfasser: \emph{chnowa ya3mel} w
\emph{3lech kteb hakka w machi b tari9a o5ra as-hal}. Fama zouz box: \textbf{Analogy}
(tashbih basit) w \textbf{Soual (Jury)} (soual li momken ye5arjek bih superviseur,
ma3 réponse). L'objectif: tefham el code kifma kif rak katbou nta, mouch just
summary sat-hi.
\end{abstract}

\tableofcontents

\chapter{Muqaddima -- kifeh el pipeline ye5dem}

ThreatLake PFA houwa version sghira mte3 ThreatLake AI: pipeline mte3 detection
mte3 honeypot attacks, kolha local, batch bark (ma fama\-ch streaming), source
wahda bark (cowrie -- SSH/Telnet honeypot). El fikra el kbira: data tji raw JSON
(NDJSON lines mte3 cowrie), tetba3 chain mte3 4 couches, ki lakehouse medallion
architecture (bronze / silver / gold):

\begin{itemize}
  \item \textbf{Bronze} (\file{ingestion/}): ya9ra el raw NDJSON lines mel landing
        zone, yparse-hom against schema explicit (\file{config/schema/cowrie.py}),
        y-stamp-hom b metadata (ingest\_date, hash...), w y-save-hom f Delta table.
        Li ma-yparse-ch (JSON invalid) yemchi l "quarantine" -- ma yentliw\-ch.
  \item \textbf{Silver} (\file{transform/silver/}): ya5ou el bronze rows w
        ya\-mapp-hom l schema unifié wahed (\code{SILVER\_EVENT\_SCHEMA}) -- kol
        eventid mte3 cowrie (\code{cowrie.login.failed}, \code{cowrie.command.input}...)
        yetba3lou category w severity.
  \item \textbf{Gold} (\file{transform/gold/}): aggregations fi 2 tables bark --
        \code{attacker\_profiles} (wahda l kol IP) w \code{attack\_timeline}
        (par heure).
  \item \textbf{ML} (\file{ml/}): zouz detectors ye5edmou b3adhom -- IsolationForest
        (unsupervised, trained 3la silver features) w rule fixe (port scan).
        Result-hom ye-append-iw f \code{ml\_scores}.
  \item \textbf{Copilot} (\file{copilot/}): natural language question $\to$ SQL
        (Gemini) $\to$ guardrail (sqlglot AST check) $\to$ execution 3la gold
        tables bark.
  \item \textbf{API} (\file{api/}): FastAPI, read-only, 3andha 3 endpoints bark
        (\code{/alerts}, \code{/attacker\_profiles}, \code{/copilot/query}).
\end{itemize}

Kol chapter jeya besh ta5ou file wahed walla package, w tefasser kol function
line-by-line: \emph{chnowa ta3mel} w \emph{3lech makteba hakka}.

\chapter{\file{common/} -- el infrastructure el mouchtaraka}

Hedha el package fih 4 files: \code{config.py} (settings), \code{paths.py}
(kol location fi lakehouse), \code{spark.py} (factory mte3 SparkSession), w
\code{fs.py} (file operations). Kol module 3la ba3dhom, w kol package l fou9
(ingestion, transform, ml, api...) ya\-import minhom.

\section{\file{common/config.py}}

Hedha el file houwa single source of truth mte3 kol configuration -- YAML file
wahda (\code{config/local.yaml}) + environment variables. Ye5dem b
\code{pydantic-settings}, li ya3mel validation automatique 3la kol value.

\subsection{Precedence -- mnin tji kol value}

Docstring mte3 el file yban clair:

\begin{lstlisting}[style=pystyle]
1. explicit keyword arguments to Settings
2. process environment (THREATLAKE_ prefix, __ nests)
3. config/local.yaml
4. field defaults
\end{lstlisting}

\begin{itemize}
  \item Ye3ni: kif t3ammer \code{Settings(storage=...)} yeddik, ki testa3mel
        environment variable ki \verb|THREATLAKE_SPARK__APP_NAME=x| yeddik
        (ahsen mel YAML), w el YAML file ahsen mel default mte3 field.
  \item \code{\_\_} (double underscore) f nom mte3 env var ya3mel "nesting":
        \verb|THREATLAKE_STORAGE__ROOT| yrouh l \code{settings.storage.root}.
        Hedha houwa \code{env\_nested\_delimiter="\_\_"} f \code{model\_config}.
\end{itemize}

\begin{soual}
\textbf{3lech ma-kteb-tech el config kif dictionary Python 3adi, walla just
os.environ.get() f kol place?}

Réponse: Pydantic ya3mel validation \emph{automatique} 3la kol field (types,
required/optional, custom validators ki \code{\_not\_blank}). Ki tketb
\code{THREATLAKE\_SPARK\_\_SHUFFLE\_PARTITIONS=abc} (mouch number), Pydantic
ye5rejlek erreur clair 3nd el startup, mouch crash randomly f middle mte3
pipeline run ba3d se3at. W \code{frozen=True} y7ell el Settings object immutable
-- ken bech tbeddel-ha lezem tal9a bug, mouch typo li y-mutate-ha silently f
place o5ra fi code.
\end{soual}

\subsection{\code{Layer} w \code{LAYERS} -- el 3 couches}

\begin{lstlisting}[style=pystyle]
Layer = Literal["bronze", "silver", "gold"]
LAYERS: tuple[Layer, ...] = ("bronze", "silver", "gold")
\end{lstlisting}

\begin{itemize}
  \item \code{Literal["bronze", "silver", "gold"]} houwa type hint -- ye9oul l
        mypy/type-checker "hedha el variable ykoun wahed mel 3 strings hedhoula
        bark, machi ay string". Ken tketb \code{layer="platinum"} f function li
        ta5ou \code{Layer}, type-checker yban erreur \emph{9bal} ma trun el code.
  \item \code{LAYERS} houwa tuple runtime mte3 nafs el 3 values -- \code{Literal}
        ye5dem 3la static analysis bark (compile-time), lezem tuple séparé ken
        7bit t-loop 3lihom walla t-validate runtime (\code{if layer not in LAYERS}).
\end{itemize}

\subsection{\code{ConfigError}}

\begin{lstlisting}[style=pystyle]
class ConfigError(RuntimeError):
    """Raised when configuration is missing, unreadable, or inconsistent."""
\end{lstlisting}

Exception class 3adi, custom, bech kol erreur mte3 config yetsayed b type
spécifique (\code{ConfigError}) mouch generic \code{Exception} -- yfahhem el
caller ken el erreur mel config walla min place o5ra.

\subsection{\code{StorageSettings}}

\begin{lstlisting}[style=pystyle]
class StorageSettings(BaseModel):
    """Physical locations of data - plain local filesystem paths."""

    model_config = {"extra": "forbid"}

    root: str = Field(description="Root of the lakehouse; bronze/silver/gold hang off this.")
    quarantine: str = Field(description="Root for records rejected by the schema gate.")
    landing: str = Field(description="Directory the cowrie log source drops raw NDJSON into.")

    @field_validator("root", "quarantine", "landing")
    @classmethod
    def _not_blank(cls, value: str) -> str:
        if not value.strip():
            raise ValueError("storage locations must be non-empty")
        return value.rstrip("/")
\end{lstlisting}

\begin{itemize}
  \item \code{model\_config = \{"extra": "forbid"\}} -- ken el YAML file 3andha
        key ma-mou9out-cha f el model (typo ki \code{roott} instead \code{root}),
        Pydantic ye5rej erreur \emph{9bal} ma yestamer, mouch yetjaheluh silently.
        Hedhi tafsila sghira barcha muhimma -- bidoun-ha, typo fi config yensa
        w tebda t-debug 3la wa9t twil bech tel9a el erreur mabch minha.
  \item \code{root} / \code{quarantine} / \code{landing}: 3 paths, kol wahed
        3andou \code{description} -- mouch just documentation, Pydantic ye5dem-ha
        f auto-generated schemas (JSON schema, error messages) ken 7bit.
  \item \code{@field\_validator("root", "quarantine", "landing")}: nafs el
        validator ye5dem 3la 3 fields b marra (decorator ya59bel tuple mte3
        names).
  \item \code{\_not\_blank}: ken el string fargha walla just whitespace, ye5rej
        \code{ValueError} (Pydantic ye-catch-ha w y-wrap-ha f validation error
        mte3ou). \code{value.rstrip("/")} yne99i trailing slash -- hedhi mohemma
        bech \code{paths.py} ma-y-concat-ch \code{"root//bronze"} (double slash)
        ken el user 7at \code{root: ./data/lakehouse/} (b slash f a5er el value).
\end{itemize}

\begin{analogy}
\code{extra="forbid"} kifha bouncer 3nd bab -- ken jeya field ma-mektoubch f
guest list (el model), ma-ydakhal-cha, ye9tol el process kamel bech ta3rf fama
typo, mouch t5alliha tdakhal w tban silently f runtime.
\end{analogy}

\subsection{\code{TableSettings}}

\begin{lstlisting}[style=pystyle]
class TableSettings(BaseModel):
    """Logical table key -> physical Delta table name, grouped by medallion layer."""

    model_config = {"extra": "forbid"}

    bronze: dict[str, str] = Field(default_factory=dict)
    silver: dict[str, str] = Field(default_factory=dict)
    gold: dict[str, str] = Field(default_factory=dict)

    def names(self, layer: Layer) -> dict[str, str]:
        """Return the logical -> physical table map for `layer`."""
        mapping: dict[str, str] = getattr(self, layer)
        return dict(mapping)

    def resolve(self, layer: Layer, table: str) -> str:
        """Resolve a logical table key to its physical name.

        Unregistered keys pass through unchanged so ad-hoc/test tables work
        without editing config/local.yaml.
        """
        return self.names(layer).get(table, table)
\end{lstlisting}

\begin{itemize}
  \item El fikra: code ye-reference table b "logical key" (\code{"events"},
        \code{"attacker\_profiles"}) machi b physical name direct. El mapping
        (\code{bronze: \{cowrie: bronze\_cowrie\}} f el YAML) ye5alli tbeddel
        smiya physical mte3 table (production rename, walla naming convention
        jdida) bidoun ma tmess el code.
  \item \code{names(self, layer)}: \code{getattr(self, layer)} -- dynamic
        attribute access, \code{layer} houwa string ("bronze"/"silver"/"gold")
        w \code{getattr} ye5ou el dict mte3ha (\code{self.bronze} walla
        \code{self.silver}...) bidoun \code{if/elif} chain manuel.
  \item \code{dict(mapping)}: copy defensive -- caller ma-y9dr-ch y-mutate el
        dict el original mte3 Settings (li houwa frozen barcha, tab3an, walima
        el dict inside momken y-mutate-ha ken ma-3malnach copy).
  \item \code{resolve}: \code{.get(table, table)} -- ken el logical key
        \emph{machi} f el mapping, yrajja3 nafs el key kifha (passthrough). Hedha
        ye5alli ad-hoc tables (tests, walla table jdida experimental) ye5edmou
        bidoun ma tzid entry f YAML l kol table jdida.
\end{itemize}

\subsection{\code{SparkSettings}, \code{CopilotSettings}, \code{MLSettings}}

\begin{lstlisting}[style=pystyle]
class SparkSettings(BaseModel):
    model_config = {"extra": "forbid"}

    app_name: str = "threatlake-pfa"
    master: str | None = Field(default="local[2]", description="Local Spark master.")
    conf: dict[str, str] = Field(default_factory=dict)
    shuffle_partitions: int = Field(default=8, ge=1)
    log_level: Literal[...] = "WARN"
\end{lstlisting}

\begin{itemize}
  \item \code{master="local[2]"}: Spark ye5dem 3la 2 cores bark, machi
        \code{local[*]} (kol cores). 3la laptop, ken el API server w el pipeline
        run ye5edmou f nafs el wa9t, kol wahed y7eb kol el cores -- \code{local[2]}
        y5alli headroom l OS w l processes o5rin.
  \item \code{shuffle\_partitions=8}: default mte3 Spark houwa 200 -- kbir
        barcha l data sghira (test corpus mte3na 3andou 177 rows). 8 partitions
        yban a9rab l size el data el 7a9i9i, ya3mel less overhead.
  \item \code{ge=1} (\code{Field(..., ge=1)}): "greater or equal 1" -- constraint
        numérique, Pydantic ye-reject 0 walla negative automatiquement.
\end{itemize}

\begin{lstlisting}[style=pystyle]
class CopilotSettings(BaseModel):
    """SOC Copilot's NL-to-SQL provider.

    Deliberately NOT a place for the API key: it belongs in that provider's
    own conventional env var (GEMINI_API_KEY), not a THREATLAKE_-prefixed
    setting or a committed YAML file.
    """
    model_config = {"extra": "forbid"}

    provider: Literal["gemini"] = "gemini"
    model: str = "gemini-flash-latest"
    max_output_tokens: int = Field(default=2048, ge=64)
\end{lstlisting}

\begin{soual}
\textbf{3lech el API key (GEMINI\_API\_KEY) mahouch f hedha el Settings class,
w howa configuration barra kif kolchay?}

Réponse (mel docstring): secret keys ma-lezem-hech ykounou f YAML file
(momken yetcommitiw b accident f git) walla f Settings object (li kif tal3ha
Settings f logs walla error messages, el secret momken y-leak). El convention
houwa: kol provider 3andou env var mte3ou el "conventional" (\code{GEMINI\_API\_KEY}),
w \code{copilot/text\_to\_sql.py} ya9raha direct mel \code{os.environ} \emph{f
wa9t el call}, machi mel Settings.
\end{soual}

\begin{lstlisting}[style=pystyle]
class MLSettings(BaseModel):
    """Where the trained anomaly detector lives on disk."""
    model_config = {"extra": "forbid"}
    model_path: str = Field(description="Local path to the joblib-persisted IsolationForest.")
\end{lstlisting}

Field wahed bark: \code{model\_path}. Kif nchoufou f \file{ml/train\_anomaly.py}
w \file{ml/score\_events.py}, hedha el path houwa fein el trained model
(\code{.joblib} file) ye-save w ye-load mennou.

\subsection{\code{\_find\_config\_dir()} -- kifeh yal9a \code{config/}}

\begin{lstlisting}[style=pystyle]
def _find_config_dir() -> Path:
    override = os.environ.get(CONFIG_DIR_VAR)
    if override:
        path = Path(override).expanduser()
        if not path.is_dir():
            raise ConfigError(f"{CONFIG_DIR_VAR}={override!r} is not a directory")
        return path

    candidates = [Path.cwd(), *Path.cwd().parents, *Path(__file__).resolve().parents]
    for base in candidates:
        candidate = base / "config"
        if candidate.is_dir():
            return candidate

    raise ConfigError(
        f"Could not locate a 'config' directory. Run from the repo root or set {CONFIG_DIR_VAR}."
    )
\end{lstlisting}

\begin{itemize}
  \item \code{THREATLAKE\_CONFIG\_DIR} (el \code{CONFIG\_DIR\_VAR}) win 3andha
        priority el kbira -- ken el user 7ata explicit, ye5dem biha bark, w ken
        mouhich directory 7a9i9i ye5rej erreur direct (ma-y3addich silently).
  \item Ken ma-fama-ch override: \code{candidates} tban list mel current
        directory (\code{Path.cwd()}) + kol parent mte3ha (\code{.parents}) +
        kol parent mel file el actuel (\code{\_\_file\_\_}, ye3ni win el
        \code{config.py} rasha mrakez fi \code{src/threatlake/common/}).
  \item El loop y-check kol wahed mel candidates, ye7ell \code{config/}
        (subdirectory) fihom, w yrajja3 l wa7ad li mawjouda. Hedhi el logique
        te5alli el script ye5dem sawa min \code{cwd} el repo root, sawa min
        subdirectory ba3ida, sawa min package installé (editable install) --
        bidoun ma tzid hardcoded absolute path.
  \item Ken \emph{7atta wa7ad} ma-tal9ach, \code{ConfigError} b message clair
        (win yfattech w kifeh yfixiha).
\end{itemize}

\begin{analogy}
Hedhi el logique kif \code{.git} discovery mte3 git nafsou -- git ye-walk-up
mel current directory l fou9 7atta yal9a \code{.git/} folder. Hna nafs el
principe, ma3 fallback l parents mte3 \code{\_\_file\_\_} zeda ken \code{cwd}
ma-3andha-ch \code{config/} qrib.
\end{analogy}

\subsection{\code{YamlSettingsSource} -- kifeh el YAML tji Settings source}

\begin{lstlisting}[style=pystyle]
class YamlSettingsSource(PydanticBaseSettingsSource):
    """Feed config/local.yaml into the settings model."""

    def __call__(self) -> dict[str, Any]:
        path = _find_config_dir() / "local.yaml"
        if not path.is_file():
            raise ConfigError(f"Config file not found: {path}")

        with path.open("rb") as handle:
            loaded = yaml.safe_load(handle) or {}
        if not isinstance(loaded, dict):
            raise ConfigError(f"{path} must contain a YAML mapping at the top level")

        loaded["config_file"] = str(path)
        return loaded
\end{lstlisting}

\begin{itemize}
  \item Hedhi class custom ta3mel implement mte3 \code{PydanticBaseSettingsSource}
        (interface mte3 pydantic-settings) -- ye3ni "source" jdid mte3 configuration,
        ki el environment variables walla init kwargs, ken pydantic yal9a-ha
        3andha \code{\_\_call\_\_} li yrajja3 dict.
  \item \code{yaml.safe\_load} machi \code{yaml.load} -- \code{safe\_load}
        ma-ye5allich el YAML file ye5alle3 arbitrary Python objects (security:
        YAML momken tehtwi \code{!!python/object} tags li yexecutiw code ken
        testa3mel \code{load} el 3adi). \code{safe\_load} ya9ra data structures
        basiques bark (dict, list, str, int...).
  \item \code{or \{\}}: ken el YAML file fargh (\code{yaml.safe\_load} yrajja3
        \code{None}), \code{loaded} ykoun dict fargh mouch \code{None} -- ye5alli
        el code ba3dha (\code{isinstance(loaded, dict)}) ye5dem bidoun crash.
  \item \code{loaded["config\_file"] = str(path)}: yzid field ekstra fi
        el dict el rajja3, bech \code{Settings.config\_file} ykoun m3ammar
        automatiquement b win jat el config -- useful l debugging (ken el
        settings ghalt, ta3raf mnin jaw).
\end{itemize}

\begin{soual}
\textbf{\code{get\_field\_value} ye5rej \code{NotImplementedError} -- 3lech mouch
just implémenter-ha correctly?}

Réponse (mel comment f el code): hedhi method abstract mte3 el ABC
(\code{PydanticBaseSettingsSource}) -- pydantic-settings ye7tajha texist bech
el class ma-tbe9ach abstract (Python ma-y5alli-cha t-instantiate class
3andha abstract method ma-mimplementéech), walakin \code{\_\_call\_\_} houwa
el method el 7a9i9i li pydantic ya3ayet-lou -- \code{get\_field\_value}
ma-yet3ayet-lou 9att f el use-case hedha (source-level, mouch field-level).
\end{soual}

\subsection{\code{Settings} -- el class el kbira}

\begin{lstlisting}[style=pystyle]
class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_prefix=ENV_PREFIX,
        env_nested_delimiter="__",
        extra="forbid",
        frozen=True,
    )

    env: str = "local"
    config_file: str | None = None

    storage: StorageSettings
    tables: TableSettings
    spark: SparkSettings = Field(default_factory=SparkSettings)
    copilot: CopilotSettings = Field(default_factory=CopilotSettings)
    ml: MLSettings

    @classmethod
    def settings_customise_sources(cls, settings_cls, init_settings, env_settings,
                                    dotenv_settings, file_secret_settings):
        return (init_settings, env_settings, dotenv_settings,
                YamlSettingsSource(settings_cls), file_secret_settings)
\end{lstlisting}

\begin{itemize}
  \item \code{frozen=True}: el Settings object immutable ba3d ma yetbena --
        \code{settings.spark.app\_name = "x"} ye5rej erreur. Hedhi te-guarantee
        eno kol part mel code li 3andha reference l nafs Settings object ta5ou
        nafs el values, ma-fama-ch race condition walla mutation surprise.
  \item \code{storage}, \code{tables}, \code{ml}: 3andhom-ch default -- required.
        Ken el YAML ma-fihech-hom, Settings() ye5rej validation error 3nd
        el startup, machi crash randomly wa9t ma el code y7awel ye5dem-hom.
  \item \code{spark}, \code{copilot}: 3andhom \code{default\_factory} -- optional,
        ken mouch f el YAML, ye5dou el defaults mte3 el sub-model.
  \item \code{settings\_customise\_sources}: hedhi el method li t7att order mte3
        el 4 sources (kifma mkeltou fi \code{config.py}'s docstring): init kwargs
        awel (highest priority), ba3d env vars, ba3d dotenv (\code{.env} file --
        pydantic-settings 3andha support native l\-ha walaw ma testa3malha-ch
        el project f manage.sh, testa3ml-ha directly hna kordre fallback),
        ba3d el YAML custom source, w akhiran file secrets (Docker secrets style,
        machi mousta3mla hna walakin lezem tkoun present f tuple bech el ABC
        signature ykoun sa7i7).
\end{itemize}

\subsection{\code{get\_settings()} w \code{reset\_settings()}}

\begin{lstlisting}[style=pystyle]
@lru_cache(maxsize=1)
def get_settings() -> Settings:
    """Return the process-wide settings, loading them on first use."""
    return Settings()


def reset_settings() -> None:
    """Drop the cached settings. Used by tests that swap environments."""
    get_settings.cache_clear()
\end{lstlisting}

\begin{itemize}
  \item \code{@lru\_cache(maxsize=1)} 3la function bidoun arguments: el marra
        el oula ma ye3ayet-lou 7ad, Python ya7seb \code{Settings()} (ya9ra YAML,
        yparse env vars...) w y5azzen result. Kol call jeya ba3d, yrajja3
        \emph{nafs} el object (identity, machi copy) -- bidoun ma yre-parse
        el YAML file kol marra.
  \item Hedhi optimization muhimma: \code{Settings()} ta3mel I/O (ya9ra file
        mel disk), w el project ya3ayet \code{get\_settings()} f barcha places
        (kol \code{silver\_path()}, \code{gold\_path()}...). Bidoul el cache,
        kol call ye3awed I/O -- ma3 el cache, I/O wa7da bark f kol process.
  \item \code{reset\_settings()}: ye-clear el cache -- mousta3mla f tests, win
        kol test 7ab settings jdida (b environment variables mbeddla, ki
        \code{THREATLAKE\_STORAGE\_\_ROOT} l tmp directory). Bidoun \code{reset},
        el test el theni ye5ou nafs el cached Settings mel test el oul.
\end{itemize}

\begin{analogy}
\code{@lru\_cache} 3la function bidoun args kifha "singleton" pattern classic
f OOP (instance wa7da mel class fi kol el application), walakin b decorator
wa7ad bark, bidoun class 5assa. W \code{reset\_settings()} kifha "reset button"
-- testa3mlha ken 7bit tbeddel el configuration f runtime (tests bark, el
production code ma-ye3ayet-lha-ch abadan).
\end{analogy}

\section{\file{common/paths.py}}

Hedha el file houwa "single source of truth" mte3 kol location fi lakehouse.
Docstring ye9oul kol chay clair:

\begin{lstlisting}[style=pystyle]
No other module builds a path by string concatenation - everything goes
through these helpers. Trimmed from ThreatLake AI's version: no
``checkpoint_path`` (no streaming here) and no ``cache_path``/``table_fqn``
(no enrichment cache, no Unity Catalog metastore - PFA is local-path-only).
\end{lstlisting}

\begin{itemize}
  \item Hedhi regle muhimma: \emph{kol} path fi el codebase ye-construct mel
        functions hedhouma bark, 7atta test wa7ad ma-ye5dem-ch b
        \code{f"\{root\}/bronze/\{table\}"} manuel. Ken lezem tbeddel structure
        mte3 lakehouse (nesta3malou naming convention jdid mathalan) tbeddel
        f file wa7ad bark.
\end{itemize}

\subsection{\code{PathError}}

\begin{lstlisting}[style=pystyle]
class PathError(ValueError):
    """Raised for table or checkpoint names that cannot form a safe path."""
\end{lstlisting}

Custom exception, subclass mte3 \code{ValueError} (machi \code{RuntimeError}
kif \code{ConfigError}) -- 3la 5atr el erreur hedhi te-concerne \emph{value}
ghalta (esm table fih "/" walla "..") mouch configuration missing.

\subsection{\code{\_normalise\_root}}

\begin{lstlisting}[style=pystyle]
def _normalise_root(location: str) -> str:
    """Return an absolute, trailing-slash-free root."""
    return str(Path(location).expanduser().resolve())
\end{lstlisting}

\begin{itemize}
  \item \code{.expanduser()}: ken el path fih \code{\~{}} (home directory
        shorthand ki \code{\~{}/data}), ye5dem-ha l absolute home path.
  \item \code{.resolve()}: ye5alli el path absolute, w ye5alli symlinks w
        \code{..} (parent references) yetsetliw -- \code{./data/../data/x}
        w \code{/abs/data/x} ykounou nafs el result akhir.
  \item 3lech muhim ye-normalise: ken zouz configs mte3 test 3andhom \code{root}
        mektouba b tari9tin mo5telfin (relative vs absolute) mel nafs directory
        el 7a9i9i, el pipeline momken ye5el9 zouz Delta tables f zouz paths
        physically mo5telfin ken el comparison textuelle bark (bidoun
        normalisation) -- bug s3ib ye-debug.
\end{itemize}

\subsection{\code{\_validate\_segment}}

\begin{lstlisting}[style=pystyle]
def _validate_segment(segment: str, kind: str) -> str:
    """Reject empty names and traversal, which would escape the storage root."""
    cleaned = segment.strip()
    if not cleaned:
        raise PathError(f"{kind} name must not be empty")
    if cleaned != segment:
        raise PathError(f"{kind} name {segment!r} has leading or trailing whitespace")
    if "/" in cleaned or "\\" in cleaned or cleaned in {".", ".."}:
        raise PathError(f"{kind} name {segment!r} must be a single path segment")
    return cleaned
\end{lstlisting}

\begin{itemize}
  \item \code{cleaned = segment.strip()}: yne99i whitespace mel bidaya w
        el a5er, ba3d ye-compare \code{cleaned != segment} -- ken el original
        3andou whitespace (\code{" cowrie"} mathalan), el function te-reject-ha
        b erreur clair, machi t9bel-ha silently b whitespace jowa esm el table.
  \item \code{"/" in cleaned or "\textbackslash{}\textbackslash{}" in cleaned or cleaned in \{".", ".."\}}:
        hedhi el check el security el muhimma -- \textbf{path traversal
        prevention}. Ken \code{table} houwa user input (mathalan mel API
        walla mel copilot), w ma-3andkech el check hedha, wa7ad momken y3addi
        \code{table="../../etc/passwd"} w el path el rajja3 ye5rej barra mel
        \code{storage\_root} el mou9arrar.
  \item \code{kind} (parameter): ye5alli nafs el function te5dem l "table"
        walla "source" walla ay esm segment o5or, w el error message ykoun
        specifique (\code{"table name must not be empty"} vs \code{"source
        name must not be empty"}).
\end{itemize}

\begin{soual}
\textbf{3lech testa3mel manual string checks (\code{"/" in cleaned}) mouch
regex wahda walla \code{pathlib} validation built-in?}

Réponse: el check basic (whitespace, "/", "\textbackslash{}\textbackslash{}",
"." / "..") ye-cover kol el cas realistes mte3 "logical key" jeya mel YAML
config walla mel API request -- machi arbitrary filesystem paths. Regex
momken ykoun over-engineered l use-case sghir hakka, w explicit checks
(kifma hakka) as-hal ye9raw w ye-debug mel regex pattern kif ma yetkassar.
\end{soual}

\subsection{\code{\_join}}

\begin{lstlisting}[style=pystyle]
def _join(root: str, *segments: str) -> str:
    return "/".join([root.rstrip("/"), *segments])
\end{lstlisting}

Simple join b \code{"/"} -- \code{root.rstrip("/")} yne99i trailing slash mel
root 9bal el join, bech ma-tsir-ch \code{"root//segment"} (double slash) ken
el root deja fih slash f a5er.

\subsection{\code{storage\_root}, \code{layer\_path}, w el helpers el mbniya 3liha}

\begin{lstlisting}[style=pystyle]
def storage_root(settings: Settings | None = None) -> str:
    """Absolute root of the lakehouse."""
    settings = settings or get_settings()
    return _normalise_root(settings.storage.root)


def layer_path(layer: Layer, table: str, settings: Settings | None = None) -> str:
    """Location of a Delta table in ``layer``.

    ``table`` is a *logical* key; the physical name comes from
    ``tables.<layer>`` in ``config/local.yaml``, falling back to the key itself.
    """
    if layer not in LAYERS:
        raise PathError(f"Unknown layer {layer!r}. Expected one of: {', '.join(LAYERS)}")
    settings = settings or get_settings()
    name = _validate_segment(settings.tables.resolve(layer, table), "table")
    return _join(storage_root(settings), layer, name)
\end{lstlisting}

\begin{itemize}
  \item \code{settings: Settings | None = None} -- pattern ye-repeat rou7ou
        f kol function fi hedha el file: ken el caller ma-3atach settings
        (test mathalan li 7eb settings custom), \code{settings or
        get\_settings()} ye5ou el cached global wa7ed. Hedhi te5alli el
        functions testable (dependency injection) w practical fi production
        (default automatique) fi nafs el wa9t.
  \item \code{layer\_path}: ye-valid el \code{layer} l'awel (raise ken mahouch
        f \code{LAYERS}), ba3d ye-resolve el logical \code{table} key l
        physical name b \code{settings.tables.resolve(layer, table)} (mel
        \code{TableSettings} li chefnaha f \code{config.py}), ba3d ye-validate
        el physical name (\code{\_validate\_segment}) 9bal ma y-join-ha m3a
        el root w el layer.
  \item Order el operations muhim: resolve el logical-to-physical \emph{9bal}
        el validation, 3la 5atr el physical name (jeya mel YAML, trusted) houwa
        eli lezem ykoun path segment valide -- el logical key (\code{"events"})
        momken ykoun ay chay.
\end{itemize}

\begin{lstlisting}[style=pystyle]
def bronze_path(source_type: str, settings: Settings | None = None) -> str:
    """Location of a source type's own bronze table (only ``cowrie`` here)."""
    return layer_path("bronze", source_type, settings)


def silver_path(table: str, settings: Settings | None = None) -> str:
    """Location of the cleaned, conformed silver events table."""
    return layer_path("silver", table, settings)


def gold_path(table: str, settings: Settings | None = None) -> str:
    """Location of an aggregated, serving-ready gold table."""
    return layer_path("gold", table, settings)
\end{lstlisting}

Tlata thin wrappers 3la \code{layer\_path} -- kol wa7ad ye-hardcode el layer
name mte3ou. Hedhi machi duplication mouzou3iya: el fikra houwa API clair
(\code{bronze\_path("cowrie")} as-hal ye9ra mel \code{layer\_path("bronze",
"cowrie")}), w ken 7bit tzid layer jdid (mathalan "platinum") tzid function
wa7da bark hna, el logique el kbira ma-tetbeddel-ch.

\begin{lstlisting}[style=pystyle]
def landing_path(source: str, settings: Settings | None = None) -> str:
    """Directory the cowrie log source drops raw NDJSON files into, pre-ingest."""
    settings = settings or get_settings()
    name = _validate_segment(source, "source")
    return _join(_normalise_root(settings.storage.landing), name)


def quarantine_path(table: str, settings: Settings | None = None) -> str:
    """Where records rejected by the schema gate are parked."""
    settings = settings or get_settings()
    name = _validate_segment(table, "table")
    return _join(_normalise_root(settings.storage.quarantine), name)
\end{lstlisting}

\code{landing\_path} w \code{quarantine\_path} \emph{ma-ye3addiwch} b
\code{layer\_path} -- 3la 5atr mahomch "layers" mte3 medallion architecture
(bronze/silver/gold), homa 2 zones separate: landing (win jeya raw data 9bal
ma ta5ol el pipeline) w quarantine (win temchi data li rejected mel schema
gate). Kol wa7ad 3andou root mte3ou fi \code{StorageSettings} (\code{landing},
\code{quarantine}), machi \code{root}.

\section{\file{common/fs.py}}

Hedha el file ya3mel operations basiques (list, move) 3la files, walakin
\emph{machi} b \code{os}/\code{shutil} el Python normal -- ye5dem b
\code{org.apache.hadoop.fs.FileSystem} (el classe Java li Spark yesta3melha
tahtou).

\begin{soual}
\textbf{3lech testa3mel Hadoop FileSystem API (Java, 3ber py4j) mouch just
\code{os.rename()} w \code{os.listdir()} el basiques mte3 Python?}

Réponse (mel docstring): storage roots momken ykounou local paths (PFA,
hedhi el configuration el mous-ta3mla hna) walla cloud URIs (\code{abfss://},
\code{dbfs:/} f ThreatLake AI el kbir 3la Databricks). \code{os.rename}
ye5dem 3la local filesystem bark -- ma-y3arraf-ch \code{abfss://}. El
Hadoop \code{FileSystem} abstraction ye5dem nafs el kif 3la el zouz --
\code{fs.rename(src, dst)} ye5dem sawa 3la \code{file://} sawa 3la
\code{abfss://} bidoun ma tbeddel el code. Hedha ye5alli \code{bronze\_writer.py}
(li testa3mel \code{move}) ye5dem bidoun tbeddil ken el project yetla3 mel
laptop l Databricks cluster.
\end{soual}

\begin{analogy}
Hadoop FileSystem API kifha adapter universel -- ki adapter mte3 prise
kahraba li ye5dem fi kol pays (US, Europe, UK), \code{FileSystem} ye5dem 3la
kol "storage backend" (local disk, Azure, S3, HDFS) b nafs el interface.
Bidoulha, kol module li 7eb yemchi/ye-list files lezem ykoun 3andou code
separate l kol backend.
\end{analogy}

\subsection{\code{\_hadoop\_path} w \code{\_filesystem\_for}}

\begin{lstlisting}[style=pystyle]
def _hadoop_path(spark: SparkSession, path: str) -> JavaObject:
    # _jvm/_jsc are only None before a SparkContext exists; an active `spark`
    # session (this function's precondition) always has both populated.
    jvm = spark.sparkContext._jvm
    assert jvm is not None  # noqa: S101 - type narrowing, not a runtime guard
    result: JavaObject = jvm.org.apache.hadoop.fs.Path(path)
    return result


def _filesystem_for(spark: SparkSession, hadoop_path: JavaObject) -> JavaObject:
    jsc = spark.sparkContext._jsc
    assert jsc is not None  # noqa: S101 - type narrowing, not a runtime guard
    hadoop_conf = jsc.hadoopConfiguration()
    result: JavaObject = hadoop_path.getFileSystem(hadoop_conf)
    return result
\end{lstlisting}

\begin{itemize}
  \item \code{spark.sparkContext.\_jvm}: hedha el "gateway" li py4j ye5alli
        Python code ye3ayet Java classes direct (\code{jvm.org.apache.hadoop...}
        ye5dem kif import Java class mel Python).
  \item \code{assert jvm is not None}: hedha \emph{machi} runtime check
        (comment ye9oul "type narrowing, not a runtime guard") -- houwa hna
        bark bech mypy/type-checker yfahhem eli \code{jvm} mouch \code{None}
        ba3d el assert, w el return type (\code{JavaObject} mouch
        \code{JavaObject | None}) ykoun sa7i7 statically. \code{\_jvm} ma-ykoun-ch
        \code{None} illa 9bal ma yetbena \code{SparkContext} -- w el precondition
        mte3 hedhi el function (\code{spark} active session) te-guarantee ma
        yasal-ch l hedha el cas.
  \item \code{\_filesystem\_for}: ya5ou el \code{hadoop\_conf} (configuration
        mte3 Hadoop, jeyya mel \code{SparkContext}) w y-resolve minha el
        \code{FileSystem} object el correct l el path el m3ayen (local, abfss...)
        -- \code{getFileSystem} ye-decide automatiquement el implementation
        correcte b nazar l el scheme mte3 el path (\code{file://} vs
        \code{abfss://}).
\end{itemize}

\subsection{\code{list\_files}}

\begin{lstlisting}[style=pystyle]
def list_files(spark: SparkSession, directory: str) -> list[str]:
    """Non-recursive list of file (not subdirectory) paths directly under ``directory``.

    Returns ``[]`` if the directory does not exist yet - an empty/absent
    landing directory is a normal state, not an error.
    """
    path = _hadoop_path(spark, directory)
    fs = _filesystem_for(spark, path)
    if not fs.exists(path):
        return []
    return [
        status.getPath().toString()
        for status in fs.listStatus(path)
        if not status.isDirectory() and not status.getPath().getName().startswith((".", "_"))
    ]
\end{lstlisting}

\begin{itemize}
  \item \code{if not fs.exists(path): return []} -- 9rar design muhim: directory
        li ma-mawjoud-ch (landing zone 9bal ma yjini 7atta file wa7ad) houwa
        \emph{normal state} mouch erreur. Ken el function te5rej exception,
        el pipeline el oula run (landing fargh 3ad) ye-crash bidoun 7atta
        sabab 7a9i9i.
  \item List comprehension b 2 filters: \code{not status.isDirectory()}
        (bark files, machi subdirectories -- el function \emph{non-recursive}
        kifma el docstring te9oul), w \code{not ...startswith((".", "\_"))}
        yestab3ed hidden files (\code{.DS\_Store} 3la macOS mathalan) w
        Spark/Hadoop internal marker files (li ye-start b \code{\_}, ki
        \code{\_SUCCESS}).
\end{itemize}

\subsection{\code{move}}

\begin{lstlisting}[style=pystyle]
def move(spark: SparkSession, source: str, destination: str) -> None:
    """Move a single file, creating destination parent directories as needed.

    A move, never a copy-and-delete-source performed from Python: this calls
    the filesystem's own atomic-where-possible rename, which is what makes it
    safe to use as an audit trail (the file never exists in two places at
    once from the caller's perspective, and nothing is ever deleted).
    """
    src_path = _hadoop_path(spark, source)
    fs = _filesystem_for(spark, src_path)
    dest_path = _hadoop_path(spark, destination)
    fs.mkdirs(dest_path.getParent())
    if not fs.rename(src_path, dest_path):
        raise OSError(f"Failed to move {source!r} -> {destination!r}")
\end{lstlisting}

\begin{itemize}
  \item \code{fs.mkdirs(dest\_path.getParent())}: ye5ala9 el parent directory
        mte3 el destination ken ma-mawjoud-ch (mathalan \code{processed/2026-08-22/})
        -- bidoulha \code{fs.rename} ye-fail ken el parent ma-mawjoud-ch.
  \item \code{fs.rename} machi copy-then-delete: \code{rename} houwa atomic
        (walla "atomic-where-possible" 3la el backend) 3nd el filesystem
        nafsou -- el file ma-yban-ch f zouz places f nafs el wa9t, w ma-fama-ch
        risque data loss ken el process ye-crash f nos el operation (copy
        na9sa + delete temmet = data lost; rename atomic ma-3andou-ch hedha
        el risque).
  \item \code{if not fs.rename(...): raise OSError}: \code{fs.rename} yrajja3
        boolean (machi exception 3la failure, kif barcha Java APIs el classic).
        El code hna ye-convert-ha l exception Python-style (\code{OSError})
        bech el caller (\code{bronze\_writer.py}) ma-y-forget-ch ye-check
        el return value.
\end{itemize}

\begin{soual}
\textbf{3lech "move" (mouch "copy") houwa el operation el mous-ta3mla l
ingestion, w 3lech el docstring te-emphasize "audit trail"?}

Réponse: \code{bronze\_writer.py} ya9ra raw files mel landing zone, yparse-hom,
w ba3d ye-move-hom l \code{processed/} (machi ye-delete-hom). Hedha ye5alli
el system "at-least-once": ken el process ye-crash ba3d el parse walakin
9bal el move, el file yeb9a f landing w el run el jayya ye-reprocess-ha
(dedup ye-sir b content-hash downstream, mouch b "did we see this file
before"). W \code{move} (rename atomic) ye-guarantee eli el file ma-yeb9a-ch
"nos processed" -- sawa houwa fi landing (lazem processing) sawa fi processed
(temmet), 7atta wa7ad mel 2.
\end{soual}

\section{\file{common/spark.py} -- el partie el akthar critique}

Hedha el file ya3mel factory l \code{SparkSession}, w fih el logique el akthar
delicate f kol el codebase: verification eli el session ye5dem 7a9i9i f UTC,
machi just "3andou config UTC". El docstring mte3 el file kamel ye-explique
3lech:

\begin{lstlisting}[style=pystyle]
Every session is verified to actually run in UTC before ``get_spark()``
returns it. This exists because ``spark.sql.session.timeZone=UTC`` alone
does not guarantee it: that setting governs Spark SQL's own computation,
but PySpark materializes a collected TimestampType into a Python
``datetime`` via the JVM's OS-level default timezone instead, which can
silently disagree. A session where that gap holds would shift every
collected timestamp by the host's UTC offset without raising anything -
this check exists so that happens loudly, at session start, instead of
silently corrupting every ``event_time`` this pipeline ever reads back.
\end{lstlisting}

\begin{analogy}
5alli el fikra t-bin b exemple: 9oul el host machine (laptop mte3ek) rasha
m3ammra b timezone \code{Africa/Tunis} (UTC+1). \code{spark.sql.session.timeZone
= "UTC"} ye9oul l Spark SQL: "wa9t te-calculate walla te-format timestamp
\emph{jowa} SQL queries, esta3mel UTC". Walakin ken t-collect-i (\code{.collect()})
timestamp l Python \code{datetime} object, hedhi el conversion \emph{ma
te-3addich} b Spark SQL -- ta3addi b el JVM's default timezone (li ye5ou-ha
mel OS mte3 el host, machi mel Spark conf). Ken el JVM 3andou timezone
mahouch UTC, kol timestamp \code{.collect()}-a ye-jini \textbf{shifted b
sa3a} (walla akthar) bidoun 7atta erreur wahda. Kifha saa3a fi bit 3andha
zouz cadrans mo5tlifin -- wa7ad ye9oul el wa9t es-sa7i7, el theni ye9oul
wa9t ghalt, w ma-fama-ch aya indication eli fama probleme.
\end{analogy}

\begin{soual}
\textbf{Kifeh a3raftou b eli hedhi el discrepancy (session.timeZone=UTC
walakin collect() ye3tik wa9t ghalt) mawjouda 7a9i9atan, mouch just
theoretical concern?}

Réponse (mel comment fi \code{\_pin\_jvm\_timezone}): \emph{"confirmed
empirically: with session.timeZone=UTC but a non-UTC host, date\_format()
(pure SQL-side) shows the correct UTC instant while .collect() shows it
shifted by the host's offset."} Ye3ni: testés real, machi assumption --
b date\_format() (li ye5dem 100\% jowa Spark SQL) el output sa7i7, walakin
nafs el instant, ki t-collect-ih l Python, ye5rej ghalt. Hedha el evidence
li 5alla el authors yzidou el \code{TZ} environment variable fix (mouch
just Spark conf fix).
\end{soual}

\subsection{\code{SparkTimezoneError}}

\begin{lstlisting}[style=pystyle]
class SparkTimezoneError(RuntimeError):
    """Raised when a SparkSession cannot be confirmed to run in UTC.

    ThreatLake assumes every timestamp collected to Python is UTC
    wall-clock time - silver mappers, feature windows, and every test rely
    on it.
    """
\end{lstlisting}

Exception class custom -- \code{RuntimeError} (machi \code{ValueError} kif
\code{PathError}) 3la 5atr hedhi mochkla runtime/environment (JVM
misconfiguration), machi valeur ghalta mel user.

\subsection{\code{\_DELTA\_CONF}}

\begin{lstlisting}[style=pystyle]
_DELTA_CONF: dict[str, str] = {
    "spark.sql.extensions": "io.delta.sql.DeltaSparkSessionExtension",
    "spark.sql.catalog.spark_catalog": "org.apache.spark.sql.delta.catalog.DeltaCatalog",
}
\end{lstlisting}

Zouz Spark confs required bech Delta Lake ye5dem: el awel ye-enable el
extensions mte3 Delta 3la SQL parser (ye5alli \code{MERGE}, \code{DELETE},
\code{time travel} syntax ye5edmou), el theni ye-register Delta ki el
catalog implementation predefini (\code{spark\_catalog}) bech kol table
tetsawwar/ta9ra automatiquement b format Delta bidoun ma tzid \code{USING
delta} f kol query.

\subsection{\code{\_pin\_python\_interpreter}}

\begin{lstlisting}[style=pystyle]
def _pin_python_interpreter() -> None:
    """Make Spark's Python workers use the same interpreter as the driver.

    Without this the JVM spawns workers with whatever ``python3`` is first
    on PATH, which fails with PYTHON_VERSION_MISMATCH whenever the active
    venv is not the system Python.
    """
    for var in ("PYSPARK_PYTHON", "PYSPARK_DRIVER_PYTHON"):
        os.environ.setdefault(var, sys.executable)
\end{lstlisting}

\begin{itemize}
  \item \code{sys.executable}: el path exact mte3 l'interpreteur Python li
        rah ye5dem el process el actuel (yzid \code{.venv/bin/python} mathalan,
        machi \code{/usr/bin/python3} el system).
  \item \code{os.environ.setdefault}: ye7ott el variable \emph{ken mahouch
        deja mou3ammar} -- ken el user 3andou \code{PYSPARK\_PYTHON} deja
        f environment mte3ou (override intentionnel), el function
        ma-t-override-ch.
  \item 3lech muhim: fi local mode, Spark yesta3mel subprocess Python
        separate ("workers") l el execution el 7a9i9i. Ken el driver ye5dem
        b virtual environment (\code{.venv}) w el workers ye5edmou b system
        Python (li momken ykoun version mo5talfa), Spark ye5rej erreur
        \code{PYTHON\_VERSION\_MISMATCH} -- w hedhi el fix el standard.
\end{itemize}

\subsection{\code{\_pin\_jvm\_timezone}}

\begin{lstlisting}[style=pystyle]
def _pin_jvm_timezone() -> None:
    """Force the JVM's default timezone to UTC before it starts.

    ``spark.sql.session.timeZone`` (set below) only governs Spark SQL's own
    timestamp computation. When PySpark materializes a TimestampType into a
    Python ``datetime`` (e.g. ``.collect()``), that conversion goes through
    the JVM's OS-level default timezone instead - confirmed empirically:
    with session.timeZone=UTC but a non-UTC host, ``date_format()`` (pure
    SQL-side) shows the correct UTC instant while ``.collect()`` shows it
    shifted by the host's offset. Only the ``TZ`` process environment
    variable, set before the JVM is spawned, fixes this in local mode.
    """
    os.environ.setdefault("TZ", "UTC")
\end{lstlisting}

\begin{itemize}
  \item Ligne wa7da bark f el code (\code{os.environ.setdefault("TZ", "UTC")}),
        walakin hedhi el \emph{fix} el 7a9i9i l el probleme el mecheh fi el
        docstring mte3 el file kamel. \code{TZ} houwa el environment variable
        el standard (POSIX) li kol JVM ya9raha 3nd el startup bech ye-decide
        el default timezone mte3ha.
  \item \emph{"before it starts"} (fi el function name w docstring): muhim
        \code{TZ} yet7att \emph{9bal} el JVM yetbena -- ken el JVM deja
        started (session existant), tbeddel \code{TZ} f Python process
        environment ma-yeb-dilech chay, 3la 5atr el JVM deja 9ra el
        \code{TZ} mel bidaya w \code{cache}-ha internally.
  \item \code{setdefault} nafs el logique: ma-y-override-ch ken el user
        7ata \code{TZ} mou3ayen mel bara7.
\end{itemize}

\subsection{\code{\_local\_session}}

\begin{lstlisting}[style=pystyle]
def _local_session(settings: Settings) -> SparkSession:
    """Build a Delta-enabled local session."""
    from delta import configure_spark_with_delta_pip
    from pyspark.sql import SparkSession

    _pin_python_interpreter()
    _pin_jvm_timezone()
    builder = SparkSession.builder.appName(settings.spark.app_name)
    if settings.spark.master:
        builder = builder.master(settings.spark.master)

    conf: dict[str, str] = {
        **_DELTA_CONF,
        "spark.sql.shuffle.partitions": str(settings.spark.shuffle_partitions),
        **settings.spark.conf,
    }
    for key, value in conf.items():
        builder = builder.config(key, value)

    return configure_spark_with_delta_pip(builder).getOrCreate()
\end{lstlisting}

\begin{itemize}
  \item \code{from delta import ...} w \code{from pyspark.sql import ...}
        jowa el function, machi f top mte3 el file -- imports "lazy". 3lech:
        \code{spark.py} ye-import mel modules o5rin (\code{config.py}) li
        \emph{ma lezemhomch} PySpark bech ye5edmou (unit tests basiques
        mte3 \code{config.py} ma-lezemhomch install kamla mte3 PySpark).
        Ken el import ykoun global f top mte3 el file, kol chay li
        ye-import \code{threatlake.common.spark} (7atta indirectement)
        ye-force install PySpark, 7atta ken ma-yesta3melha-ch fi el path
        el actuel.
  \item \code{\_pin\_python\_interpreter()} w \code{\_pin\_jvm\_timezone()}:
        ye3ayet-lhom \emph{9bal} ma yetbena el \code{SparkSession.builder}
        -- lezem el environment variables (\code{PYSPARK\_PYTHON},
        \code{TZ}) ykounou mou3ammrin 9bal ma el JVM process yetsawwar.
  \item \code{if settings.spark.master:}: \code{master} optional (\code{str
        | None}), ken \code{None} (mathalan f environment Databricks-style
        win el master jeya automatiquement mel cluster), el code ma-y-set-ha-ch
        manuel w y5alli Spark ye5dem b default mte3ha.
  \item \code{conf} dict: b \code{**} unpacking, ye-merge tlata sources
        b ordre precis -- \code{\_DELTA\_CONF} (base), ba3d
        \code{shuffle.partitions} mel settings, ba3d \code{**settings.spark.conf}
        (extra confs mel YAML, momken toverride 7atta \code{\_DELTA\_CONF}
        ken l'user 7eb -- 3la 5atr houma a5er f el merge order, "last wins").
  \item \code{configure\_spark\_with\_delta\_pip(builder).getOrCreate()}:
        \code{configure\_spark\_with\_delta\_pip} houwa helper mel package
        \code{delta-spark} li ye-download/y-attach el JAR files el corrects
        mte3 Delta Lake automatiquement (b nazar l version mte3 PySpark
        installé) -- bidoulha lezem tzid \code{--packages io.delta:...}
        manuel 3nd kol run.
\end{itemize}

\subsection{El probe -- \code{\_PROBE\_WALL\_CLOCK} w \code{\_PROBE\_EXPECTED\_DATETIME}}

\begin{lstlisting}[style=pystyle]
#: The instant the live probe below asks Spark to parse and hand back.
#: Chosen arbitrarily; only its exact round-trip matters.
_PROBE_WALL_CLOCK = "1970-01-01 00:00:01"
_PROBE_EXPECTED_DATETIME = datetime(1970, 1, 1, 0, 0, 1)  # noqa: DTZ001 - deliberately naive, see below
\end{lstlisting}

\begin{itemize}
  \item \code{\_PROBE\_WALL\_CLOCK}: string wa9t arbitraire (Unix epoch + 1
        second, walakin momken ykoun ay wa9t) -- houwa el "input" li rah
        ye-round-trip-ih el probe: ye-parse-ha Spark SQL ki timestamp, ba3d
        ye-collect-ha l Python, w ye-9aren el result m3a el original.
  \item \code{\# noqa: DTZ001 - deliberately naive, see below}: linter
        (ruff, rule DTZ001) 3adatan ye-warn 3la \code{datetime()} bidoun
        timezone info ("naive datetime"), 3la 5atr naive datetimes momken
        ykounou ambiguous. Hna, "naive" houwa \emph{deliberate}: PySpark's
        \code{.collect()} ye-rajja3 hakka naive Python \code{datetime}
        objects (bidoun tzinfo) -- assumption el system kamel houwa eli
        "naive datetime = UTC wall-clock" (kifma el docstring mte3 el
        class \code{SparkTimezoneError} ye9oul). \code{\_PROBE\_EXPECTED\_DATETIME}
        lezem ykoun naive nafsou bech el comparison (\code{==}) ma3 el
        collected value ykoun sa7i7 (comparer naive m3a aware ye-crash f
        Python, w comparer aware m3a naive ye-crash zeda).
\end{itemize}

\subsection{\code{\_check\_utc\_timezone} -- el core logic, factored out l test}

\begin{lstlisting}[style=pystyle]
def _check_utc_timezone(session_timezone: str | None, sql_side: str, collected: datetime) -> None:
    """Pure comparison logic behind the live probe, factored out from
    :func:`_verify_utc_timezone` so it can be unit tested directly: a
    genuinely divergent JVM default timezone can't be faked at runtime on
    an already-started JVM, so reproducing the failure mode for a test
    means calling this function with hand-picked disagreeing values
    instead of a real broken session.
    """
    if session_timezone != "UTC":
        raise SparkTimezoneError(
            f"spark.sql.session.timeZone is {session_timezone!r}, expected 'UTC'. "
            "ThreatLake PFA assumes every Spark session runs in UTC."
        )

    if sql_side != _PROBE_WALL_CLOCK:
        # Extremely unlikely - would mean session.timeZone itself doesn't do
        # what it says - but if Spark SQL's own formatting doesn't agree
        # with what was asked for, the Python-side comparison below is moot.
        raise SparkTimezoneError(
            f"Spark SQL formatted a UTC probe timestamp as {sql_side!r}, expected "
            f"{_PROBE_WALL_CLOCK!r}, despite spark.sql.session.timeZone=UTC."
        )

    if collected != _PROBE_EXPECTED_DATETIME:
        offset = collected - _PROBE_EXPECTED_DATETIME
        raise SparkTimezoneError(
            "spark.sql.session.timeZone reports UTC and Spark SQL agrees (a probe "
            f"timestamp formatted as {sql_side!r}), but materializing that same "
            f"timestamp to Python gives {collected} - off by {offset}. This means "
            "the JVM's OS-level default timezone is not UTC, which would silently "
            "shift every timestamp this pipeline collects to the driver."
        )
\end{lstlisting}

\begin{soual}
\textbf{3lech el authors 5arjou \code{\_check\_utc\_timezone} f function
séparée (pure, bidoun Spark) mnin \code{\_verify\_utc\_timezone}, minstead
ma yketbou kol chay f function wa7da?}

Réponse: houwa el docstring li ye9olha directly -- \emph{"a genuinely
divergent JVM default timezone can't be faked at runtime on an
already-started JVM"}. Ye3ni: ken 7bit test-i el cas ("JVM timezone
mahouch UTC, lezem yban erreur"), ma t9darch t-simule hedha b session Spark
7a9i9i f nafs el process (el JVM deja started b timezone wa7ed, ma
t9darch tbeddel-ha runtime). Fa el fix houwa: tfasel el \emph{comparison
logic} (pure function, ta5ou strings/datetime, terja3 walla te-raise) mel
\emph{probe execution} (li lezemha Spark 7a9i9i). El test unitaire ye3ayet
\code{\_check\_utc\_timezone} direct b values "hand-picked disagreeing"
(mathalan \code{collected} 3andou offset mte3 sa3a) bidoun 7atta ye5dem
Spark -- test rapide, deterministe, w ye-cover exactement el failure mode
el mou5if bidoun infrastructure te9ila.
\end{soual}

\begin{itemize}
  \item 3 checks séquentiels, kol wa7ad y-raise b message clair w specifique:
  \begin{enumerate}
    \item \code{session\_timezone != "UTC"}: el check el basic -- el conf
          nafsou (\code{spark.sql.session.timeZone}) 9al eli mahouch UTC.
    \item \code{sql\_side != \_PROBE\_WALL\_CLOCK}: el conf t9oul UTC,
          walakin Spark SQL nafsou (\code{date\_format}) ma-formatiech el
          probe kifma el input. Comment ye9oul "extremely unlikely" --
          hedha el check ye-cover cas nadhari bark (bug fi Spark nafsou), rare
          barcha, walakin present bech el logique tkoun kamla w defensive.
    \item \code{collected != \_PROBE\_EXPECTED\_DATETIME}: hedha el check
          el \emph{7a9i9i el muhim} -- el gap bin Spark SQL (correct) w
          Python-side collect (momken ghalt). \code{offset = collected -
          \_PROBE\_EXPECTED\_DATETIME} y7seb el difference exacte
          (\code{timedelta}) w y-includi-ha f el error message, bech
          l'debugger ya3raf b kaddech el shift (sa3a wa7da? zouz?).
  \end{enumerate}
\end{itemize}

\subsection{\code{\_verify\_utc\_timezone} -- el probe el 7a9i9i}

\begin{lstlisting}[style=pystyle]
def _verify_utc_timezone(session: SparkSession) -> None:
    """Run the live probe against ``session`` and raise if it isn't UTC.

    Checks ``spark.sql.session.timeZone`` AND independently re-derives the
    answer by round-tripping a known instant through Spark SQL and back to
    Python - the conf value alone is not trusted.
    """
    from pyspark.sql import functions as F

    probe = session.createDataFrame([(1,)], ["_i"]).select(
        F.to_timestamp(F.lit(_PROBE_WALL_CLOCK)).alias("ts")
    )
    row = probe.select(
        F.col("ts"), F.date_format(F.col("ts"), "yyyy-MM-dd HH:mm:ss").alias("sql_side")
    ).first()
    assert row is not None  # noqa: S101 - always exactly one row from an unconditional select

    _check_utc_timezone(session.conf.get("spark.sql.session.timeZone"), row["sql_side"], row["ts"])
\end{lstlisting}

\begin{itemize}
  \item \code{session.createDataFrame([(1,)], ["\_i"])}: ye5al9 DataFrame
        b row wa7da bark, column wa7da (\code{\_i}, value \code{1}) --
        el fikra hna mahich el data nafsou, houwa just "scaffold" bech
        na3mlou \code{.select()} 3lih w n7otou el probe expression.
  \item \code{F.to\_timestamp(F.lit(\_PROBE\_WALL\_CLOCK))}: ye-parse el
        string \code{"1970-01-01 00:00:01"} l \code{TimestampType} \emph{jowa
        Spark SQL} (b nazar l \code{spark.sql.session.timeZone}, li lezemha
        tkoun UTC).
  \item \code{F.date\_format(F.col("ts"), "yyyy-MM-dd HH:mm:ss")}: ye-format
        el timestamp \emph{mel jdid} l string, dima jowa Spark SQL -- hedha
        el "sql\_side" el check el theni.
  \item \code{.first()}: ye5dem \code{.collect()} implicitement 3la row
        wa7da -- hna houwa el moment el critique win \code{row["ts"]}
        (el TimestampType) ye-materialize l Python \code{datetime} \emph{3ber
        el JVM's default timezone}, machi \code{session.timeZone}.
  \item \code{assert row is not None}: guarantee eli \code{.first()} ma
        rajja3-ch \code{None} -- 3la 5atr el \code{select} bidoun \code{WHERE}
        3la DataFrame b row wa7da, el result lezem ykoun exactement row
        wa7da, 9att \code{None}. Comment ye9oul "always exactly one row
        from an unconditional select" -- nafs pattern el \code{assert}
        el mous-ta3mal f \code{fs.py} (type narrowing, machi runtime
        defensive check 7a9i9i).
  \item El call l \code{\_check\_utc\_timezone}: y3addi tlata values --
        el conf (\code{session.conf.get(...)}), el sql-side result
        (\code{row["sql\_side"]}), w el collected datetime (\code{row["ts"]}).
        Hedhi el "impure" part mte3 el probe (ta9ra mel session 7a9i9i)
        separee 3la el "pure" comparison logic el testable.
\end{itemize}

\subsection{\code{get\_spark} w \code{stop\_spark}}

\begin{lstlisting}[style=pystyle]
def get_spark(settings: Settings | None = None) -> SparkSession:
    """Return the local SparkSession, verified to run in UTC.

    Raises ``SparkTimezoneError`` rather than return a session that would
    silently corrupt every timestamp it collects.
    """
    settings = settings or get_settings()
    session = _local_session(settings)
    _verify_utc_timezone(session)
    session.sparkContext.setLogLevel(settings.spark.log_level)
    return session


def stop_spark() -> None:
    """Stop the active session, if any."""
    from pyspark.sql import SparkSession

    session = SparkSession.getActiveSession()
    if session is not None:
        session.stop()
\end{lstlisting}

\begin{itemize}
  \item \code{get\_spark}: el entry point el public el wa7ed -- kol module
        o5or (\code{ingestion/}, \code{transform/}, \code{ml/}...) ye3ayet
        \code{get\_spark()} bark, ma ye3rafouch 7atta wa7ad mel details
        (\code{\_local\_session}, \code{\_verify\_utc\_timezone}) -- hedha
        houwa el "factory pattern": function wa7da public, kol el complexity
        mahfouza jowa.
  \item Order el operations: \code{\_local\_session} (session ye5dem),
        ba3d \emph{direct} \code{\_verify\_utc\_timezone} (9bal ma yrajja3
        el session l caller). Ken el verification t-fail, exception
        ye-propagate w el caller ma-ya5dech-ch session ghalt fi yaddih --
        \emph{"rather than return a session that would silently corrupt every
        timestamp"} (docstring).
  \item \code{session.sparkContext.setLogLevel(settings.spark.log\_level)}:
        yet7att ba3d el verification, mouch 9bal -- 3la 5atr el log level
        (\code{"WARN"} default) ye5dem 3la logs mte3 Spark internal, machi
        7aja m3allé9a m3a el correctness mte3 el session.
  \item \code{stop\_spark}: \code{SparkSession.getActiveSession()} ye-check
        ken fama session active 9bal ma y7awel ye-stop-ha -- \code{if session
        is not None} ye5alli \code{stop\_spark()} ykoun idempotent (call-ha
        marat 3adda, bidoun erreur ken deja stopped walla ma-tbenetch 7atta
        marra).
\end{itemize}

\chapter{\file{ingestion/} -- mel raw NDJSON l bronze}

Hedha el package fih 3 files: \code{schemas.py} (registry mte3 schemas
explicit), \code{bronze\_transform.py} (transforms pure, action-free), w
\code{bronze\_writer.py} (l'orchestration el 7a9i9iya li ta9ra, tparse, w
tektb).

\section{\file{ingestion/schemas.py}}

\begin{lstlisting}[style=pystyle]
The schema lives in ``config/schema/cowrie.py`` rather than ``src/``
because, like ``config/local.yaml``, it describes *this deployment's*
known data shape and should be editable without a code change. It is
discovered through the same config-directory mechanism as
``threatlake.common.config``, then loaded as a plain Python module by
file path.

ThreatLake AI registers four source types (suricata, cowrie, dionaea,
heralding) here. PFA is cowrie-only (see ARCHITECTURE.md), so
``SOURCE_TYPES`` has exactly one entry - but the loader mechanism itself
is unchanged, so adding a second source later is a one-line addition to
``SOURCE_TYPES``/``_MODULE_BY_SOURCE`` plus a new schema file, not a
redesign.
\end{lstlisting}

\begin{soual}
\textbf{3lech el schema (structure mte3 el JSON, kol field w type mte3ha)
mahouch f \code{src/}, walakin f \code{config/schema/cowrie.py} -- 7atta
houwa fi el a5er file \code{.py} kifma ay module fi \code{src/}?}

Réponse: el schema ye-describe \emph{el deployment el actuel}, mouch el
code logique. Ken cowrie (el honeypot nafsou) ye-log field jdid ba3d
update, walla ye-drop field 9dim, el fix houwa tbeddel \code{config/schema/cowrie.py}
-- \emph{configuration change}, machi code change (ma-lezmek-ch PR review
mte3 \code{src/}, ma-lezmek-ch re-deploy el package). Nafs el falsafa mte3
\code{config/local.yaml}: "chnowa el shape/values el m3aynin l hedha
el deployment" ye-b9a separate 3la "kifeh el logique te5dem".
\end{soual}

\subsection{\code{SOURCE\_TYPES} w \code{\_MODULE\_BY\_SOURCE}}

\begin{lstlisting}[style=pystyle]
SOURCE_TYPES: tuple[str, ...] = ("cowrie",)

_MODULE_BY_SOURCE = {
    "cowrie": "cowrie.py",
}
\end{lstlisting}

\code{SOURCE\_TYPES} tuple b entry wa7da bark (\code{"cowrie"}) -- hedhi
el scope el PFA el mou9arrar (contra ThreatLake AI li 3andha 4: suricata,
cowrie, dionaea, heralding). \code{\_MODULE\_BY\_SOURCE} ye-map kol source
type l esm el file \code{.py} mte3ou fi \code{config/schema/}.

\subsection{\code{SchemaError}}

\begin{lstlisting}[style=pystyle]
class SchemaError(RuntimeError):
    """Raised when a source schema cannot be located or loaded."""
\end{lstlisting}

Exception custom o5ra, nafs el pattern (\code{RuntimeError} subclass) --
kol package 3andou exception mte3ou el specifique.

\subsection{\code{\_load\_schema} -- dynamic module loading}

\begin{lstlisting}[style=pystyle]
def _load_schema(source_type: str) -> StructType:
    try:
        filename = _MODULE_BY_SOURCE[source_type]
    except KeyError as exc:
        raise SchemaError(
            f"Unknown source type {source_type!r}. Expected one of: {', '.join(SOURCE_TYPES)}"
        ) from exc

    path = config_dir() / "schema" / filename
    if not path.is_file():
        raise SchemaError(f"Schema file not found for {source_type!r}: {path}")

    spec = importlib.util.spec_from_file_location(f"threatlake_schema_{source_type}", path)
    if spec is None or spec.loader is None:
        raise SchemaError(f"Could not load schema module from {path}")
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)

    try:
        schema: StructType = module.SCHEMA
    except AttributeError as exc:
        raise SchemaError(f"{path} does not define SCHEMA") from exc
    return schema
\end{lstlisting}

\begin{itemize}
  \item \code{except KeyError as exc: raise SchemaError(...) from exc}:
        pattern "exception chaining" -- \code{from exc} y5alli el traceback
        el original (\code{KeyError}) mzabet ma3 el exception el jdida
        (\code{SchemaError}), bech el debugging ye-b9a clair (ta3raf el
        \code{SchemaError} jeyya mel \code{KeyError} lookup, mouch min
        chay o5or).
  \item \code{importlib.util.spec\_from\_file\_location(...)} +
        \code{module\_from\_spec} + \code{exec\_module}: hedhi el
        "3 étapes" el standard mte3 Python bech t-load module mel path
        \emph{directement} (bidoun ma ykoun el file f \code{sys.path} walla
        \code{PYTHONPATH}). Mousta3mla hna 3la 5atr \code{config/schema/cowrie.py}
        mahouch jowa \code{src/} (mahouch "importable" normalement b
        \code{import threatlake....}).
  \item \code{f"threatlake\_schema\_{source\_type}"}: esm arbitraire l
        module fi \code{sys.modules} internal -- prefix \code{threatlake\_schema\_}
        y-avoid collision m3a modules o5rin f nafs el process.
  \item \code{module.SCHEMA}: el convention el mou9arrar -- kol schema file
        (\code{cowrie.py}) lezem ykoun 3andou variable esmha \code{SCHEMA}
        (\code{StructType}). Ken el file ma-3andou-ch, \code{AttributeError}
        ye-catch w ye-convert l \code{SchemaError} b message clair (\emph{"does
        not define SCHEMA"}), machi \code{AttributeError} generic li ma
        yban-ch mnin.
\end{itemize}

\begin{analogy}
\code{importlib.util.spec\_from\_file\_location} kifha "load plugin mel
path direct" -- kif application desktop (Photoshop, VS Code) t-load plugin
mel folder m3ayen (mouch mel "install location" el standard), Python hna
ye-load \code{.py} file mel path arbitraire w ye-execute-ha kifha module
3adi, bidoun ma tzid-ha l \code{PYTHONPATH} walla t-install-ha kif package.
\end{analogy}

\subsection{\code{source\_schema} -- el entry point el cached}

\begin{lstlisting}[style=pystyle]
@cache
def source_schema(source_type: str) -> StructType:
    """Return the explicit StructType for a source type.

    Cached: schema modules are loaded from disk once per process.
    """
    return _load_schema(source_type)
\end{lstlisting}

\code{@cache} (mel \code{functools}, equivalent l \code{@lru\_cache(maxsize=None)})
3la function \emph{b argument} (\code{source\_type}) -- machi bidoun
argument kif \code{get\_settings()}. Kol \code{source\_type} distinct
(hna "cowrie" bark, walakin design ye5alli barcha) ye5dou entry separate
fi el cache. Nafs el motivation kif \code{get\_settings}: \code{\_load\_schema}
ya3mel I/O (disk read + \code{exec\_module}, li houwa expensive nisbiyan),
w el function hedhi te-ncall mel barcha places (\code{bronze\_transform.py}
kol marra ye-parse walla ye-finalize).

\section{\file{ingestion/bronze\_transform.py}}

\begin{lstlisting}[style=pystyle]
Every function here is a plain column-expression transform - no
``.count()``, ``.collect()``, ``.first()``, or other Spark action, and no
I/O. Keeping this module action-free is what makes it safe to test
without a live SparkSession action being forced at import time, and keeps
the parse/quarantine-split logic in one place regardless of how many call
sites end up needing it.
\end{lstlisting}

\begin{soual}
\textbf{3lech "action-free" (bidoun \code{.count()}, \code{.collect()}...)
houwa design constraint mou9asad, mouch just coincidence?}

Réponse: Spark DataFrames ye5edmou b "lazy evaluation" -- \code{.withColumn},
\code{.select}, \code{.filter} ma-ye-executiwch chay fi el lever, ye-bnou
just "plan" (DAG). Bark \code{.count()}, \code{.collect()}, \code{.write}...
("actions") ye-forciw el execution 7a9i9iya. Ken \code{bronze\_transform.py}
kan fih actions, kol test unitaire li ye3ayet \code{parse\_source} mathalan
lezmou SparkSession \emph{active} bech el test rou7ou ye5dem -- slow tests,
w tests li lezemhom Spark cluster (7atta local) bark bech ta3mel unit test
3la logique transform basic. Bidoun actions, el functions hedhoula ye5edmou
3la DataFrame "plan" bark, w ta9dar t-test-hom b DataFrames sghar bidoun
overhead.
\end{soual}

\subsection{\code{CORRUPT\_COLUMN} w \code{METADATA\_COLUMNS}}

\begin{lstlisting}[style=pystyle]
CORRUPT_COLUMN = "_corrupt_record"

METADATA_COLUMNS = (
    "ingest_date",
    "source_type",
    "_ingested_at",
    "_source_file",
    "_record_hash",
    "_raw",
)
\end{lstlisting}

\begin{itemize}
  \item \code{CORRUPT\_COLUMN}: esm el column li Spark's PERMISSIVE JSON
        mode (chnou el pipeline testa3mel, nchoufouha ba3d) ye7ott fiha
        el raw text mte3 line li ma-parsat-ch, w \code{null} f kol chay
        o5or.
  \item \code{METADATA\_COLUMNS}: 6 columns li kol row (bronze walla
        quarantine) 3andha, barra 3la el data mte3 el event nafsou --
        \code{ingest\_date} (date mte3 el batch run), \code{source\_type}
        (\code{"cowrie"} dima hna), \code{\_ingested\_at} (timestamp exact),
        \code{\_source\_file} (win jat el line -- audit trail), \code{\_record\_hash}
        (hash mte3 el content -- l dedup, kifma chefna f \code{bronze\_writer.py}),
        w \code{\_raw} (el original text, bidoun ma tetbeddel).
\end{itemize}

\subsection{\code{with\_ingestion\_metadata}}

\begin{lstlisting}[style=pystyle]
def with_ingestion_metadata(df: DataFrame, source_type: str, ingested_at: datetime) -> DataFrame:
    """Stamp every row with its source, ingestion instant, and content hash.

    ``ingested_at`` is a single Python-computed instant for the whole
    batch, not ``current_timestamp()`` per row - every row in one run
    should agree on one ``ingest_date``.
    """
    return (
        df.withColumn("source_type", F.lit(source_type))
        .withColumn("_ingested_at", F.lit(ingested_at))
        .withColumn("ingest_date", F.to_date(F.col("_ingested_at")))
        .withColumn("_record_hash", F.sha2(F.col("_raw"), 256))
    )
\end{lstlisting}

\begin{itemize}
  \item \code{F.lit(ingested\_at)} machi \code{F.current\_timestamp()}:
        hedhi el tafsila el critique -- \code{ingested\_at} houwa Python
        \code{datetime} m7soub \emph{marra wa7da} 9bal ma yetba3et l hedhi
        el function (mel \code{bronze\_writer.write\_bronze}), ba3d
        ye-embed-ha (\code{F.lit}) f kol row. Ken testa3mel
        \code{F.current\_timestamp()}, kol row (fi partitions mo5tlifa,
        executors mo5tlifin) ye5dou timestamp \emph{légèrement} mo5tlef
        (milliseconds difference), w \code{ingest\_date} (\code{to\_date}
        mennou) momken ykoun mo5tlef bin rows mel nafs el batch ken el
        batch ye5dem 3la midnight boundary -- bug rare walakin s3ib
        ye-debug.
  \item \code{F.sha2(F.col("\_raw"), 256)}: SHA-256 hash mte3 el raw text
        -- hedha houwa el \code{\_record\_hash} li y-mentioned f
        \code{bronze\_writer.py}'s docstring l dedup: nafs el line ye-ingest
        marat 3adda ye5dou nafs el hash, w nafs el \code{event\_id} akhir
        (silver-side), fa duplicates ma-ye-corrupt-ch el data even ken
        el file ye-ingest twice.
\end{itemize}

\subsection{\code{parse\_source}}

\begin{lstlisting}[style=pystyle]
def parse_source(df: DataFrame, source_type: str) -> DataFrame:
    """Parse `_raw` against the source's schema, flagging unparseable JSON."""
    schema = source_schema(source_type)
    corrupt_field = StructField(CORRUPT_COLUMN, StringType(), True)
    schema_with_corrupt = StructType([*schema.fields, corrupt_field])
    options = {"mode": "PERMISSIVE", "columnNameOfCorruptRecord": CORRUPT_COLUMN}
    return df.withColumn("_parsed", F.from_json(F.col("_raw"), schema_with_corrupt, options))
\end{lstlisting}

\begin{itemize}
  \item \code{StructType([*schema.fields, corrupt\_field])}: y-extend el
        schema el original (mel \code{config/schema/cowrie.py}) b field
        wa7ed zeda (\code{\_corrupt\_record}) -- hedha el pattern el
        Spark documented l \code{from\_json} PERMISSIVE mode: el
        \code{columnNameOfCorruptRecord} lezem ykoun \emph{field jowa el
        schema el m3atat}, mouch column separate.
  \item \code{"mode": "PERMISSIVE"}: 3nd JSON parse fail, Spark
        \emph{ma-ye5rej-ch erreur} -- ye7ott \code{null} f kol el fields
        el parsed w yzid el raw text f \code{\_corrupt\_record}. Hedhi el
        base mte3 el "quarantine" flow -- bidoun PERMISSIVE, line JSON
        wa7da ghalta ye-crash el batch kamel.
  \item \code{F.from\_json(F.col("\_raw"), schema\_with\_corrupt, options)}:
        el parse el 7a9i9i -- ye-ekhraj struct column \code{\_parsed}
        (nested, b kol el fields mte3 el schema + \code{\_corrupt\_record}).
\end{itemize}

\subsection{\code{split\_quarantine}}

\begin{lstlisting}[style=pystyle]
def split_quarantine(df: DataFrame) -> tuple[DataFrame, DataFrame]:
    """Separate rows whose raw text failed to parse as JSON at all."""
    malformed = F.col("_parsed").isNull() | F.col(f"_parsed.{CORRUPT_COLUMN}").isNotNull()
    return df.filter(~malformed), df.filter(malformed)
\end{lstlisting}

\code{malformed} houwa el condition eli t7att row f quarantine: sawa
\code{\_parsed} nafsou null (JSON invalide kamel, ma-nja7ch el parse
kamel), sawa \code{\_corrupt\_record} mahouch null (PERMISSIVE mode 3allem
eno el line ma-t9addarch tetmatch m3a el schema). \code{return
df.filter(\textasciitilde{}malformed), df.filter(malformed)}: yrajja3
tuple b 2 DataFrames -- el awel (\code{good}) houwa el rows el sa7
(negation b \code{\textasciitilde{}}), el theni (\code{bad}) houwa el
malformed.

\subsection{\code{finalize\_bronze} w \code{finalize\_quarantine}}

\begin{lstlisting}[style=pystyle]
def finalize_bronze(df: DataFrame, source_type: str) -> DataFrame:
    """Project to metadata + ``source_type``'s own populated struct column -
    the physical shape of the bronze table, and exactly what
    ``threatlake.transform.silver.cowrie.map_cowrie`` expects to read.
    """
    parsed_field_names = [f.name for f in source_schema(source_type).fields]
    populated = F.struct(*[F.col(f"_parsed.{name}").alias(name) for name in parsed_field_names])
    return df.select(*METADATA_COLUMNS, populated.alias(source_type))


def finalize_quarantine(df: DataFrame) -> DataFrame:
    return df.select(*METADATA_COLUMNS, F.col(f"_parsed.{CORRUPT_COLUMN}").alias("_error"))
\end{lstlisting}

\begin{itemize}
  \item \code{finalize\_bronze}: ye-project mel struct \code{\_parsed}
        (li fih kol field mte3 el schema + \code{\_corrupt\_record}) l
        struct jdid \emph{bidoun} \code{\_corrupt\_record} (3la 5atr rows
        el hnaya deja \code{good}, ma-yehtajouch el field hedha) --
        \code{populated.alias(source\_type)} y-esm el struct \code{"cowrie"},
        w el result akhir houwa \code{METADATA\_COLUMNS} (6 columns) +
        \code{cowrie} (struct wa7ed).
  \item Hedhi el shape 7a9i9i mte3 bronze table -- w kifma el docstring
        ye9oul, hedhi \emph{exactement} el shape li \code{map\_cowrie}
        (silver) yes-ta5eleha. Fi ThreatLake AI (4 sources), el bronze
        row lezemha struct columns l kol el 4 sources (3 minhom null 3la
        kol row), w function \code{expand\_to\_unified\_bronze\_shape}
        te3mel had el "reconstruction". Hna, b source wa7ed bark, hedhi
        el étape \emph{ma-lezmech}: bronze row 3andha struct \code{cowrie}
        wa7ed bark, mahich nsé.
  \item \code{finalize\_quarantine}: nafs el fikra walakin ansem --
        \code{METADATA\_COLUMNS} + \code{\_error} (el raw corrupt text,
        mel \code{\_corrupt\_record}).
\end{itemize}

\section{\file{ingestion/bronze\_writer.py}}

Hedha el file houwa el orchestrateur -- ya5ou el functions pure mel
\code{bronze\_transform.py} w y-combine-hom m3a I/O 7a9i9i (Spark read,
Delta write, file move).

\begin{lstlisting}[style=pystyle]
Processed-file tracking: the exact set of files present in the landing
directory is captured once, up front, and used for both the read and the
post-write move - never re-listed - so a file that lands mid-batch is
left alone for the next run rather than partially processed. After a
successful write, every file in that set is *moved* (not deleted) to
``<landing>/cowrie/_processed/<ingest_date>/``, preserving them as an
audit trail while making the landing directory empty for the next run.
This gives at-least-once, not exactly-once, ingestion: if the process
dies between the Delta write succeeding and the move completing, the
same file is re-ingested on the next run, producing duplicate bronze
rows. That duplicate is harmless downstream - each silver ``event_id`` is
a content hash of the bronze row (see
``threatlake.transform.silver.schema``), so re-ingesting the same file
twice produces the same event_id both times rather than two silver rows.
\end{lstlisting}

\begin{soual}
\textbf{3lech "at-least-once" (momken duplicate rows f bronze) houwa
acceptable, mouch tri ye5edem "exactly-once" strict?}

Réponse: exactly-once ingestion (distributed transaction bin file-move
w Delta write) houwa complex barcha w s3ib ye-implement correctly bidoun
external coordinator (2-phase commit walla kifha). El authors 5tarou design
as-hal: at-least-once mel ingestion layer (duplicates possible, rare --
bark ken el process ye-crash f moment exact bin el write w el move), w
exactly-once semantics \emph{effective} mel dedup downstream b content-hash
(\code{event\_id}). Hedhi separation of concerns: ingestion layer ye-focus
3la robustness (never lose data), silver layer ye-focus 3la correctness
(never double-count) -- kol wa7ed 3andou el responsabilité mte3ou.
\end{soual}

\begin{analogy}
Kifha kif ta5dem b receipt/checklist mta3 déménagement -- t7ott kol box
f "processed" bark ba3d ma tet2akked eli el contenu deja fi el 5ir el jdid
(el Delta write). Ken el kahraba te9ta3 (crash) 9bal ma tzid el checkmark
(el move), tel9a el box mazelet f 'landing' -- el marra el jayya, terja3
t-check-ha (te5dem duplicate work, walakin ma-tfou-tha 9att).
\end{analogy}

\subsection{\code{\_PROCESSED\_SUBDIR} w \code{BronzeWriteResult}}

\begin{lstlisting}[style=pystyle]
_PROCESSED_SUBDIR = "_processed"


@dataclass(frozen=True)
class BronzeWriteResult:
    """Row counts from a single bronze ingestion batch."""

    source_type: str
    written: int
    quarantined: int
\end{lstlisting}

\code{@dataclass(frozen=True)}: nafs el pattern immutable li chefna f
\code{Settings} -- result object simple, 3andou 3 fields bark, machi
lezmou class kbira b methods. \code{frozen=True} ye5alli-ha immutable
(consistent m3a el falsafa el 3amma mte3 el codebase: results/settings
immutable, mutations explicit bark f DataFrame transformations).

\subsection{\code{\_read\_landing\_lines}}

\begin{lstlisting}[style=pystyle]
def _read_landing_lines(spark: SparkSession, file_paths: list[str]) -> DataFrame:
    """One row per line across exactly the given files."""
    return (
        spark.read.text(file_paths)
        .withColumnRenamed("value", "_raw")
        .withColumn("_source_file", F.input_file_name())
    )
\end{lstlisting}

\begin{itemize}
  \item \code{spark.read.text(file\_paths)}: mahouch \code{spark.read.json(...)}
        el direct -- ya9ra kol line ki string bark (column esmha
        \code{value} b default), \emph{bidoun} parsing. El parsing el
        7a9i9i ye-sir ba3d, explicit, b \code{parse\_source} (bech ye9dar
        el pipeline ye-handle malformed lines manuellement -- \code{spark.read.json}
        el direct ye-drop walla ye-corrupt rows ghalta bidoun control
        precis 3la el behavior).
  \item \code{.withColumnRenamed("value", "\_raw")}: esm clair (\code{\_raw})
        machi el default generic (\code{value}).
  \item \code{F.input\_file\_name()}: Spark function li terja3 el path
        mte3 el file li jabet minou kol row -- audit trail (\code{\_source\_file}
        f \code{METADATA\_COLUMNS}), useful l debugging (win jat data
        ghalta).
\end{itemize}

\subsection{\code{\_move\_to\_processed}}

\begin{lstlisting}[style=pystyle]
def _move_to_processed(
    spark: SparkSession, file_paths: list[str], landing_dir: str, ingest_date: str
) -> None:
    processed_dir = f"{landing_dir}/{_PROCESSED_SUBDIR}/{ingest_date}"
    for path in file_paths:
        filename = path.rsplit("/", 1)[-1]
        fs.move(spark, path, f"{processed_dir}/{filename}")
\end{lstlisting}

\code{path.rsplit("/", 1)[-1]}: ye5ou el esm mte3 el file bark (a5er
segment ba3d a5er \code{"/"}) mel path el kamel. \code{fs.move} (mel
\code{common/fs.py}, li chefnah) ye3ayet-lha l kol file, wa7ed wa7ed --
el destination \code{\{landing\}/\_processed/\{ingest\_date\}/\{filename\}}.

\subsection{\code{write\_bronze} -- el orchestration el kamla}

\begin{lstlisting}[style=pystyle]
def write_bronze(
    source_type: str, spark: SparkSession, settings: Settings | None = None
) -> BronzeWriteResult:
    """Ingest one batch from ``source_type``'s landing directory into bronze.

    Idempotent in effect, not in mechanism: once a file is moved to
    ``_processed/``, re-running this against the same landing directory
    finds no files and does no work (no Spark job is even triggered).
    """
    if source_type not in SOURCE_TYPES:
        expected = ", ".join(SOURCE_TYPES)
        raise SchemaError(f"Unknown source type {source_type!r}. Expected one of: {expected}")

    settings = settings or get_settings()
    landing_dir = landing_path(source_type, settings)

    file_paths = fs.list_files(spark, landing_dir)
    if not file_paths:
        return BronzeWriteResult(source_type=source_type, written=0, quarantined=0)

    ingested_at = datetime.now(UTC)
    ingest_date = ingested_at.date().isoformat()

    raw = _read_landing_lines(spark, file_paths)
    raw = with_ingestion_metadata(raw, source_type, ingested_at)
    parsed = parse_source(raw, source_type)
    good, bad = split_quarantine(parsed)

    bronze_df = finalize_bronze(good, source_type).cache()
    quarantine_df = finalize_quarantine(bad).cache()

    written = bronze_df.count()
    quarantined = quarantine_df.count()

    bronze_df.write.format("delta").mode("append").partitionBy("ingest_date").option(
        "mergeSchema", "true"
    ).save(bronze_path(source_type, settings))

    if quarantined:
        quarantine_df.write.format("delta").mode("append").partitionBy("ingest_date").option(
            "mergeSchema", "true"
        ).save(quarantine_path(source_type, settings))

    bronze_df.unpersist()
    quarantine_df.unpersist()

    _move_to_processed(spark, file_paths, landing_dir, ingest_date)

    return BronzeWriteResult(source_type=source_type, written=written, quarantined=quarantined)
\end{lstlisting}

\begin{itemize}
  \item \code{if source\_type not in SOURCE\_TYPES}: guard clause l'awel --
        fail fast b message clair ken \code{source\_type} ghalt (typo
        mathalan), 9bal ma yebda 7atta ye-touch Spark.
  \item \code{file\_paths = fs.list\_files(...)} \emph{marra wa7da}, w
        \code{if not file\_paths: return ...(written=0, quarantined=0)}:
        hedha el "empty landing = normal, not error" (kifma chefna
        f \code{fs.list\_files}), w hedhi el step el critique el
        docstring ye-emphasize: el list mte3 files "captured once, up
        front" -- file jdid li yjini \emph{f nos} el batch run (concurrent
        write mel honeypot) ma-yetahdech l hedha el run, y-processa el
        marra el jayya.
  \item \code{ingested\_at = datetime.now(UTC)}: instant wa7ed mel batch
        kamel, ye3addiwh l \code{with\_ingestion\_metadata} (kifma chefna,
        \code{F.lit} machi \code{current\_timestamp}).
  \item Pipeline el transforms: \code{\_read\_landing\_lines} $\to$
        \code{with\_ingestion\_metadata} $\to$ \code{parse\_source} $\to$
        \code{split\_quarantine} $\to$ \code{finalize\_bronze}/\code{finalize\_quarantine}
        -- kol step houwa function pure mel \code{bronze\_transform.py},
        el orchestrateur ye-chain-hom bark.
  \item \code{.cache()} 3la \code{bronze\_df} w \code{quarantine\_df}:
        el DataFrames rahoum lazy -- bidoun cache, kol \code{.count()}
        w kol \code{.write} y-recompute el plan el kamel mel awel
        (mel \code{spark.read.text} l fou9). \code{.cache()} y5alli
        Spark ye5azzen el result f memory ba3d el execution el oula,
        bech el \code{.count()} (li ye5dem action) w el \code{.write}
        (action o5ra) ma-y3awdouch nafs el shughol.
  \item \code{written = bronze\_df.count()}: hedhi el action el oula li
        t-force el execution -- terja3 el row count el 7a9i9i (mous-ta3mal
        f el \code{BronzeWriteResult}).
  \item \code{.write.format("delta").mode("append").partitionBy("ingest\_date").option("mergeSchema", "true")}:
        \code{mode("append")}: bronze table ye-grow bark, ma-ye-overwrite-ch
        (contra gold tables, li ye-overwrite kif nchoufou ba3dein).
        \code{partitionBy("ingest\_date")}: physical partitioning 3la
        el disque b date -- ye5alli queries li ye-filter 3la date range
        (mathalan "el a5er 7 iyem") ma-y9raouch el table kamla.
        \code{mergeSchema=true}: ken schema mte3 cowrie yetbeddel (field
        jdid f version jdida mte3 cowrie), Delta ye9bal el evolution
        automatiquement, machi ye-crash 3la mismatch.
  \item \code{if quarantined:}: write mte3 quarantine table \emph{conditionnel}
        -- ken \code{quarantined == 0} (kol el lines valides), ma-fama-ch
        7ajja te3mel write 3la table fargh.
  \item \code{.unpersist()}: yfalless el cache mel memory ba3d el writes
        -- 3ks \code{.cache()}, bech ma-yeb9ach memory mou7tal bidoun
        7ajja ba3d ma el data temmet el write.
  \item \code{\_move\_to\_processed(...)} \emph{fi el a5er}, ba3d el
        writes kamlin (bronze w quarantine) -- ordre muhim: ken el write
        ye-fail, el files ye-b9aou f landing (ma-yet7arkouch), w el run
        el jayya ye3awed y7awel -- exactement el "at-least-once" el
        docstring ye-describe.
\end{itemize}

\chapter{\file{transform/silver/} -- el schema unifié w el mapping mte3 cowrie}

Hedhi el couche ta5ou bronze rows (raw-ish, source-specific: struct
\code{cowrie} b fields mte3 cowrie nafsou) w t7awwel-hom l schema unifié
wa7ed (\code{SILVER\_EVENT\_SCHEMA}) -- 18 columns, common l kol source
(walaw PFA 3andha source wa7ed bark: cowrie).

\section{\file{transform/silver/schema.py}}

Hedha el file houwa "contract" el kbir -- kol mapper (hna, \code{cowrie.py}
bark) lezem yentej rows li ye-match had el schema exactement.

\subsection{Semantique mte3 kol field -- mel docstring}

Docstring mte3 el file ye-explique kol field b tafsil, w hedhi el fikra el
kbira: hedha schema \emph{original}, machi mou5ta mel benchmark external
(kif MITRE ATT\&CK walla CVE taxonomy).

\begin{lstlisting}[style=pystyle]
event_id
    ``sha2(source_type || ':' || _record_hash, 256)``. A content hash, not a
    random UUID: re-processing the same bronze row (e.g. after the
    at-least-once re-ingestion gap documented in
    ``threatlake.ingestion.bronze_writer``) always produces the same
    event_id, which is what makes exact-duplicate dedup
    (``threatlake.transform.silver.dedup``) possible.
\end{lstlisting}

\begin{soual}
\textbf{3lech \code{event\_id} houwa content hash (\code{sha2(...)}) mouch
UUID random (\code{uuid4()} mathalan)?}

Réponse: kifma chefna f \code{bronze\_writer.py}, el ingestion houwa
"at-least-once" -- file momken ye-ingest twice (crash bin write w move).
Ken \code{event\_id} ykoun UUID random, kol re-ingestion tebni row jdida
b \code{event\_id} mo5telfa -- duplicate 7a9i9i fi silver, w gold
aggregations (attacker\_profiles, attack\_timeline) ye-count el attack
marat 3adda. B content hash (\code{sha2(source\_type || \_record\_hash)}),
nafs el bronze row (nafs \code{\_record\_hash}, mel \code{with\_ingestion\_metadata}
f \code{bronze\_transform.py}) dima tentej nafs \code{event\_id} -- fa
dedup ye-sir automatiquement (row jdid m3a \code{event\_id} mawjoud deja
ye-drop-ha dedup step).
\end{soual}

\begin{lstlisting}[style=pystyle]
attack_category
    A small, original, coarse taxonomy chosen for cross-source filtering, NOT
    derived from any external standard: recon, credential_access,
    command_execution, malware_delivery, network_intrusion, service_probe,
    service_start, session_summary, connection, network_flow, other.

severity
    Normalized to 1-5, 5 = most severe/critical, 1 = informational. Chosen
    deliberately opposite to Suricata's native scale, where 1 is the highest
    priority - seeing "severity 5" mean "worst" is the more common SOC/
    dashboard convention, so Suricata's severity is inverted on the way in
    (native 1 -> 5, 2 -> 3, 3 -> 1).
\end{lstlisting}

\begin{itemize}
  \item \code{attack\_category} 11-valeur taxonomy: 3andha 9rar design
        muhim -- "original", machi standard external. El fikra: kol source
        (cowrie, w f ThreatLake AI el kbir: suricata, dionaea, heralding
        zeda) 3andou vocabulary mo5tlef kamel l events mte3ou -- taxonomy
        mouwahhad ye5alli \code{WHERE attack\_category = 'credential\_access'}
        ye5dem cross-source bidoun ma ta3raf source-specific event names.
  \item \code{severity} 1--5, 5=worst: hedhi el convention SOC/dashboard
        el 3adiya (opposé mte3 Suricata native scale, li 1=highest --
        mentioned l context walaw PFA cowrie-only ma3andhach Suricata).
        Cowrie \emph{ma3andouch} native severity concept 7atta wa7ad --
        el mapping (\code{\_CATEGORY\_SEVERITY} f \code{cowrie.py}) houwa
        "an original, documented, intentionally simple per-eventid ...
        judgment call".
\end{itemize}

\begin{lstlisting}[style=pystyle]
raw_ref
    ``source_type || ':' || _record_hash`` - unhashed and directly usable to
    look up the exact bronze row and its full raw JSON:
    ``SELECT * FROM bronze.raw_events WHERE source_type = split(raw_ref, ':')[0]
    AND _record_hash = split(raw_ref, ':')[1]``.

Dropped from every source (available via raw_ref, not carried into silver):
free-text fields with no analytic structure beyond what `description`
already summarizes.
\end{lstlisting}

\begin{analogy}
\code{raw\_ref} kifha "receipt number" 3nd magasin -- el ticket el mou5tasar
(silver row) ma-fihech kol details (kol item, kol prix), walakin fih
receipt number li ye5allik terja3 l el original document (bronze row, w
mennou \code{\_raw} el JSON el kamel). Hedhi el fikra: silver ye5od bark
el fields el importants l'analytics, walakin ma-yfout-ch chay -- kol chay
mawjoud, just "wara" (mch f el silver row nafsou).
\end{analogy}

\subsection{\code{SilverSchemaError} w \code{ATTACK\_CATEGORIES}}

\begin{lstlisting}[style=pystyle]
class SilverSchemaError(RuntimeError):
    """Raised when mapped rows violate the unified schema's NOT NULL columns."""


ATTACK_CATEGORIES = frozenset(
    {
        "recon",
        "credential_access",
        "command_execution",
        "malware_delivery",
        "network_intrusion",
        "service_probe",
        "service_start",
        "session_summary",
        "connection",
        "network_flow",
        "other",
    }
)
\end{lstlisting}

\code{frozenset} machi \code{set} 3adi walla \code{tuple}: \code{frozenset}
immutable (nafs falsafa: fi kol codebase, constants global lezmohom
ykounou immutable) w set membership check (\code{"x" in ATTACK\_CATEGORIES})
houwa $O(1)$ (hash lookup) machi $O(n)$ (linear scan kif tuple/list) -- mahich
critique l 11 values bark, walakin el habit el correcte.

\subsection{\code{SILVER\_EVENT\_SCHEMA} -- el 18 fields}

\begin{lstlisting}[style=pystyle]
SILVER_EVENT_SCHEMA = StructType(
    [
        StructField("event_id", StringType(), False),
        StructField("event_time", TimestampType(), True),
        StructField("ingest_date", DateType(), False),
        StructField("source_type", StringType(), False),
        StructField("source_event_type", StringType(), True),
        StructField("src_ip", StringType(), True),
        StructField("src_port", IntegerType(), True),
        StructField("dst_ip", StringType(), True),
        StructField("dst_port", IntegerType(), True),
        StructField("protocol", StringType(), True),
        StructField("attack_category", StringType(), True),
        StructField("severity", IntegerType(), True),
        StructField("session_id", StringType(), True),
        StructField("credentials_attempted", BooleanType(), False),
        StructField("attempted_username", StringType(), True),
        StructField("attempted_password", StringType(), True),
        StructField("payload_hash", StringType(), True),
        StructField("description", StringType(), True),
        StructField("raw_ref", StringType(), False),
    ]
)
\end{lstlisting}

\begin{itemize}
  \item El 3eme argument mte3 kol \code{StructField} houwa \code{nullable}
        (\code{True}/\code{False}). Bark 5 fields \code{False} (NOT NULL):
        \code{event\_id}, \code{ingest\_date}, \code{source\_type},
        \code{credentials\_attempted}, \code{raw\_ref} -- kol el baa9i
        (\code{event\_time}, \code{src\_ip}, \code{attack\_category}...)
        momken ykounou null.
  \item 3lech hedhoula 5 el 5amsa specifiquement NOT NULL: \code{event\_id}
        w \code{raw\_ref} ye-derive mel \code{\_record\_hash} (dima mawjoud,
        mel bronze metadata). \code{ingest\_date} w \code{source\_type}
        zeda mel bronze metadata (dima populated, kifma chefna f
        \code{with\_ingestion\_metadata}). \code{credentials\_attempted}
        houwa boolean li lezem ykoun true/false explicit (\emph{"always
        true/false, never null"} kifma el docstring te9oul) -- ma-fama-ch
        "unknown" state.
  \item \code{event\_time} \emph{momken ykoun null}: houwa el field el
        akthar "delicate" nadhariyan (el UTC verification f \code{spark.py}
        ye-concern-ha direct), walakin ye-b9a nullable 3la 5atr timestamp
        mte3 source momken ykoun missing/unparseable -- el row ye-b9a
        (mahich rejected), just bidoun confirmed event time.
\end{itemize}

\subsection{\code{conform\_to\_silver\_schema}}

\begin{lstlisting}[style=pystyle]
def conform_to_silver_schema(df: DataFrame) -> DataFrame:
    """Select and cast ``df`` to exactly :data:`SILVER_EVENT_SCHEMA` - column
    names, order, and types - then verify the schema's NOT NULL columns are
    actually non-null.

    That second step matters: ``.cast()`` only changes a column's *type*.
    Spark infers a DataFrame's nullability from the expression tree that
    produced it, not from a target StructType you select against - so
    without an explicit check, a mapper bug that leaves e.g. ``event_id``
    null would silently pass through instead of failing where it happened.
    """
    conformed = df.select(
        *[F.col(field.name).cast(field.dataType).alias(field.name) for field in SILVER_EVENT_SCHEMA]
    )
    _assert_not_null(conformed)
    return conformed
\end{lstlisting}

\begin{soual}
\textbf{Docstring ye9oul \code{.cast()} "only changes a column's type" --
3lech hedhi tafsila muhimma bezef bech tefhemha?}

Réponse: fi Spark, DataFrame's nullability (\code{StructField.nullable})
ma-tji-ch mel target schema li testa3mlou f \code{.select()} -- Spark
ye-infer-ha mel \emph{expression tree} li 5al9et el column. Ye3ni: ken
mapper (\code{cowrie.py}) 3andou bug w y5alli \code{event\_id} null l
row wa7da (mathalan expression ghalta li momken terja3 null), \code{.cast(StringType())}
\emph{ma-y-forceç-ch} el column tkoun NOT NULL -- houwa just ye-cast el
\emph{type}. Bidoun el check el explicite (\code{\_assert\_not\_null}),
el bug hedha ye3addi silently l gold tables, w tel9a event\_id null f
attacker\_profiles walla attack\_timeline ba3d se3at/iyem, s3ib ye-trace
l source mte3ou. B el check explicite, el pipeline ye-fail \emph{fi el
moment el mapper} rah, b message clair (\emph{"This is a mapper bug, not
bad input data"}).
\end{soual}

\subsection{\code{\_assert\_not\_null}}

\begin{lstlisting}[style=pystyle]
def _assert_not_null(df: DataFrame) -> None:
    required = [field.name for field in SILVER_EVENT_SCHEMA if not field.nullable]
    if not required:
        return
    null_counts = df.select(
        *[F.sum(F.col(name).isNull().cast("int")).alias(name) for name in required]
    ).first()
    assert null_counts is not None  # noqa: S101 - always exactly one row from an unconditional select
    violations = {name: null_counts[name] for name in required if null_counts[name]}
    if violations:
        raise SilverSchemaError(
            f"Silver rows violate NOT NULL columns: {violations}. This is a mapper "
            "bug, not bad input data - every mapper must always populate these columns."
        )
\end{lstlisting}

\begin{itemize}
  \item \code{required = [...]}: ye5ou el 5 fields NOT NULL bark (mel
        \code{SILVER\_EVENT\_SCHEMA} li chefnah).
  \item \code{F.sum(F.col(name).isNull().cast("int")).alias(name)}: pattern
        classic l count nulls -- \code{isNull()} yentej boolean column,
        \code{.cast("int")} y-convert-ha l 0/1, \code{F.sum} ye7seb el
        total. Kol el 5 counts ye-t7esbou f query wa7da (\code{.select()}
        b comprehension) machi 5 queries separate -- efficient, action
        wa7da bark (\code{.first()}).
  \item \code{violations = \{name: ... for name in required if
        null\_counts[name]\}}: dict comprehension b filter -- bark el
        fields li 3andhom count $> 0$ (\code{if null\_counts[name]},
        Python truthy 3la int: 0 = False, ay chay o5or = True) yed5elou
        f \code{violations}.
  \item El erreur message ye-includi el dict \code{violations} nafsou
        (mathalan \code{\{'event\_id': 3\}}) -- ta3raf exactement chnowa
        field w kaddech row victime, machi just "something is null".
\end{itemize}

\section{\file{transform/silver/cowrie.py} -- el mapping el kamel}

Hedha el file houwa el mapper el wa7ed el mou-esta3mel fi PFA (cowrie-only).
Docstring ye-documente el mapping field-by-field:

\begin{lstlisting}[style=pystyle]
Mapping (see threatlake.transform.silver.schema for shared field semantics):

  event_time            cowrie.timestamp
  source_event_type     cowrie.eventid (e.g. "cowrie.login.failed")
  src_ip/src_port        cowrie.src_ip/src_port - attacker
  dst_ip/dst_port        cowrie.dst_ip/dst_port - the honeypot. Only populated
                          on connection-context events (session.connect); null
                          elsewhere per Cowrie's own schema (see
                          config/schema/cowrie.py) - join on session_id to
                          recover it for a specific session's other events.
  protocol               lower(cowrie.protocol) - "ssh"/"telnet"
  session_id             cowrie.session
  credentials_attempted  true for cowrie.login.success / cowrie.login.failed
  attempted_username/    cowrie.username/password on those two eventids.
    password             Password auth only: pubkey-auth attempts carry
                          fingerprint/key instead, which are dropped here
                          (available via raw_ref).
  payload_hash            cowrie.shasum on file_download/file_upload
  description             cowrie.message (already human-written by Cowrie)

Dropped entirely (available via raw_ref -> bronze._raw): sensor, late,
realm, outfile/destfile/duplicate, ttylog, size, key/fingerprint.
\end{lstlisting}

\begin{soual}
\textbf{3lech \code{dst\_ip}/\code{dst\_port} null 3la mou3zam el events
(bark \code{session.connect} 3andhom-ha), w el fix el mou9tara7 houwa
"join 3la session\_id"?}

Réponse: cowrie's own schema (mel \code{config/schema/cowrie.py}) ye7ott
\code{dst\_ip}/\code{dst\_port} bark f el event el awel mte3 kol session
(\code{cowrie.session.connect}) -- events el o5rin (login, command, file...)
mte3 \emph{nafs} el session ma-3andhomch-ha (redundant l cowrie logger
nafsou, 5ali tekteb-ha kol marra). El mapper ma-y7awelch "reconstruct" walla
"forward-fill" hedha el value -- ye5alliha kifma jat (null). Ken analyst
7eb ya3raf el honeypot IP/port l event m3ayen (mathalan login.failed),
lezemou \code{JOIN} 3la \code{session\_id} m3a el \code{session.connect}
event mte3 nafs el session. Hedha 9rar design: keep el data kif jatek
(honest, machi inferred), w 5alli el "join logic" l el consumer/query,
mouch tzid complexity fi el mapper l chay li mahouch always needed.
\end{soual}

\subsection{\code{\_LOGIN\_EVENTS} w \code{\_FILE\_EVENTS}}

\begin{lstlisting}[style=pystyle]
_LOGIN_EVENTS = ("cowrie.login.success", "cowrie.login.failed")
_FILE_EVENTS = ("cowrie.session.file_download", "cowrie.session.file_upload")
\end{lstlisting}

Zouz tuples sghar, kol wa7ed ye-group eventids li 3andhom nafs el
"treatment" f el mapping (login events $\to$ credentials\_attempted +
username/password, file events $\to$ payload\_hash).

\subsection{\code{\_CATEGORY\_SEVERITY} -- el jadwal el kamel}

Hedha el "table" el user 7eb tekoun mfasra kamla -- kol \code{eventid}
mte3 cowrie mapped l \code{(attack\_category, severity)}:

\begin{lstlisting}[style=pystyle]
# eventid -> (attack_category, severity). severity: 5=critical .. 1=informational.
_CATEGORY_SEVERITY = {
    "cowrie.session.connect": ("connection", 1),
    "cowrie.login.success": ("credential_access", 5),  # attacker got in
    "cowrie.login.failed": ("credential_access", 2),
    "cowrie.command.input": ("command_execution", 4),
    "cowrie.session.file_download": ("malware_delivery", 4),
    "cowrie.session.file_upload": ("malware_delivery", 4),
    "cowrie.session.closed": ("session_summary", 1),
    "cowrie.log.closed": ("session_summary", 1),
}
_DEFAULT_CATEGORY_SEVERITY = ("other", 2)
\end{lstlisting}

\begin{itemize}
  \item \code{cowrie.session.connect} $\to$ (\code{connection}, 1): just
        TCP connection etabli, 7atta authentication ma-sartech ba3d --
        severity el a9al (1, informational). Category \code{connection}
        houwa el wahda el bark mte3ha specifique l "network-level" event
        (mahouch behavior mte3 el attacker ba3d).
  \item \code{cowrie.login.success} $\to$ (\code{credential\_access}, 5):
        el event el akthar critique -- attacker \emph{da5al} el honeypot
        b succès. Comment inline (\code{\# attacker got in}) ye-explique
        3lech severity 5 (max) -- login success houwa el signal el akthar
        actionable/dangereux f el system kamel.
  \item \code{cowrie.login.failed} $\to$ (\code{credential\_access}, 2):
        nafs category (\code{credential\_access}, 3la 5atr houwa zeda
        "attempt" 3la credentials) walakin severity ba3ida barcha (2,
        low-ish) -- failed attempts homa el mou3zam mte3 el traffic 3la
        honeypot (barcha brute-force noise), fa severity ba3ida t5alli
        el dashboard ma-yeb9ach flooded b "alerts" l chay mtawa9a3.
  \item \code{cowrie.command.input} $\to$ (\code{command\_execution}, 4):
        attacker deja da5al w rah ye5dem commands 3la el shell -- severity
        3alya (4) 3la 5atr hedha behavior actif w potentiellement
        destructif (attacker momken ye7awel install malware, exfiltrate
        data...).
  \item \code{cowrie.session.file\_download} w \code{cowrie.session.file\_upload}
        $\to$ (\code{malware\_delivery}, 4): kol el 2 (upload w download)
        nafs el category w severity -- el fikra: file transfer (fi ay
        direction) 3la honeypot houwa \emph{presque dima} malware-related
        (attacker ye7ott payload, walla ye-download tool).
  \item \code{cowrie.session.closed} w \code{cowrie.log.closed} $\to$
        (\code{session\_summary}, 1): events "housekeeping" -- el session
        temmet, ma-3andhomch-ch signal nouveau 3la el attacker behavior
        (el behavior deja captured mel events el sa-b9in), severity el
        a9al.
  \item \code{\_DEFAULT\_CATEGORY\_SEVERITY = ("other", 2)}: fallback l
        eventid mahouch f el dict -- category \code{"other"} (mawjouda f
        \code{ATTACK\_CATEGORIES}) w severity metoseta (2). Hedhi ye5alli
        el mapper ma-y-crash-ch 3la eventid jdid (cowrie update, feature
        jdida) -- el row ye-b9a mapped (bidoun ma tel9a exception), just
        b category generic 7atta tzid entry jdid f el dict.
\end{itemize}

\begin{soual}
\textbf{Kifeh 9arrart el severity mte3 kol eventid -- (5, 2, 4, 4, 4, 1,
1)? Fama methodology 7a9i9iya walla judgment call?}

Réponse: docstring mte3 el file el kbir (\code{schema.py}) ye9oul directly:
\emph{"The other three sources have no native severity concept at all;
their mapping is an original, documented, intentionally simple
per-eventid/connection-type judgment call"}. Ye3ni: mahiche derived men
CVSS walla standard external -- houwa 9rar design mou5tasar w documenté,
mbni 3la "kaddech hedha el event ye3ni chay actionable/dangereux l analyst
SOC". \code{login.success} (5, max) houwa el akthar objectivement critique
(attacker got in). \code{command.input} w file transfer (4) homa
"post-compromise activity". \code{login.failed} (2) low 3la 5atr houwa
noise (barcha minha, mou3zam-hom automated bots). \code{connection}/\code{closed}
(1) houwa metadata bark, machi signal behavior. El transparence houwa el
9ima hna: el comment inline w el docstring ye5alliw kol reader (jury
mathalan) ye-verifier el logique w ye-conteste-ha ken 7eb, contra "black
box scoring" bidoun rationale.
\end{soual}

\subsection{\code{\_category\_severity\_columns} -- kifeh el dict ye-b9a Spark expression}

\begin{lstlisting}[style=pystyle]
def _category_severity_columns() -> tuple[Column, Column]:
    # Each eventid is mutually exclusive, so iteration order doesn't matter -
    # every branch below only ever overrides the still-default value.
    category = F.lit(_DEFAULT_CATEGORY_SEVERITY[0])
    severity = F.lit(_DEFAULT_CATEGORY_SEVERITY[1])
    for eventid, (cat, sev) in _CATEGORY_SEVERITY.items():
        is_match = F.col("cowrie.eventid") == eventid
        category = F.when(is_match, F.lit(cat)).otherwise(category)
        severity = F.when(is_match, F.lit(sev)).otherwise(severity)
    return category, severity
\end{lstlisting}

\begin{itemize}
  \item El fikra el jdida hna: el dict Python (\code{\_CATEGORY\_SEVERITY})
        \emph{mahouch} 9adar ye-esta3mel direct f Spark expression (Spark
        ma-y3arraf-ch \code{dict.get()} 3la column mte3ou -- lezem kol
        chay ykoun \code{Column} expression). Fa el function hedhi
        t-convert el dict l \emph{chain} mte3 \code{F.when(...).otherwise(...)}
        expressions.
  \item \code{category = F.lit(\_DEFAULT...)}: yebda b el default (\code{"other"})
        ki starting point.
  \item El loop: kol iteration, \code{category = F.when(is\_match, F.lit(cat)).otherwise(category)}
        -- hedhi \emph{ma tbeddelch} el value fil-7in (Spark expressions
        immutable, lazy), houwa ye-build expression jdid \emph{yelfef}
        el expression el 9dim (\code{otherwise(category)}). Ba3d el loop
        kamel, \code{category} houwa chain twil mte3 8 \code{when} clauses
        (wa7ed l kol eventid f \code{\_CATEGORY\_SEVERITY}), b el default
        f el a5er (a3ma9 nested \code{otherwise}).
  \item Comment (\emph{"Each eventid is mutually exclusive, so iteration
        order doesn't matter"}): 3la 5atr kol row 3andha \code{eventid}
        wa7ed bark, bark \code{when} wa7ed momken y-match (el ba9i
        \code{is\_match} houm False l nafs el row), fa el ordre eli el
        dict ye-iterate bih (Python 3.7+ dict ye-preserve insertion order,
        walakin hna ma-yehemch) ma-yathrouch 3la result el akhir.
\end{itemize}

\begin{analogy}
\code{F.when(...).otherwise(...)} chain houwa el equivalent Spark mte3
\code{if/elif/elif/.../else} statement el 3adi f Python -- walakin
\emph{expression-based} (yentej value, machi statement li ye-execute).
Nested \code{otherwise} kifha "nesting" mte3 conditions -- kol wa7ed
ye-check 9bal ma yerja3 l el "else" (el default walla el when el 9bel).
\end{analogy}

\subsection{\code{map\_cowrie} -- el function el kamla}

\begin{lstlisting}[style=pystyle]
def map_cowrie(bronze_df: DataFrame) -> DataFrame:
    """Map bronze rows with ``source_type = 'cowrie'`` to the unified silver model."""
    df = bronze_df.filter((F.col("source_type") == "cowrie") & F.col("cowrie").isNotNull())

    is_login_event = F.col("cowrie.eventid").isin(*_LOGIN_EVENTS)
    is_file_event = F.col("cowrie.eventid").isin(*_FILE_EVENTS)
    attack_category, severity = _category_severity_columns()

    mapped = df.select(
        F.sha2(F.concat(F.col("source_type"), F.lit(":"), F.col("_record_hash")), 256).alias(
            "event_id"
        ),
        F.to_timestamp(F.col("cowrie.timestamp"), "yyyy-MM-dd'T'HH:mm:ss.SSSSSS'Z'").alias(
            "event_time"
        ),
        F.col("ingest_date").alias("ingest_date"),
        F.col("source_type").alias("source_type"),
        F.col("cowrie.eventid").alias("source_event_type"),
        F.col("cowrie.src_ip").alias("src_ip"),
        F.col("cowrie.src_port").alias("src_port"),
        F.col("cowrie.dst_ip").alias("dst_ip"),
        F.col("cowrie.dst_port").alias("dst_port"),
        F.lower(F.col("cowrie.protocol")).alias("protocol"),
        attack_category.alias("attack_category"),
        severity.alias("severity"),
        F.col("cowrie.session").alias("session_id"),
        is_login_event.alias("credentials_attempted"),
        F.when(is_login_event, F.col("cowrie.username")).alias("attempted_username"),
        F.when(is_login_event, F.col("cowrie.password")).alias("attempted_password"),
        F.when(is_file_event, F.col("cowrie.shasum")).alias("payload_hash"),
        F.col("cowrie.message").alias("description"),
        F.concat(F.col("source_type"), F.lit(":"), F.col("_record_hash")).alias("raw_ref"),
    )
    return conform_to_silver_schema(mapped)
\end{lstlisting}

\begin{itemize}
  \item \code{df = bronze\_df.filter((F.col("source\_type") == "cowrie")
        \& F.col("cowrie").isNotNull())}: double filter -- \code{source\_type
        == "cowrie"} (defensive, walaw PFA cowrie-only, el function
        te-check-ha explicit) w \code{cowrie IS NOT NULL} (row lezemha
        el struct \code{cowrie} populated, machi struct fargh/null).
  \item \code{is\_login\_event} w \code{is\_file\_event}: 2 boolean column
        expressions (\code{.isin(*tuple)}) mou7sbin \emph{marra wa7da},
        w mous-ta3mlin barcha marat ba3d (f \code{credentials\_attempted},
        \code{attempted\_username}, \code{attempted\_password},
        \code{payload\_hash}) -- DRY (Don't Repeat Yourself), machi
        \code{F.col("cowrie.eventid").isin(...)} mekrar f kol place.
  \item \code{event\_id}: \code{sha2(concat(source\_type, ":", \_record\_hash))}
        -- exactement kifma el docstring mte3 \code{schema.py} 9al.
  \item \code{event\_time}: \code{F.to\_timestamp(..., "yyyy-MM-dd'T'HH:mm:ss.SSSSSS'Z'")}
        -- format string explicite (ISO 8601 b microseconds w \code{Z}
        literal l UTC marker), machi \code{F.to\_timestamp} bidoun format
        (li y3awel 3la inference automatique, momken tefchel walla ta5ou
        format ghalt).
  \item \code{dst\_ip}/\code{dst\_port}: direct mel struct, \emph{bidoun}
        ay "fill" walla "forward" logic (kifma et9echna fi el Soual box
        el fou9).
  \item \code{protocol}: \code{F.lower(...)} -- normalisation basic
        (cowrie momken ye-log-ha b capitalisation mo5tlifa, "SSH" vs
        "ssh").
  \item \code{attempted\_username}/\code{attempted\_password}: \code{F.when(is\_login\_event,
        ...)} \emph{bidoun} \code{.otherwise(...)} -- ken \code{is\_login\_event}
        False, \code{F.when} bidoun otherwise yrajja3 \code{null}
        automatiquement (default behavior mte3 Spark). Hedhi el tari9a
        el as-hal ye-express "populate bark 3la certain rows, null 3la
        el baa9i" bidoun ma tekteb \code{.otherwise(F.lit(None))} explicite.
  \item \code{raw\_ref}: nafs el concat expression kif \code{event\_id}
        walakin \emph{bidoun} hash (\code{sha2}) -- unhashed, direct
        usable l lookup (kifma el docstring te-explique b el SQL example).
  \item \code{return conform\_to\_silver\_schema(mapped)}: el a5er step
        -- ye-verify eli el result ye-match \code{SILVER\_EVENT\_SCHEMA}
        exactement (order, types, NOT NULL constraints).
\end{itemize}

\chapter{\file{transform/gold/} -- el 2 tables el akhrin}

Gold houwa el couche el a5ira -- aggregations ready l'API/dashboard.
PFA 3andha 2 gold tables bark: \code{attacker\_profiles} (wa7ed l kol IP)
w \code{attack\_timeline} (par heure). Kol wa7da mennhom mbniya 3la
\code{writer.py} el mouchtarek.

\section{\file{transform/gold/writer.py}}

\begin{lstlisting}[style=pystyle]
Every gold table in this package is a full recompute from silver on each
run - not an incremental/merge update. ``mode("overwrite")`` makes reruns
idempotent by construction: the previous version of the table is entirely
replaced by the newly computed one, so re-running against unchanged silver
data produces a byte-identical table, and re-running after new silver data
arrives produces the correct up-to-date aggregate with no leftover or
duplicate rows from a prior run.

This trades incremental-update efficiency for simplicity and unconditional
correctness. Reasonable at ThreatLake's current data volumes; a production
system at much larger scale would likely partition-scope these overwrites
(``replaceWhere``) or move to MERGE-based incremental updates instead of
recomputing full history on every run.
\end{lstlisting}

\begin{soual}
\textbf{3lech "full recompute" (overwrite kamel) mouch "incremental update"
(merge bark el data el jdida)? Mahouch wasteful, especially ken silver
table tkabber?}

Réponse: el docstring nafsou honest 3la had el trade-off: \emph{"trades
incremental-update efficiency for simplicity and unconditional
correctness"}. El sabab el 7a9i9i (mel \code{attacker\_profiles.py}'s
docstring): aggregate metrics ki \code{first\_seen} w \code{top\_credentials\_tried}
momken ye-t2ather b \emph{ay} event jdid l nafs el IP, \emph{f ay wa9t}
fi el history -- machi bark events jdida. Ye3ni ma-fama-ch "natural
incremental partition boundary" -- event jdid l IP deja mawjoud momken
ye-jib-lou credential jdid li ye-changi el top-5 ranking mte3ou kamel,
walla event b timestamp a9dam (edge case walakin possible) ye-changi
\code{first\_seen}. Full recompute houwa el simplest way tguarantee
correctness \emph{unconditional}. El docstring ye9oul explicitement:
had el trade-off ye5dem "at ThreatLake's current data volumes" -- machi
claim eli hedha el approach ye-scale bidoun limite; solution production
akbar (\code{replaceWhere} partition-scoped, walla MERGE incremental)
houma "future extension" mou3trafin, machi hidden limitation.
\end{soual}

\subsection{\code{write\_gold\_table}}

\begin{lstlisting}[style=pystyle]
def write_gold_table(
    df: DataFrame,
    table: str,
    settings: Settings | None = None,
    partition_by: list[str] | None = None,
) -> None:
    """Fully overwrite the gold Delta table ``table`` with ``df``."""
    settings = settings or get_settings()
    writer = df.write.format("delta").mode("overwrite").option("overwriteSchema", "true")
    if partition_by:
        writer = writer.partitionBy(*partition_by)
    writer.save(gold_path(table, settings))
\end{lstlisting}

\begin{itemize}
  \item \code{partition\_by: list[str] | None = None}: optional, machi
        kol gold table t7eb partitioning. \code{attacker\_profiles} (grain
        wa7ed l kol IP, mahich time-series) ma ye5dmech b partitioning
        (ye3ayet-lha bidoun \code{partition\_by}), \code{attack\_timeline}
        (b \code{hour}/\code{date}) ye5dem \code{partition\_by=["date"]}.
  \item \code{.option("overwriteSchema", "true")}: ye5alli el schema
        yetbeddel bin runs (mathalan ken tzid column jdid l aggregate)
        \emph{bidoun} ma yehtej drop-and-recreate manuel mte3 el table --
        Delta b default ye-reject overwrite ken el schema differ, hedhi
        el option ye-override el behavior el hedha.
  \item \code{if partition\_by: writer = writer.partitionBy(*partition\_by)}:
        pattern "builder" -- \code{writer} object ye-accumulate config
        (\code{.format}, \code{.mode}, \code{.option}, w conditionnellement
        \code{.partitionBy}) 9bal el \code{.save()} el akhir, li houwa
        el action el 7a9i9i.
\end{itemize}

\section{\file{transform/gold/attacker\_profiles.py}}

\begin{lstlisting}[style=pystyle]
GRAIN: one row per ``src_ip``. Recomputed in full from the entire silver
table on each run (see ``threatlake.transform.gold.writer``) - an
attacker's ``first_seen``, top credentials, etc. can all be affected by
any new event for that IP anywhere in history, so there is no natural
incremental partition boundary for this table.
\end{lstlisting}

\begin{itemize}
  \item \code{GRAIN}: kelma el data-engineering el standard l "chnowa
        ye-identifier row wa7da f el table" -- hna, \code{src\_ip} wa7ed
        (kol IP 3andou row wa7da bark, machi wa7ed l kol event).
\end{itemize}

\subsection{Kol column, w el 3lech mte3ou}

\begin{lstlisting}[style=pystyle]
Columns:
  src_ip                    key.
  first_seen / last_seen     min/max(event_time).
  total_events                count(*).
  distinct_ports_hit           count(distinct dst_port).
  distinct_honeypots_hit        count(distinct source_type). Always 1 in
                              PFA (cowrie-only, see ARCHITECTURE.md) -
                              kept as a real column rather than dropped,
                              since it is exactly what makes this table's
                              shape unchanged if a second source is added
                              later.
  top_credentials_tried          array of up to 5 ``{username, password,
                              count}`` structs, this IP's most frequent
                              credential pairs, ranked by count desc. Only
                              counts rows with both attempted_username AND
                              attempted_password non-null. Empty array
                              (not null) for an IP with no credential
                              attempts.
  geo                            ``{country, city, latitude, longitude, asn,
                              asn_org}``, looked up ONCE per distinct src_ip
                              via a GeoEnricher, not once per event.
\end{lstlisting}

\begin{soual}
\textbf{3lech \code{distinct\_honeypots\_hit} ye-b9a column real f PFA,
walaw dima 1 (source wa7ed bark: cowrie)? Mahouch redundant/wasteful?}

Réponse: mel docstring direct -- houwa \emph{"exactly what makes this
table's shape unchanged if a second source is added later"}. Ye3ni:
9rar design forward-looking -- ken tzid source jdid (dionaea mathalan)
l PFA fi el moustaqbal, \code{attacker\_profiles} table \emph{ma-tehtejch}
migration walla schema change 7atta wa7ad -- el column deja mawjoud,
just el values ye-badlou min 1 l 1-4. Hedha ye5alli el schema stable
3ber evolution mte3 el scope, machi tzid columns ba3d kol source jdid.
\end{soual}

\begin{soual}
\textbf{3lech \code{top\_credentials\_tried} array fargha (\code{[]}),
mouch \code{null}, l IP li ma-3andou-ch 7atta credential attempt wa7ed?}

Réponse: mel docstring: \emph{"Empty array (not null) for an IP with no
credential attempts"}. Hedha 9rar API/consumer-friendly -- ken el column
tkoun \code{null}, kol consumer (dashboard, API endpoint) lezmou special-case
\code{if credentials is not None else []} 9bal ma ye-iterate 3liha. B
empty array dima, el consumer ye9dar ye-loop 3la \code{top\_credentials\_tried}
mba-cher (\code{for c in profile.top\_credentials\_tried}) bidoun 7atta
null-check -- l IP li 3andou 0 attempts, el loop just ma-ye-iterate 3la
7atta wa7ad, bidoun erreur.
\end{soual}

\subsection{\code{\_TOP\_CREDENTIALS\_LIMIT} w \code{\_EMPTY\_CREDENTIALS\_TYPE}}

\begin{lstlisting}[style=pystyle]
_TOP_CREDENTIALS_LIMIT = 5
_EMPTY_CREDENTIALS_TYPE = "array<struct<username:string,password:string,count:bigint>>"
\end{lstlisting}

\code{\_EMPTY\_CREDENTIALS\_TYPE}: string mte3 Spark SQL DDL type (machi
Python type) -- mous-ta3mla ba3dein f \code{F.array().cast(...)} bech
t5al9 array fargh \emph{b el type el correct exactement} (struct b 3
fields specifiques), machi array fargh generic (li Spark momken ye-infer-ha
b type ghalt walla \code{void} type).

\subsection{\code{\_top\_credentials\_by\_ip} -- top-N par groupe}

\begin{lstlisting}[style=pystyle]
def _top_credentials_by_ip(df: DataFrame) -> DataFrame:
    """One row per src_ip: its top _TOP_CREDENTIALS_LIMIT (username, password) pairs by count."""
    creds = df.filter(
        F.col("attempted_username").isNotNull() & F.col("attempted_password").isNotNull()
    )
    counted = creds.groupBy("src_ip", "attempted_username", "attempted_password").agg(
        F.count(F.lit(1)).alias("cred_count")
    )
    ranked = counted.withColumn(
        "rank",
        F.row_number().over(
            Window.partitionBy("src_ip").orderBy(
                F.col("cred_count").desc(), F.col("attempted_username")
            )
        ),
    ).filter(F.col("rank") <= _TOP_CREDENTIALS_LIMIT)

    return (
        ranked.groupBy("src_ip")
        .agg(
            F.sort_array(
                F.collect_list(F.struct("cred_count", "attempted_username", "attempted_password")),
                asc=False,
            ).alias("_ranked")
        )
        .withColumn(
            "top_credentials_tried",
            F.transform(
                F.col("_ranked"),
                lambda s: F.struct(
                    s["attempted_username"].alias("username"),
                    s["attempted_password"].alias("password"),
                    s["cred_count"].alias("count"),
                ),
            ),
        )
        .drop("_ranked")
    )
\end{lstlisting}

\begin{itemize}
  \item \code{creds = df.filter(... isNotNull() \& ... isNotNull())}:
        filter l'awel -- bark rows b credentials 7a9i9iyin (zouz mel 2:
        username \emph{w} password mawjoudin).
  \item \code{counted = creds.groupBy("src\_ip", "attempted\_username",
        "attempted\_password").agg(F.count(...))}: group by 3 columns
        (IP + username + password) -- yentej "kaddech marra hedha el IP
        7awel had el credential pair exactement".
  \item El "top-N per group" pattern -- hedha el pattern el classic Spark
        l "top N par IP": \code{F.row\_number().over(Window.partitionBy("src\_ip").orderBy(...))}
        y7ott rank (1, 2, 3...) l kol credential pair \emph{jowa} kol IP
        (partition), ordoné b \code{cred\_count desc} (el akthar frequent
        el awel). \code{F.col("attempted\_username")} ki tie-breaker
        (secondary sort key) ye5alli el result deterministe ken zouz
        credentials 3andhom nafs el count exactement (bidoun-ha, Spark
        momken yerja3 ordre mo5tlef bin runs 3la nafs el data, 3la 5atr
        distributed execution).
  \item \code{.filter(F.col("rank") <= \_TOP\_CREDENTIALS\_LIMIT)}: ye5alli
        bark el top-5 (rank 1 l 5) l kol IP.
  \item \code{F.collect\_list(F.struct(...))} + \code{F.sort\_array(...,
        asc=False)}: ba3d ma el filter ye5alli el top-5 bark, el
        \code{groupBy("src\_ip")} el theni ye-collect-hom f array wa7ed
        (\code{collect\_list}), w \code{sort\_array} ye-re-sort-hom
        (defensive -- \code{collect\_list} ma-guarantee-ch ordre stable
        3la distributed data, fa el sort el a5er hna houwa el guarantee
        el 7a9i9i eli el array output ordoné b \code{cred\_count desc}).
  \item \code{F.transform(F.col("\_ranked"), lambda s: F.struct(...))}:
        \code{F.transform} houwa el equivalent Spark mte3 Python's
        \code{map()} 3la array column -- ye-apply lambda 3la kol element
        mte3 el array (\code{s}), y-rename el fields (\code{cred\_count}
        $\to$ \code{count}) l el shape el akhir el mou9arrar
        (\code{username}, \code{password}, \code{count}).
  \item \code{.drop("\_ranked")}: yne99i el column intermediate (esm
        b underscore, convention l "internal/temp column") ba3d ma
        \code{top\_credentials\_tried} tetbena mennha.
\end{itemize}

\begin{analogy}
El pattern "row\_number over Window.partitionBy(...).orderBy(...), ba3d
filter rank $\le$ N" houwa el "top-N per group" el classic f SQL/Spark --
kifha t7eb ta5ou "top 3 étudiants f kol classe": partitionBy("classe"),
orderBy("note" desc), row\_number, w ba3d filter rank $\le$ 3. Nafs el
fikra hna, walakin "classe" = \code{src\_ip} w "note" = \code{cred\_count}.
\end{analogy}

\subsection{\code{build\_attacker\_profiles}}

\begin{lstlisting}[style=pystyle]
def build_attacker_profiles(
    silver_df: DataFrame,
    spark: SparkSession,
    geo_enricher: GeoEnricher | None = None,
) -> DataFrame:
    """Aggregate ``silver_df`` into the one-row-per-src_ip grain described above."""
    base = silver_df.filter(F.col("src_ip").isNotNull())

    profiles = base.groupBy("src_ip").agg(
        F.min("event_time").alias("first_seen"),
        F.max("event_time").alias("last_seen"),
        F.count(F.lit(1)).alias("total_events"),
        F.countDistinct("dst_port").alias("distinct_ports_hit"),
        F.countDistinct("source_type").alias("distinct_honeypots_hit"),
        F.sort_array(F.collect_set("attack_category")).alias("attack_categories"),
    )

    profiles = profiles.join(_top_credentials_by_ip(base), on="src_ip", how="left").withColumn(
        "top_credentials_tried",
        F.coalesce(F.col("top_credentials_tried"), F.array().cast(_EMPTY_CREDENTIALS_TYPE)),
    )
\end{lstlisting}

\begin{itemize}
  \item \code{base = silver\_df.filter(F.col("src\_ip").isNotNull())}:
        filter l'awel -- IP null (event bidoun source IP m3aruf) ma-y9darch
        yetgroupé (\code{groupBy("src\_ip")} 3la null ye5dem, walakin
        yentej row \code{"unknown"} li mahouch useful/meaningful).
  \item \code{groupBy("src\_ip").agg(...)}: el aggregation el asasi --
        \code{F.min}/\code{F.max("event\_time")} l first/last seen,
        \code{F.count(F.lit(1))} l total events (\code{F.lit(1)} machi
        \code{F.col("x")} bech el count ma-y-affecté-ch b nulls f column
        specifique), \code{F.countDistinct} l distinct ports/honeypots,
        w \code{F.sort\_array(F.collect\_set(...))} l categories (distinct
        + sorted, deterministe).
  \item \code{.join(\_top\_credentials\_by\_ip(base), on="src\_ip",
        how="left")}: LEFT join, machi INNER -- 3la 5atr IP momken ma-3andouch
        7atta credential attempt (kol events mte3ou homa connections walla
        commands bark), w lezem yeb9a f el result (b \code{top\_credentials\_tried
        = null} 9bal el coalesce).
  \item \code{F.coalesce(F.col("top\_credentials\_tried"), F.array().cast(\_EMPTY\_CREDENTIALS\_TYPE))}:
        hna el "null $\to$ empty array" conversion el discuté f el Soual
        box el fou9 -- \code{coalesce} yrajja3 l'awel valeur non-null,
        fa IP bidoun credentials (null mel LEFT join) ye5dou empty array
        el typed correctement.
\end{itemize}

\subsection{\code{geo} struct -- b enricher walla bidoun}

\begin{lstlisting}[style=pystyle]
    if geo_enricher is not None:
        profiles = enrich_geo(profiles, spark, geo_enricher, ip_column="src_ip")
        profiles = profiles.withColumn(
            "geo",
            F.struct(
                F.col("geo_country").alias("country"),
                F.col("geo_city").alias("city"),
                F.col("geo_latitude").alias("latitude"),
                F.col("geo_longitude").alias("longitude"),
                F.col("geo_asn").alias("asn"),
                F.col("geo_asn_org").alias("asn_org"),
            ),
        ).drop("geo_country", "geo_city", "geo_latitude", "geo_longitude", "geo_asn", "geo_asn_org")
    else:
        # A present struct with null fields, not a null struct: this must
        # match the shape enrich_geo itself produces for an IP it can't
        # resolve, so `geo.latitude` etc. is always safe to select
        # downstream regardless of whether a GeoEnricher was supplied.
        profiles = profiles.withColumn(
            "geo",
            F.struct(
                F.lit(None).cast("string").alias("country"),
                F.lit(None).cast("string").alias("city"),
                F.lit(None).cast("double").alias("latitude"),
                F.lit(None).cast("double").alias("longitude"),
                F.lit(None).cast("int").alias("asn"),
                F.lit(None).cast("string").alias("asn_org"),
            ),
        )
\end{lstlisting}

\begin{soual}
\textbf{Comment el code ye9oul "must match the shape enrich\_geo itself
produces" -- 3lech had el matching exact houwa lezem, machi just
"good practice"?}

Réponse: ken \code{geo\_enricher} houwa \code{None}, el code ma-y-3addich
\code{geo} column simplement (\code{null} kamel) -- houwa yebni struct
\code{present} b kol el 6 fields \code{null} \emph{jowa}. El sabab: kol
consumer downstream (API, dashboard) ye5dem b syntax \code{profile.geo.latitude}
walla \code{profile["geo"]["latitude"]}. Ken \code{geo} kan \code{null}
(machi struct b fields null), had el access ye-crash (\code{None} ma-3andou-ch
attribute \code{.latitude}) -- consumer lezmou special-case 9bal ma
ya3mel access. B struct present dima (b nulls jowa ken ma fama-ch geo
data), \code{profile.geo.latitude} dima "safe" syntax-wise, sawa el IP
3andou geo data 7a9i9i sawa la -- el difference bark houwa el value
(coordonnée 7a9i9iya vs \code{null}), machi el "shape" nafsou.
\end{soual}

\begin{analogy}
Hedhi kifha template mte3 formulaire fargh -- 7atta ken ma-3andekech
information ("city": unknown), el formulaire nafsou ye-b9a m3andou kol
el champs (name, city, country...), just mel-i-in b "N/A" walla fargh.
Ma tsawwarch formulaire kamel yb9a "missing" just 3la 5atr wahda mel
champs ma3andekech-ha -- structure el formulaire dima consistante.
\end{analogy}

\subsection{\code{write\_attacker\_profiles}}

\begin{lstlisting}[style=pystyle]
def write_attacker_profiles(
    spark: SparkSession,
    settings: Settings | None = None,
    geo_enricher: GeoEnricher | None = None,
) -> DataFrame:
    """Read silver, build attacker_profiles, and idempotently overwrite the gold table."""
    silver_df = spark.read.format("delta").load(silver_path("events", settings))
    profiles = build_attacker_profiles(silver_df, spark, geo_enricher)
    write_gold_table(profiles, "attacker_profiles", settings)
    return profiles
\end{lstlisting}

3 steps clairs: \code{spark.read.format("delta").load(...)} (ta9ra el
silver table kamla mel disque), \code{build\_attacker\_profiles(...)}
(el aggregation pure, li chefna kamla fou9), w \code{write\_gold\_table(...)}
(el write idempotent, mel \code{writer.py}). \code{return profiles}:
yrajja3 el DataFrame zeda (machi \code{None}) -- useful l tests (verify
el content bidoun ma tre-read mel disque) w l orchestration scripts
(\code{run\_pipeline.py}) li 7ebbin y-print row counts mba-cher.

\section{\file{transform/gold/attack\_timeline.py}}

\begin{lstlisting}[style=pystyle]
GRAIN: one row per ``(hour, attack_category, dst_port)``. ``hour`` is
``event_time`` truncated to the top of the hour (UTC - every timestamp in
this project is).

This is the finest-grained time-series gold table: rolling up to
``(hour, attack_category)`` or ``(hour, dst_port)`` alone is a single
``groupBy`` away from this table. The reverse - recovering the port
breakdown from a category-only rollup - is not, so this grain is kept rather
than pre-aggregating a dimension away.

``dst_port`` is nullable and kept that way: rows for application-layer
events with no destination context (e.g. a Cowrie command-input event) group
together under a null port rather than being dropped or assigned an
invented sentinel value.
\end{lstlisting}

\begin{soual}
\textbf{3lech el grain el m5tar houwa \code{(hour, category, port)}, mouch
category bark walla port bark -- 3lech "finest-grained" el ekhtiyar?}

Réponse: mel docstring direct -- \emph{"rolling up ... is a single groupBy
away from this table. The reverse ... is not"}. Hedha principe important
f data modeling: dima 5tar el grain \emph{el akthar fine} li reasonable,
3la 5atr ta9dar dima "t-aggregate l fou9" (rollup, groupBy sghir) mel
grain fine, walakin ma-t9darch "t-disaggregate l ta7t" (mel grain gros
l fine) 3la 5atr el information deja tlefet f el aggregation el oula.
Ken el table 3andha bark \code{(hour, category)}, ma-3andekch tari9a
terja3 l breakdown mte3 ports mel data hedhi -- lezmek te5dem query
jdid mel silver mel awel. B \code{(hour, category, port)} el fine grain,
kol dashboard query (category bark, port bark, walla el zouz) ye5dem
b groupBy basic 3la had el table wa7da.
\end{soual}

\subsection{\code{build\_attack\_timeline}}

\begin{lstlisting}[style=pystyle]
def build_attack_timeline(silver_df: DataFrame) -> DataFrame:
    """Aggregate ``silver_df`` into the hourly (category, port) grain described above."""
    base = silver_df.filter(F.col("event_time").isNotNull())

    timeline = base.groupBy(
        F.date_trunc("hour", F.col("event_time")).alias("hour"),
        F.col("attack_category"),
        F.col("dst_port"),
    ).agg(
        F.count(F.lit(1)).alias("event_count"),
        F.countDistinct("src_ip").alias("distinct_src_ip_count"),
    )

    return timeline.withColumn("date", F.to_date(F.col("hour"))).select(
        "date", "hour", "attack_category", "dst_port", "event_count", "distinct_src_ip_count"
    )
\end{lstlisting}

\begin{itemize}
  \item \code{base = silver\_df.filter(F.col("event\_time").isNotNull())}:
        filter l'awel -- row bidoun \code{event\_time} confirmé (chefna
        f \code{schema.py} eli \code{event\_time} nullable) ma-t9darch
        tetgroupé b heure, fa te5rej mel timeline hedhi (mahich lost --
        raw\_ref dima ye5allik terja3-lha).
  \item \code{F.date\_trunc("hour", F.col("event\_time"))}: ye-truncate
        el timestamp l a3la sa3a (mathalan \code{14:37:22} $\to$
        \code{14:00:00}) -- hedhi el "bucketing" el time-series el standard.
  \item \code{groupBy(hour, attack\_category, dst\_port).agg(...)}: el
        grain el 3 dimensions, kifma et9echna.
  \item \code{F.count(F.lit(1))}: total events fi had el bucket.
        \code{F.countDistinct("src\_ip")}: kaddech IP mo5telfa 7awlou
        f had el bucket (useful l "houwa attacker wa7ed kbir walla
        barcha IPs mo5tlifin?").
  \item \code{.withColumn("date", F.to\_date(F.col("hour")))}: yzid
        column \code{date} zeda (bidoun heure) -- houwa el partition
        column el mous-ta3mla f \code{write\_attack\_timeline}
        (\code{partition\_by=["date"]}) -- partitioning b \code{date}
        (machi \code{hour}, li ykoun barcha partitions sghar barcha)
        houwa el balance el pratique bin "efficient date-range queries"
        w "not too many tiny partitions".
\end{itemize}

\subsection{\code{write\_attack\_timeline}}

\begin{lstlisting}[style=pystyle]
def write_attack_timeline(spark: SparkSession, settings: Settings | None = None) -> DataFrame:
    """Read silver, build attack_timeline, and idempotently overwrite the gold table."""
    silver_df = spark.read.format("delta").load(silver_path("events", settings))
    timeline = build_attack_timeline(silver_df)
    write_gold_table(timeline, "attack_timeline", settings, partition_by=["date"])
    return timeline
\end{lstlisting}

Nafs el pattern kif \code{write\_attacker\_profiles} -- read, build,
write. El difference el wa7ida: \code{partition\_by=["date"]} ye3addi
explicite hna (contra \code{attacker\_profiles} li ma3andou-ch partitioning
-- grain mte3ou mahouch time-series).

\chapter{\file{ml/} -- el 2 detectors}

PFA 3andha 2 detectors ye5edmou b3adhom (mouch wa7ed instead el theni):
IsolationForest (unsupervised, trained 3la silver features) w rule fixe
(port scan). Kol wa7ed 3andou strengths/limitations mo5tlifin, w
\code{score\_events.py} y-combine-hom.

\section{\file{ml/features.py}}

\begin{lstlisting}[style=pystyle]
Every feature here is TRAILING-WINDOW: for a given event, "how much
activity has this src_ip generated in the ``window_seconds`` before (and
including) this event?" - not a whole-batch total. A whole-batch total
can't tell "20 ports touched in one burst" from "20 ports touched over a
week"; a trailing window can, and it lets the exact same feature
computation run unchanged whether scoring one hour of silver data or one
year of it.

Implemented with a single Spark window function per feature -
``Window.partitionBy("src_ip").orderBy(event_time_unix).rangeBetween(-window_seconds, 0)`` -
no join, no self-union, no UDF: every row's window is computed directly
against that same src_ip's other rows, ordered by time.
\end{lstlisting}

\begin{analogy}
"Trailing window" kifha t-check-i el historique mte3 chkoun li da5al
l immeuble f a5er 60 secondes, machi el historique \emph{kamel} mel bidaya
mte3 el yaum. Kol marra 7ad ye-swipe badge mte3ou (event jdid), ta3mel
"combien de personnes ont badgé dans les 60 dernières secondes?" -- mahiche
"combien depuis ce matin?". Hedhi el window ye5allik ta3rf "burst" (barcha
activité f wa9t 9sir) contra activité normale mfarr9a 3la yaum kamel,
7atta ken el 2 3andhom \emph{nafs el total} 3la el yaum kamel.
\end{analogy}

\begin{soual}
\textbf{3lech IsolationForest lezmou trailing-window features (mahouch
raw event fields direct), w 3lech window wa7ed (60s) mouch barcha windows
(1min, 5min, 1h...) kif barcha systems o5rin?}

Réponse: IsolationForest (w rule fixe zeda) ye7taj signal 3la "\emph{comportement}"
el attacker, machi 3la event wa7ed b rou7ou. Event wa7ed (\code{login.failed}
mathalan) houwa normal 7atta -- barcha bots automated ye3mlou login attempts
3ala kol honeypot. Chnowa \emph{anormal} houwa el \emph{fréquence} -- 20
login attempts f 60 secondes mel nafs el IP houwa signal brute-force, 20
login attempts mfarr9a 3la yaum kamel houwa noise 3adi. Window wa7da bark
(60s, \code{DEFAULT\_WINDOW\_SECONDS}) houwa scope minimal 3adéquat: PFA
3andha 2 attack shapes bark ye7taj ye-detecter-hom (brute-force burst,
port scan burst), w el 2 homa "burst" patterns short-term nisbiyan. Barcha
windows (multi-scale) yzid complexity (barcha features, barcha training
dimensions) bidoun benefit clair l had el scope el sghir.
\end{soual}

\subsection{\code{DEFAULT\_WINDOW\_SECONDS} w \code{ANOMALY\_FEATURE\_NAMES}}

\begin{lstlisting}[style=pystyle]
DEFAULT_WINDOW_SECONDS = 60

ANOMALY_FEATURE_NAMES: tuple[str, ...] = (
    "events_by_src_ip_in_window",
    "distinct_dst_ports_touched_by_src_ip_in_window",
    "login_attempts_by_src_ip_in_window",
    "is_login_attempt",
)
\end{lstlisting}

\begin{itemize}
  \item Comment fou9 \code{ANOMALY\_FEATURE\_NAMES} (mel code el 7a9i9i)
        ye9oul clair: \emph{"Order matters: scikit-learn has no
        column-name concept, it trusts positional order"} -- hedhi el
        tafsila el critique. scikit-learn's \code{IsolationForest.fit()}
        ta5ou \code{numpy} array (walla pandas DataFrame, mel-i converti
        l array internally) -- ma-3andou-ch concept mte3 "esm el column",
        bark position. Ken el features t-passiw f ordre mo5tlef bin
        training (\code{train\_anomaly.py}) w scoring
        (\code{score\_events.py}), el model ye-interpret feature 1 kifha
        feature 2 -- \emph{silently ghalt}, bidoun 7atta erreur.
  \item \code{ANOMALY\_FEATURE\_NAMES} houwa el "single source of truth"
        l had el ordre -- kol wa7ed mel 2 modules (\code{train\_anomaly.py},
        \code{score\_events.py}) ye-import-ha, machi ye-hardcode-ha
        separately.
  \item 4 features, kol wa7da 3andha role clair (mel comment el kbir):
        \code{events\_by\_src\_ip\_in\_window} w
        \code{login\_attempts\_by\_src\_ip\_in\_window} l brute-force
        (barcha events/logins f wa9t 9sir), \code{distinct\_dst\_ports\_touched\_by\_src\_ip\_in\_window}
        l port scan (barcha ports \emph{mo5tlifin} f wa9t 9sir, machi
        nafs el port marat 3adda), w \code{is\_login\_attempt}
        (per-event, machi per-window) y-refine el 2 patterns.
\end{itemize}

\subsection{\code{FeatureValidationError}}

\begin{lstlisting}[style=pystyle]
class FeatureValidationError(RuntimeError):
    """Raised when a feature DataFrame's columns don't match what a model expects."""
\end{lstlisting}

Exception custom o5ra, nafs el pattern el mou3tad fi kol el codebase.

\subsection{\code{build\_features} -- el core}

\begin{lstlisting}[style=pystyle]
def build_features(
    silver_df: DataFrame, window_seconds: int = DEFAULT_WINDOW_SECONDS
) -> DataFrame:
    """Compute :data:`ANOMALY_FEATURE_NAMES` for every silver event that has
    both a src_ip and an event_time - the two columns every feature here
    is computed relative to. Rows missing either can't be attributed to an
    attacker's trailing activity and are dropped.

    Keeps ``event_id`` and ``src_ip`` alongside the features so callers can
    join scores back to the events they were computed for, and so
    ``threatlake.ml.rules.port_scan_rule`` can be applied per-row without a
    second pass over silver.
    """
    base = silver_df.filter(F.col("src_ip").isNotNull() & F.col("event_time").isNotNull())

    event_time_unix = F.unix_timestamp(F.col("event_time"))
    trailing_window = (
        Window.partitionBy("src_ip").orderBy(event_time_unix).rangeBetween(-window_seconds, 0)
    )
    # credentials_attempted is a NOT NULL boolean column (see
    # threatlake.transform.silver.schema) - always true/false, safe to
    # cast straight to 0/1.
    is_login_attempt = F.col("credentials_attempted").cast("int")

    return base.select(
        "event_id",
        "src_ip",
        F.count(F.lit(1)).over(trailing_window).alias("events_by_src_ip_in_window"),
        # collect_set (used here as a window aggregate) ignores nulls, so
        # events with no dst_port (e.g. a cowrie.command.input event) simply
        # don't contribute a port to the count, rather than counting as a
        # fake "null port".
        F.size(F.collect_set("dst_port").over(trailing_window)).alias(
            "distinct_dst_ports_touched_by_src_ip_in_window"
        ),
        F.sum(is_login_attempt).over(trailing_window).alias("login_attempts_by_src_ip_in_window"),
        is_login_attempt.alias("is_login_attempt"),
    )
\end{lstlisting}

\begin{itemize}
  \item \code{base = silver\_df.filter(... src\_ip isNotNull \& event\_time
        isNotNull)}: filter l'awel -- kifma el docstring te9oul, kol
        feature hna mou7sba \emph{relative} l \code{src\_ip} (partition
        key) w \code{event\_time} (order key), fa row bidoun 7ad mel 2
        ma-t9darch tetgroupé fi window 7a9i9i.
  \item \code{event\_time\_unix = F.unix\_timestamp(F.col("event\_time"))}:
        Spark's \code{rangeBetween} (contra \code{rowsBetween}) ye5dem
        3la \emph{values numériques} (secondes), mouch 3la row-count --
        lezmou timestamp mou7awel l Unix epoch seconds (integer) bech
        \code{-window\_seconds} l \code{0} ykoun range clair (secondes,
        machi rows).
  \item \code{Window.partitionBy("src\_ip").orderBy(event\_time\_unix).rangeBetween(-window\_seconds, 0)}:
        el window definition el kbira -- \code{partitionBy("src\_ip")}
        (kol IP 3andou window separate mel IPs el o5rin), \code{orderBy(event\_time\_unix)}
        (rows ordoné b wa9t jowa kol partition), \code{rangeBetween(-window\_seconds, 0)}
        (mel \code{window\_seconds} 9bal el row el actuel, l el row nafsou
        inclus -- \emph{"trailing", including current"}).
  \item \code{is\_login\_attempt = F.col("credentials\_attempted").cast("int")}:
        comment inline ye-explique el confidence hna -- \code{credentials\_attempted}
        houwa NOT NULL boolean (mel \code{SILVER\_EVENT\_SCHEMA} li chefna),
        fa \code{.cast("int")} dima ye5dem b confidence (0 walla 1, 9att
        \code{null}).
  \item \code{F.count(F.lit(1)).over(trailing\_window)}: window aggregate
        \emph{machi} groupBy aggregate -- \code{.over(...)} y-apply el
        aggregation 3la kol row \emph{bidoun ma tne99es} el row count
        (contra \code{groupBy} li y-collapse el rows). Kol row t5rej
        b \code{events\_by\_src\_ip\_in\_window} mte3ha el 5assa, based
        3la el window mte3ha.
  \item \code{F.size(F.collect\_set("dst\_port").over(trailing\_window))}:
        \code{collect\_set} (window aggregate) ye5ammel el \emph{distinct}
        values mte3 \code{dst\_port} jowa el window f set, \code{F.size}
        ye7seb el cardinality. Comment inline ye-explique el edge-case:
        \code{collect\_set} ignore nulls automatiquement (behavior
        mte3 Spark), fa events bidoun \code{dst\_port} (command.input
        mathalan) ma-y-contribuwch port fake l el count.
  \item \code{F.sum(is\_login\_attempt).over(trailing\_window)}: sum mte3
        el 0/1 flag 3la el window -- "kaddech login attempt f had el
        window".
  \item \code{is\_login\_attempt.alias("is\_login\_attempt")}: hedhi
        el feature el wahida el \emph{mahiche} window aggregate -- houwa
        el value mte3 el row nafsou (per-event, machi trailing).
\end{itemize}

\subsection{\code{validate\_feature\_frame}}

\begin{lstlisting}[style=pystyle]
def validate_feature_frame(
    columns: list[str], expected: tuple[str, ...] = ANOMALY_FEATURE_NAMES
) -> None:
    """Raise if ``columns`` is missing any column in ``expected``.

    Guards the model-input boundary: threatlake.ml.train_anomaly and
    threatlake.ml.score_events both call this right before handing a
    pandas frame to scikit-learn, so a feature-computation bug surfaces as
    an immediate, specific error instead of a silently wrong model.
    """
    missing = [name for name in expected if name not in columns]
    if missing:
        raise FeatureValidationError(
            f"Feature frame is missing expected columns: {missing}. Got: {columns}."
        )
\end{lstlisting}

Nafs el falsafa el "fail loud, fail early" li nchoufouha 3ber el codebase
kamel (kif \code{\_assert\_not\_null} f \code{silver/schema.py}) -- guard
clause mou3ayen 9bal el boundary el critique (hna: passing data l
scikit-learn), machi assumption implicite.

\section{\file{ml/rules.py}}

\begin{lstlisting}[style=pystyle]
WHY A RULE, GIVEN A TRAINED MODEL ALREADY EXISTS: threatlake.ml.train_anomaly's
"KNOWN LIMITATION" section documents a specific, verified failure mode - on the
project's injection test corpus, IsolationForest's ``contamination`` parameter
fixes a hard budget on how many events can be flagged per batch, and a more
extreme pattern (credential brute-force) crowded out a less extreme but still
real one (port scan) for most of that budget, even after
threatlake.ml.features.ANOMALY_FEATURE_NAMES gave the model a fan-out feature
that separates port scans from normal traffic almost perfectly on its own.
A fixed threshold rule has no such budget: it fires per-event, independent of
what else is in the same batch, so it isn't subject to that crowding-out
failure mode. It is also not a replacement for the model - it has no answer
for anomaly patterns nobody has written a rule for yet, which is the entire
reason threatlake.ml.train_anomaly exists.
\end{lstlisting}

\begin{soual}
\textbf{Hedhi el Soual el akthar muhimma f el chapter el ML kamel: 3lech
zouz detectors (ML + rule) mahouch wa7ed bark? Mahouch redundant?}

Réponse: docstring ye-documente failure mode \emph{verified} 7a9i9i, machi
theoretical -- 3la corpus test (injection corpus) mte3 el project,
IsolationForest's \code{contamination} parameter (mathalan 0.02, ye3ni
"2\% mel events houma anomalies") ye7ott \emph{hard budget}: ken el batch
3andou brute-force pattern (extreme, barcha events "weird" f wa9t 9sir)
w port-scan pattern (real, walakin less extreme relatively), el
brute-force ye5ou mou3zam el "budget" mte3 el 2\%, w el port-scan ye-b9a
\emph{under-flagged} -- \emph{7atta ken} el model 3andou feature
(\code{distinct\_dst\_ports\_touched\_by\_src\_ip\_in\_window}) li
ye-separate el 2 patterns clairement. Hedha houwa el "crowding-out"
mentioned. Rule fixe (threshold-based) ma-3andou-ch had el budget --
ye-fire per-event, independamment 3la chnowa fama f el batch el nafsou.
Fa el 2 detectors 3andhom \emph{failure modes mo5tlifa}: model ye-cover
patterns \emph{ma-3arafhomch-ch} 7ad w ma-kteb-lhomch rule (el "unknown
unknowns"), rule ye-cover el pattern el m3aruf (port scan) b guarantee
(ma-ye-crowd-out-ch). Kombinaison-hom ye5alli coverage a7sen mel 7ad
mennhom bark.
\end{soual}

\begin{analogy}
Kifha kif security team 3andha zouz layers: camera b AI detection (ye-learn
patterns "weird" automatiquement, walakin momken yekhtar el akthar dramatic
event ken 3andou "alert budget" limité) + alarm fixe 3la sensor precis
(mathalan "ken 3adad el bebs el maftou7in $>$ 5 f nafs el wa9t, do99" --
simple, deterministe, ma-yetathrouch b chnowa el AI rah ta3mel f nafs el
moment). El 2 sawa ye5allik ma-tfoutch 7ata pattern, sawa el AI "distracted"
b chay o5or akthar dramatique.
\end{analogy}

\subsection{\code{PORT\_SCAN\_DISTINCT\_PORT\_THRESHOLD}}

\begin{lstlisting}[style=pystyle]
#: Chosen from the observed separation in the project's injection test
#: corpus (threatlake.ml.train_anomaly's KNOWN LIMITATION section, same
#: corpus, same 60s window): every one of the 565 ordinary events in that
#: corpus has this feature exactly equal to 1 (including brute-force events,
#: which hammer a single port repeatedly); every injected port-scan event
#: reaches up to 8. 5 is a threshold with headroom on both sides of that
#: gap - well above the "1" normal traffic never exceeds, well below the
#: observed "8" ceiling of an actual scan - rather than a value fit exactly
#: to this dataset (e.g. 2, which would perfectly split 1 from 8 here but
#: is a description of this specific corpus, not a general claim about what
#: "many distinct ports" means for honeypot traffic).
PORT_SCAN_DISTINCT_PORT_THRESHOLD = 5
\end{lstlisting}

\begin{soual}
\textbf{3lech 5 el threshold, mouch 2 (li "y-split perfectement" el 2
groupes 1 vs 8 fi el corpus mte3 test)?}

Réponse: hedha el comment el akthar transparent f el codebase kamel 3la
"kifeh 9rart 3la magic number" -- w houwa answer direct l had el soual
nafsou. Ken el threshold ykoun 2 (li y-split "perfectement" el data el
observed), el number ykoun \emph{overfit} 3la el corpus el test el
specifique -- mahouch generalizable l data jdida (real honeypot traffic,
li momken 3andou distribution mo5talfa chwaya). B 5 (headroom fi el zouz
jihat: 5 3la 5atr "well above the 1 normal traffic never exceeds" w "well
below the observed 8 ceiling"), el threshold houwa \emph{robust} l
variation naturelle f el data, machi calibré exactement 3la data
particulière. El comment ye9oul explicitement: 2 "is a description of
this specific corpus, not a general claim" -- distinction muhimma bin
"chnowa el data hedhi t9oul" w "chnowa el rule el general el correcte".
\end{soual}

\subsection{\code{port\_scan\_rule}}

\begin{lstlisting}[style=pystyle]
def port_scan_rule(
    distinct_dst_ports_touched: int, threshold: int = PORT_SCAN_DISTINCT_PORT_THRESHOLD
) -> bool:
    """True if a src_ip touched more than ``threshold`` distinct dst_ports in
    the trailing window - see :data:`PORT_SCAN_DISTINCT_PORT_THRESHOLD` for
    where the default comes from.
    """
    return distinct_dst_ports_touched > threshold
\end{lstlisting}

Function pure, ligne wa7da -- takes \code{distinct\_dst\_ports\_touched}
(el feature el mou7sba f \code{build\_features}) w yerja3 boolean. Simplicité
intentionnelle: el "rule" kamla houwa comparison wa7da, deterministe,
predictable, testable b ligne wa7da (\code{assert port\_scan\_rule(8) is
True}) -- 3ks el model ML li houwa "black box" nisbiyan.

\section{\file{ml/train\_anomaly.py}}

\begin{lstlisting}[style=pystyle]
Live honeypot data has no ground-truth label - there is no analyst
sitting behind cowrie tagging each session "attack" or "not attack" - so
this module never fits a supervised model and never reports a supervised
accuracy/F1 figure. IsolationForest is unsupervised: it scores how easily
a point is isolated by random feature splits, under the assumption that
anomalies are "few and different" and so isolate in fewer splits than
normal points.
\end{lstlisting}

\begin{analogy}
IsolationForest kifha t7eb tal9a "el chakhs el ghalt" f foule mel 3ibad
bidoun ma ta3raf mel-9bal chkoun houwa -- houwa el chakhs eli "as-hal
te-isole-h" (ta9dar te-separate-h mel groupe b su2al wa7ed walla zouz:
"houwa lébès a7mar?", "houwa twil?"...), 3la 5atr houwa \emph{different}
mel majorité. El 3ibad el "normal" (mcheb-hin ba3dhom) ye7tajou barcha
su2al (splits) bech te-isole wa7ed mennhom mel groupe, 3la 5atr homa
mtabhin barcha m3a ba3dhom. Hedha houwa el fikra: anomaly = as-hal
ye-isole = 9rib mel root mte3 el decision tree.
\end{analogy}

\begin{soual}
\textbf{3lech IsolationForest \emph{unsupervised} (ma ye5demch b labels)
houwa el ekhtiyar el m3a9oul hna, w mouch supervised classifier
(mathalan XGBoost 3la labeled attacks)?}

Réponse: docstring ye9olha directly -- \emph{"Live honeypot data has no
ground-truth label"}. Ma-fama-ch analyst 7a9i9i 9a3ed ye-tag kol session
"attack" walla "not attack" f real-time -- el honeypot ye-log kol chay
li yousal-lou (kollou "attack" f sense generic, honeypot ma3andou-ch
"legitimate users" 7a9i9iyin). Fa training supervised classifier lezmou
labels 7a9i9iyin -- w bidoun-hom, ay label momken tzid-ha (mathalan
"heuristic labeling" 3la eventid) ye-b9a biased/circular (ta3mel model
ye-learn el heuristic nafsou, machi signal jdid). IsolationForest ye5dem
bidoun labels 7atta wa7ed -- assumption mte3ou el wahid houwa "anomalies
are few and different", w hedhi assumption reasonable l honeypot traffic
(mou3zam el traffic houwa noise/bots automated mtabhin, el attacks
7a9i9iyin/sophistiqués homa rare w mo5talfin).
\end{soual}

\begin{lstlisting}[style=pystyle]
PERSISTENCE: a plain joblib file (``settings.ml.model_path``), not an
MLflow model registry. ThreatLake AI (the full project this is a subset
of) tracks every run through MLflow - parameters, metrics, a registered
model with staged versions - which is real, useful machinery for a system
with a retraining schedule and multiple models competing for a
"Staging"/"Production" slot. PFA trains one model, once, for one batch
pipeline run; a single file scikit-learn's own ``joblib.dump``/``load``
round-trips is the whole story, and is what a from-scratch reader can
open and reason about without also learning MLflow's tracking/registry
API.
\end{lstlisting}

\begin{soual}
\textbf{3lech joblib mouch MLflow, walaw MLflow houwa el "standard"
industriel l ML model management?}

Réponse: 9rar mou9asad, w el docstring transparent 3la el trade-off
(kifma etl9inha f zouz places o5rin f hedha el document): MLflow ye5dem
real value ken 3andek \emph{retraining schedule} (train kol yaum/jom3a,
compare versions) w \emph{multiple models} li ye-compétionw 3la "Staging"/"Production"
slot (deployment strategy). PFA 3andha use-case as-hal bezef: train
\emph{marra wa7da}, l \emph{batch pipeline run wa7ed}. MLflow f had el
context ye-zid complexity (tracking DB, registry API, concepts jdida
l fahmemhom) bidoun benefit 7a9i9i -- persistence file basic (joblib)
ye5dem exactement nafs el chay el mou7taj (save model, load-ha ba3dein
l scoring), w kol wa7ed ya9ra el code (jury mathalan) ye9dar yefhem
kif ye5dem bidoun ma yet3allem MLflow API l'awel. Hedha exemple 7a9i9i mte3
"YAGNI" (You Aren't Gonna Need It) applied bien -- simplicité proportionnelle
l el scope el 7a9i9i.
\end{soual}

\subsection{\code{\_RANDOM\_STATE} w \code{AnomalyTrainingResult}}

\begin{lstlisting}[style=pystyle]
_RANDOM_STATE = 42


@dataclass(frozen=True)
class AnomalyTrainingResult:
    """Outcome of one IsolationForest fit."""

    n_events: int
    n_flagged: int
    contamination: float
    model_path: str

    def summary(self) -> str:
        rate = self.n_flagged / self.n_events if self.n_events else 0.0
        return (
            f"events={self.n_events} flagged={self.n_flagged} "
            f"({rate:.1%}) contamination_param={self.contamination} "
            f"model_path={self.model_path}"
        )
\end{lstlisting}

\begin{itemize}
  \item \code{\_RANDOM\_STATE = 42}: seed fixe (magic number el classique
        f el ML world, "the answer to life, the universe, and everything").
        IsolationForest ye5dem b random splits internally -- seed fixe
        ye5alli el training \emph{reproductible}: nafs el data $\to$
        nafs el model exactement, kol marra. Bidoun-ha, kol training run
        yentej model chwaya mo5telef, w tests/debugging ye-b9aou s3ib
        (\emph{"kifeh el bug hedha, el model deja mo5telef bin runs?"}).
  \item \code{AnomalyTrainingResult}: nafs pattern dataclass frozen kif
        \code{BronzeWriteResult}. \code{summary()} method: yentej string
        human-readable, useful l orchestration scripts (\code{run\_pipeline.py})
        li 7ebbin y-print result 3la terminal bidoun ma yeb-nou format-ha
        manuellement kol marra.
  \item \code{rate = self.n\_flagged / self.n\_events if self.n\_events
        else 0.0}: guard 3la division-by-zero -- ken \code{n\_events == 0}
        (edge case, silver table fargh mathalan), \code{rate} ye-b9a 0.0
        machi \code{ZeroDivisionError}.
\end{itemize}

\subsection{\code{train\_anomaly} -- el function el kamla}

\begin{lstlisting}[style=pystyle]
def train_anomaly(
    spark: SparkSession,
    settings: Settings | None = None,
    contamination: float = 0.02,
    n_estimators: int = 200,
    window_seconds: int = 60,
) -> AnomalyTrainingResult:
    """Fit IsolationForest on the current silver table's feature space and
    save it to ``settings.ml.model_path``.

    ``contamination`` is IsolationForest's own prior on the expected
    anomaly fraction, not a measured rate - it directly controls how many
    points get flagged, which is why it is reported alongside the result
    rather than presented as a finding.
    """
    settings = settings or get_settings()

    silver = spark.read.format("delta").load(silver_path("events", settings))
    feature_df = build_features(silver, window_seconds=window_seconds)
    validate_feature_frame(feature_df.columns, expected=ANOMALY_FEATURE_NAMES)

    pdf: pd.DataFrame = feature_df.toPandas()
    if pdf.empty:
        raise ValueError("Silver table produced zero feature rows - nothing to train on.")

    features_only = pdf[list(ANOMALY_FEATURE_NAMES)]

    model = IsolationForest(
        n_estimators=n_estimators,
        contamination=contamination,
        random_state=_RANDOM_STATE,
        n_jobs=-1,
    )
    model.fit(features_only)
    predictions = model.predict(features_only)  # -1 = anomaly, 1 = normal
    n_flagged = int((predictions == -1).sum())

    model_path = Path(settings.ml.model_path).expanduser().resolve()
    model_path.parent.mkdir(parents=True, exist_ok=True)
    joblib.dump(model, model_path)

    return AnomalyTrainingResult(
        n_events=len(pdf),
        n_flagged=n_flagged,
        contamination=contamination,
        model_path=str(model_path),
    )
\end{lstlisting}

\begin{soual}
\textbf{Docstring ye9oul \code{contamination} "is IsolationForest's own
prior ... not a measured rate". Chnowa el difference, w 3lech muhimma?}

Réponse: \code{contamination=0.02} ma-ye9olch "measured-na eli 2\% mel
data houma attacks 7a9i9iyin" -- houwa \emph{input} l el model, machi
\emph{output} mennou. Houwa ye9oul l IsolationForest: "esta3mel had el
prior 9bal ma t9arrar el decision boundary (threshold) mel anomaly
scores continues l flag/no-flag binaire". Ken thebdel \code{contamination}
l 0.10 (10\%), el \emph{nafs el data exactement} ye5rej 5 marat akthar
"flagged" events -- mahouch 3la 5atr el data tbeddlet, houwa 3la 5atr
threshold-na tbeddel. Hedhi el sabab el \code{contamination} yetrapporta
"alongside the result rather than presented as a finding" (f
\code{AnomalyTrainingResult.contamination}) -- lezem ykoun clair l reader
eli \code{n\_flagged} houwa function mte3 \code{contamination} el
mou5tar, machi discovery mte3 "kaddech attacks 7a9i9i fama".
\end{soual}

\begin{itemize}
  \item \code{spark.read.format("delta").load(silver\_path("events", settings))}
        $\to$ \code{build\_features} $\to$ \code{validate\_feature\_frame}:
        el pipeline el mou3tad -- read, transform, validate 9bal ma
        t3addi l model.
  \item \code{pdf: pd.DataFrame = feature\_df.toPandas()}: hna houwa el
        moment el critique win Spark DataFrame (distributed) ye-collect
        l pandas DataFrame (single-machine, memory). scikit-learn
        ma-ye5demch 3la Spark DataFrames -- lezmou numpy/pandas.
        \code{.toPandas()} houwa action (kif \code{.collect()}) --
        l had el reason, el feature computation lezmou ykoun "reasonable"
        f size (silver events, machi bronze raw kamel).
  \item \code{if pdf.empty: raise ValueError(...)}: guard clause -- IsolationForest
        ma-y9darch ye-train 3la 0 samples, fa el erreur el explicite
        (message clair "nothing to train on") ahsen mel exception generic
        mte3 scikit-learn eli momken ma-tbanach clear l reader.
  \item \code{features\_only = pdf[list(ANOMALY\_FEATURE\_NAMES)]}: select
        el 4 feature columns bark (bidoun \code{event\_id}, \code{src\_ip}
        li deja f \code{pdf} mel \code{build\_features}) -- \code{list(...)}
        3la tuple 3la 5atr pandas \code{.[]} indexing ye5taj list (mahouch
        ye9bel tuple direct fi kol el versions).
  \item \code{IsolationForest(n\_estimators=200, contamination=contamination,
        random\_state=\_RANDOM\_STATE, n\_jobs=-1)}: \code{n\_estimators=200}
        (kaddech "trees" fi el forest -- akthar trees, akthar stable
        walakin akthar compute). \code{n\_jobs=-1}: esta3mel kol el cores
        disponibles (parallelism 3la level mte3 scikit-learn, machi
        Spark -- houwa training local 3la machine wa7da b had el noqta).
  \item \code{model.predict(features\_only)}: \code{-1} = anomaly, \code{1}
        = normal (convention mte3 scikit-learn el standard, comment
        inline ye3awed-ha bech ma-tensach). \code{n\_flagged = int((predictions
        == -1).sum())}: count el \code{-1}s.
  \item \code{model\_path.parent.mkdir(parents=True, exist\_ok=True)}:
        ye5al9 el parent directory ken ma-mawjoud-ch (nafs pattern kif
        \code{fs.move}) -- \code{exist\_ok=True} ye5alli ma-yfail-ch ken
        el directory deja mawjoud (idempotent).
  \item \code{joblib.dump(model, model\_path)}: el persistence el 7a9i9iya
        -- \code{joblib} houwa serialization library optimisé l objects
        mel numpy/scikit-learn (ahsen men \code{pickle} el standard l
        had el use-case, ye5dem ahsen m3a arrays kbar).
\end{itemize}

\section{\file{ml/score\_events.py}}

\begin{lstlisting}[style=pystyle]
COMBINED ANOMALY DETECTION: an event is flagged (``is_anomaly=True``) if
EITHER detector fires. The two are deliberately NOT merged into
one opaque score: ``alert_source`` records exactly which one(s) fired
(``"rule"``, ``"ml"``, or ``"both"``), so a flagged event's cause is
always attributable rather than hidden behind a single number.

APPEND, NOT OVERWRITE: unlike ``attacker_profiles``/``attack_timeline``,
this table is not a full recompute - it is a growing log of scoring
results, one row per scored event. Re-running against a silver batch that
was already scored is a no-op, not a duplicate: this module reads back
the event_ids already present in ``ml_scores`` and excludes them before
scoring, so re-running the pipeline produces no new rows rather than
double-counting.
\end{lstlisting}

\begin{soual}
\textbf{3lech \code{alert\_source} ("rule"/"ml"/"both") houwa design
choice muhim, mouch just \code{is\_anomaly} boolean wa7ed bark?}

Réponse: kifma el docstring te9oul, el 2 detectors "deliberately NOT
merged into one opaque score". Ken el output ykoun \code{is\_anomaly}
bark (bidoun \code{alert\_source}), analyst SOC li ye-check el alert
ma-3andou-ch idea 3lech el event flagged -- houwa ML (pattern statistique
li el model 3arf-ha mel training) walla rule (port scan explicite,
deterministe)? Had el information critique l el analyst bech ya3raf
kifeh ye-investigui (ML flag momken ykoun false positive rare, rule
flag houwa deterministe w explicable direct). \code{alert\_source}
ye5alli kol alert \emph{attributable} -- traceable l el detector
exact li fired-ha, machi black box.
\end{soual}

\subsection{\code{SCORES\_SCHEMA}}

\begin{lstlisting}[style=pystyle]
SCORES_SCHEMA = StructType(
    [
        StructField("event_id", StringType(), False),
        StructField("scored_at", TimestampType(), False),
        StructField("anomaly_score", DoubleType(), False),
        StructField("is_anomaly", BooleanType(), False),
        StructField("alert_source", StringType(), True),
    ]
)
\end{lstlisting}

\code{alert\_source} houwa el field el wahed nullable f had el schema --
3la 5atr event mahouch flagged (\code{is\_anomaly=False}) ma-3andou-ch
"source" mte3 alert (null valide -- machich rule wala ML fired-ha).

\subsection{\code{ScoringResult}}

\begin{lstlisting}[style=pystyle]
@dataclass(frozen=True)
class ScoringResult:
    """Outcome of one scoring run."""

    n_candidates: int
    n_already_scored: int
    n_newly_scored: int
    n_flagged: int

    def summary(self) -> str:
        return (
            f"{self.n_candidates} events, {self.n_already_scored} already scored, "
            f"{self.n_newly_scored} newly scored, {self.n_flagged} flagged"
        )
\end{lstlisting}

Nafs pattern dataclass frozen + \code{summary()} kif \code{AnomalyTrainingResult}.
4 counters différents (candidates, already scored, newly scored, flagged)
ye5alliw el observer ya3raf exactement chnowa sar f el run (mathalan
"3andi 100 candidates, 80 deja scored mel run el 9bel, 20 jdida, 3 minhom
flagged").

\subsection{\code{\_already\_scored\_event\_ids}}

\begin{lstlisting}[style=pystyle]
def _already_scored_event_ids(spark: SparkSession, settings: Settings) -> set[str]:
    try:
        existing = spark.read.format("delta").load(gold_path("ml_scores", settings))
    except Exception:  # table not created yet is normal, not an error
        return set()
    rows = existing.select("event_id").distinct().collect()
    return {row["event_id"] for row in rows}
\end{lstlisting}

\begin{itemize}
  \item \code{try/except Exception}: pattern "catch-all" (machi exception
        specifique) -- comment inline ye9oul l'raison: \emph{"table not
        created yet is normal, not an error"}. El run el awel mte3 el
        pipeline (7atta wa9t ma \code{ml\_scores} ma-t5al9et-ch 3ad),
        \code{spark.read.format("delta").load(...)} 3la path li ma-fihech
        Delta table ye-throw exception (Delta-specific, kif
        \code{AnalysisException}) -- had el except el generic ye-catch-ha
        w yerja3 set fargh (\emph{"ma-fama-ch events scored 9bal, kolchay
        candidate"}).
  \item \code{.select("event\_id").distinct().collect()}: ya9ra bark el
        column \code{event\_id} (machi el table kamla -- efficient, w
        Delta/Parquet ye5edmou b columnar storage fa had el projection
        ye9lel el I/O barcha), \code{.distinct()} (walaw \code{event\_id}
        lezmou unique mel design, defensive), \code{.collect()} (action
        -- el set el akhir lezmou ykoun f Python memory bech t9dar
        t-esta3mel-ha b \code{.isin()} ba3dein).
  \item \code{return \{row["event\_id"] for row in rows\}}: set comprehension
        -- $O(1)$ membership check ba3dein (\code{isin} 3la had el set),
        machi list ($O(n)$).
\end{itemize}

\subsection{\code{score\_silver\_events}}

\begin{lstlisting}[style=pystyle]
def score_silver_events(
    spark: SparkSession,
    settings: Settings | None = None,
    window_seconds: int = 60,
) -> tuple[DataFrame, ScoringResult]:
    settings = settings or get_settings()

    model_path = Path(settings.ml.model_path).expanduser().resolve()
    if not model_path.is_file():
        raise FileNotFoundError(
            f"No trained model at {model_path} - run threatlake.ml.train_anomaly.train_anomaly "
            "first."
        )
    model = joblib.load(model_path)

    silver = spark.read.format("delta").load(silver_path("events", settings))
    feature_sdf = build_features(silver, window_seconds=window_seconds)
    validate_feature_frame(feature_sdf.columns, expected=ANOMALY_FEATURE_NAMES)

    already_scored = _already_scored_event_ids(spark, settings)
    feature_sdf = feature_sdf.filter(~feature_sdf.event_id.isin(already_scored))

    pdf = feature_sdf.toPandas()
    n_candidates = len(pdf) + len(already_scored)
    scored_at = datetime.now(UTC)

    if pdf.empty:
        empty = spark.createDataFrame([], schema=SCORES_SCHEMA)
        result = ScoringResult(n_candidates, len(already_scored), 0, 0)
        return empty, result
\end{lstlisting}

\begin{itemize}
  \item \code{if not model\_path.is\_file(): raise FileNotFoundError(...)}:
        guard clause -- ken 7ad ye3ayet \code{score\_silver\_events}
        9bal ma \code{train\_anomaly} 7atta marra wa7da, el erreur clair
        (\emph{"run ... train\_anomaly first"}) ahsen men \code{joblib.load}
        crash generic (\code{FileNotFoundError} el standard, bidoun
        context).
  \item \code{already\_scored = \_already\_scored\_event\_ids(...)} +
        \code{feature\_sdf.filter(\textasciitilde{}feature\_sdf.event\_id.isin(already\_scored))}:
        hna houwa el "no-op re-run" el docstring ye-describe -- events
        li \code{event\_id} mte3hom deja f \code{ml\_scores} ye-exclu
        mel \code{feature\_sdf} 9bal el scoring, fa re-running 3la
        nafs el silver data ma-yentejch duplicate rows.
  \item \code{n\_candidates = len(pdf) + len(already\_scored)}: total
        "kol events li 9addarhom yetscoriw" (jdid + deja scored) -- useful
        l reporting.
  \item \code{if pdf.empty: ... return empty, result}: early return --
        ken kol el events deja scored (0 jdid), ma-fama-ch 7ajja te3mel
        \code{.decision\_function()} 3la DataFrame fargh (momken ye-crash
        walla ye-behave ghalt 3la input fargh).
\end{itemize}

\begin{lstlisting}[style=pystyle]
    features_only = pdf[list(ANOMALY_FEATURE_NAMES)]
    anomaly_score = model.decision_function(features_only)
    is_ml_anomaly = model.predict(features_only) == -1
    is_rule_anomaly = pdf["distinct_dst_ports_touched_by_src_ip_in_window"].map(port_scan_rule)

    alert_source = pd.Series(
        [
            "both" if ml and rule else "ml" if ml else "rule" if rule else None
            for ml, rule in zip(is_ml_anomaly, is_rule_anomaly, strict=True)
        ],
        index=pdf.index,
    )
    is_anomaly = is_ml_anomaly | is_rule_anomaly

    scored_pdf = pd.DataFrame(
        {
            "event_id": pdf["event_id"],
            "scored_at": scored_at,
            "anomaly_score": anomaly_score.astype(float),
            "is_anomaly": is_anomaly,
            "alert_source": alert_source,
        }
    )
    scored = spark.createDataFrame(scored_pdf, schema=SCORES_SCHEMA)

    result = ScoringResult(
        n_candidates=n_candidates,
        n_already_scored=len(already_scored),
        n_newly_scored=len(pdf),
        n_flagged=int(is_anomaly.sum()),
    )
    return scored, result
\end{lstlisting}

\begin{itemize}
  \item \code{anomaly\_score = model.decision\_function(features\_only)}:
        \emph{machi} \code{.predict()} -- \code{decision\_function}
        yrajja3 score continu (float, lower = more anomalous, kifma
        comment mte3 \code{SCORES\_SCHEMA} ye9oul), machi bark
        binaire (-1/1). El score el continu ye5alli el consumer (API,
        dashboard) ye-rank events b "kaddech anomalous", machi bark
        flag/no-flag.
  \item \code{is\_ml\_anomaly = model.predict(features\_only) == -1}:
        el flag binaire, separate 3la el score el continu.
  \item \code{is\_rule\_anomaly = pdf["distinct\_dst\_ports\_touched..."].map(port\_scan\_rule)}:
        \code{.map()} houwa el equivalent pandas mte3 \code{apply function
        3la kol element} -- ye3ayet \code{port\_scan\_rule} (mel
        \code{rules.py}) 3la kol row's feature value.
  \item El list comprehension l \code{alert\_source}: nested conditional
        expression (\code{"both" if ml and rule else "ml" if ml else
        "rule" if rule else None}) -- Python ye-evaluate mel a5er
        clause l el bidaya: ken el 2 (\code{ml} w \code{rule}) True,
        "both"; ken \code{ml} bark, "ml"; ken \code{rule} bark, "rule";
        ken 7atta wa7ad, \code{None}.
  \item \code{zip(is\_ml\_anomaly, is\_rule\_anomaly, strict=True)}:
        \code{strict=True} (Python 3.10+) ye-force el 2 iterables ykounou
        \emph{nafs} el taille -- ken mo5tlfin (bug mel awel), \code{zip}
        ye-raise \code{ValueError} direct machi ye-truncate silently
        l el a9sar minhom (behavior el default mte3 \code{zip} el 3adi,
        li momken ye5abbi bugs).
  \item \code{is\_anomaly = is\_ml\_anomaly | is\_rule\_anomaly}: pandas
        boolean OR (element-wise, machi Python \code{or}) -- exactement
        el "EITHER detector fires" mel docstring.
  \item \code{spark.createDataFrame(scored\_pdf, schema=SCORES\_SCHEMA)}:
        el "return trip" mel pandas l Spark -- b schema explicite (machi
        inference automatique), bech el types ykounou exact match m3a
        \code{SCORES\_SCHEMA} (mathalan \code{anomaly\_score} lezmou
        \code{DoubleType} exact, machi \code{FloatType} inferred).
\end{itemize}

\subsection{\code{write\_scored\_events}}

\begin{lstlisting}[style=pystyle]
def write_scored_events(
    spark: SparkSession,
    settings: Settings | None = None,
    window_seconds: int = 60,
) -> ScoringResult:
    """Score unscored silver events and append the results to ``ml_scores``."""
    settings = settings or get_settings()
    scored, result = score_silver_events(spark, settings, window_seconds)
    if result.n_newly_scored:
        scored.write.format("delta").mode("append").option("mergeSchema", "true").save(
            gold_path("ml_scores", settings)
        )
    return result
\end{lstlisting}

\begin{itemize}
  \item \code{.mode("append")}: \emph{machi} \code{"overwrite"} --
        contra \code{write\_gold\_table} (attacker\_profiles, attack\_timeline)
        li dima full recompute, \code{ml\_scores} houwa "growing log" (kifma
        el module docstring te9oul el 3enwan "APPEND, NOT OVERWRITE").
        El "idempotence" ma tji-ch mel overwrite hna, tji mel dedup logic
        (\code{\_already\_scored\_event\_ids}) 9bal el scoring.
  \item \code{if result.n\_newly\_scored:}: write conditionnel -- ken
        \code{0} events jdida (kolchay deja scored), ma-fama-ch 7ajja
        te3mel write 3la DataFrame fargh (nafs pattern kif
        \code{bronze\_writer.py}'s quarantine write conditionnel).
\end{itemize}

\chapter{\file{copilot/} -- natural language l SQL, b guardrails}

Hedha el package ye5alli analyst yes2al su2al f langage naturel
(\emph{"chkoun el top 5 attackers?"}), w yeraj3ou result mel gold tables
-- b LLM (Gemini) li yentej SQL, w guardrail (AST-based) li ye-valid es
SQL 9bal ma tetnaffed 3la Spark 7atta marra.

\section{\file{copilot/guardrails.py} -- el couche el akthar securisée}

\begin{lstlisting}[style=pystyle]
Why AST-based, not regex/keyword-matching: a regex over the raw SQL string
cannot correctly distinguish "DROP appears inside an executable statement"
from "DROP appears inside a comment or a string literal" - it either
over-rejects safe queries that happen to mention a banned word in a
comment, or (worse) under-rejects a real attack that uses whitespace,
casing, or comment tricks a regex didn't anticipate. Parsing with sqlglot
and inspecting the resulting expression tree answers the actual question
this module cares about: what will Spark SQL actually EXECUTE, not what
substrings appear in the text.

Every rejection is raised as :class:`GuardrailRejectionError` with a specific,
honest reason - the API layer logs it and returns it to the caller
verbatim, never a generic "invalid query".
\end{lstlisting}

\begin{soual}
\textbf{Hedha el chapter (guardrails) houwa el akthar critique securité-wise
f el codebase kamel: 3lech AST parsing (sqlglot) mouch regex/keyword scan
simple 3la el SQL text?}

Réponse: docstring ye9oul l'raison exactement -- regex 3la raw string
ma-9adrach \emph{temyiz} bin "DROP mawjoud f statement executable 7a9i9i"
w "DROP mawjoud jowa comment walla string literal" (mathalan
\code{SELECT * FROM x WHERE note = 'please DROP the old habit'}). Hedha
ye5alli 2 problèmes: false positives (regex ye-reject query safe li just
3andha kelma "DROP" f text/comment, machi f SQL logic 7a9i9i) w -- el
akthar dangereux -- false negatives (attacker momken ye-esta3mel whitespace
tricks, casing mo5talef, comment tricks li regex ma-anticipet-hech, w el
attack ye3addi). AST parsing (\code{sqlglot}) ye-answer el su2al el 7a9i9i:
\emph{chnowa Spark SQL rah ye-execute 7a9i9atan}, machi \emph{chnowa el
substrings mawjoudin f el text}. Hedha houwa el difference bin "syntax
checking" (regex, superficiel) w "semantic checking" (AST, ye-comprend
el structure 7a9i9iya).
\end{soual}

\begin{analogy}
Regex 3la SQL text kifha guard 3nd bab li ye-check bark \emph{kelmat}
mawjoudin f ID card (esm, prénom...) bidoun ma ye-verify \emph{sitructure}
el document nafsou (houwa formaté sa7i7? houwa mel gouvernement el
correcte?). AST parsing kifha guard li ye-verify el document \emph{kamel}
(structure, format, signature) -- ye-comprend "chnowa hedha el document
\emph{ye3ni} 7a9i9atan", machi bark "chnowa el mots mawjoudin fih".
\end{analogy}

\subsection{\code{ALLOWED\_TABLES} -- el allowlist el trimmed}

\begin{lstlisting}[style=pystyle]
#: The only tables the copilot may ever read. Bronze, silver, and ml_scores
#: are deliberately excluded - a smaller, safer surface given the
#: timeline, not an oversight.
#:
#: ThreatLake AI (the full project this is a subset of) allows 5 gold
#: tables here. PFA only ever builds 2 (see
#: threatlake.transform.gold) - trimmed to match, since prompts.py reads
#: every allowed table's live schema at request time and would fail on a
#: table that doesn't exist.
ALLOWED_TABLES: frozenset[str] = frozenset(
    {
        "attacker_profiles",
        "attack_timeline",
    }
)
\end{lstlisting}

\begin{soual}
\textbf{Hedhi el constante houwa el ekhtilaf el wa7ed li PFA 3andha 3la
ThreatLake AI f had el file kamel (5 tables $\to$ 2). 3lech el trimming
sar hna, w 3lech had el ligne wa7da bark yekfi (bidoun rewrite mte3 el
guardrail logic el kamla)?}

Réponse: comment ye-explique el "why" 3la 2 niveaux -- \code{(1)}
\emph{"a smaller, safer surface given the timeline, not an oversight"}:
PFA cowrie-only, 3andha 2 gold tables bark (\code{attacker\_profiles},
\code{attack\_timeline}) -- el 3 gold tables el o5rin f ThreatLake AI
(\code{service\_targeting}, \code{credential\_intelligence},
\code{campaign\_candidates}) ma-mawjoudinch f PFA b tout, fa el copilot
ma-lezmou-ch ye-reference-hom. \code{(2)} el sabab el "mechanical": module
o5or (\code{prompts.py}) ya9ra el schema el 7a9i9i mte3 kol table f
\code{ALLOWED\_TABLES} \emph{f wa9t el request} (\code{spark.read.format("delta").load(...)}
3la kol wa7da) -- ken \code{ALLOWED\_TABLES} kan 3andha 5 entries walakin
bark 2 tables 7a9i9i mawjoudin 3la disk, \code{prompts.py} ye-crash 3la
3 tables ma-mawjoudinch. \code{ALLOWED\_TABLES} houwa el single source
of truth (kifma comment ye9oul: \emph{"exactly one place the allowlist
can be edited"}) li \code{prompts.py} w el guardrail checks (\code{\_check\_table\_allowlist})
kolhom ye3addiw minha -- fa tbdil ligne wa7da (el set nafsou) ye-propagate
automatiquement l kol el system, bidoun ma tmess el logique el AST parsing
walla el validation flow kamel (li rahou identique bin ThreatLake AI w
PFA).
\end{soual}

\subsection{\code{MAX\_ROWS} w \code{\_READ\_ONLY\_TYPES}}

\begin{lstlisting}[style=pystyle]
#: Server-side row cap, enforced regardless of what the generated SQL asks
#: for - a query with no LIMIT, or a LIMIT larger than this, is rewritten
#: to exactly this value before execution.
MAX_ROWS = 200

#: Expression types sqlglot can produce at the top level of a parsed
#: statement that are genuinely read-only. Anything else (Insert, Update,
#: Delete, Drop, Alter, Create, TruncateTable, Grant, Command, ...) is
#: rejected by construction - this is an allowlist of what's PERMITTED,
#: not a blocklist of what's forbidden, so a statement type this module's
#: author didn't think to name explicitly is rejected by default rather
#: than accepted by default.
_READ_ONLY_TYPES = (exp.Select, exp.Union, exp.Intersect, exp.Except)
\end{lstlisting}

\begin{soual}
\textbf{3lech \code{\_READ\_ONLY\_TYPES} houwa design "allowlist" (accept
had el 4 types bark) machi "blocklist" (reject Insert/Update/Delete/...)?
Mahouch nafs el result akhir?}

Réponse: comment ye9olha directly -- \emph{"rejected by construction ...
not accepted by default"}. B allowlist, statement type \emph{jdid} li
sqlglot momken yentejou (mathalan version jdida mte3 sqlglot tzid support
l statement type jdid ma-kanch existant 9bal) ye-reject \emph{automatiquement}
(3la 5atr mahouch f el tuple \code{\_READ\_ONLY\_TYPES}) -- \emph{safe by
default}. B blocklist (reject Insert/Update/Delete/Drop...), statement
type jdid \emph{ma-mahfoudch} f el blocklist ye3addi \emph{automatiquement}
(3la 5atr mahouch explicitement banned) -- \emph{unsafe by default}. Hedha
principe security el classique: "fail closed" (reject bidefault, accept
bark el m3aroufin safe) ahsen mel "fail open" (accept bidefault, reject
bark el m3aroufin dangereux) -- 5assatan ken el "known dangerous list"
momken ma-tkounech exhaustive.
\end{soual}

\subsection{\code{\_BANNED\_KEYWORDS} w \code{\_BANNED\_KEYWORD\_PATTERN}}

\begin{lstlisting}[style=pystyle]
#: Belt-and-suspenders raw scan (see module docstring): these must not
#: appear anywhere in the input text at all, including inside a comment -
#: a legitimate analytics question about the gold tables never needs to
#: mention any of them, so their presence anywhere is itself suspicious
#: regardless of whether a real parser would treat it as inert.
_BANNED_KEYWORDS = (
    "INSERT", "UPDATE", "DELETE", "DROP", "ALTER", "CREATE",
    "TRUNCATE", "GRANT", "REVOKE", "MERGE", "EXEC", "EXECUTE", "CALL",
)
_BANNED_KEYWORD_PATTERN = re.compile(r"\b(" + "|".join(_BANNED_KEYWORDS) + r")\b", re.IGNORECASE)
\end{lstlisting}

\begin{soual}
\textbf{Attends -- el module docstring 9al eli regex/keyword-matching
mahouch reliable (false positives/negatives) w AST houwa el correcte.
Fa 3lech had el file \emph{nafsou} 3andou raw keyword regex scan
(\code{\_raw\_keyword\_scan}) zeda? Mahouch contradiction?}

Réponse: mahiche contradiction -- houwa "belt-and-suspenders" (comment
ye9olha explicitement): AST check houwa el layer el asasi (correct,
semantic), w el regex scan houwa layer \emph{ekstra}, deliberately
\emph{stricter mel AST}. El AST check ye-accept query li fiha "DROP"
jowa comment walla string literal (3la 5atr semantically inerte -- Spark
ma-ye-executi-hach). Walakin el regex scan ye-reject nafs el query
\emph{7atta ken semantically safe}: mel docstring, \emph{"a legitimate
analytics question about the gold tables never needs to mention any of
them"} -- ye3ni: mahiche fama use-case legitime l analyst SOC y-esta3mel
kelma "DROP" f su2al mte3ou 3la attacker profiles. Fa el regex houwa
"belt" ekstra (paranoid intentionnellement) fou9 el AST el "suspenders"
el asasi -- defense-in-depth, machi replacement.
\end{soual}

\subsection{\code{GuardrailRejectionError}}

\begin{lstlisting}[style=pystyle]
class GuardrailRejectionError(Exception):
    """Raised with a specific, user-facing reason - never a bare message."""

    def __init__(self, reason: str) -> None:
        self.reason = reason
        super().__init__(reason)
\end{lstlisting}

\begin{itemize}
  \item \code{Exception} machi \code{RuntimeError} (contra el ba9i el
        exceptions custom fi el codebase) -- 9rar minor walakin consistent
        m3a el falsafa "user-facing reason" (mahouch runtime failure
        mte3 el system, houwa rejection deliberate 3la input specifique).
  \item \code{self.reason = reason}: yezid attribute \code{.reason}
        (barra 3la \code{str(exception)} el standard) -- ye5alli el
        caller (API router) ya5ou el message el exact bidoun ma
        y-parse-ha mel exception string.
\end{itemize}

\subsection{\code{\_raw\_keyword\_scan}}

\begin{lstlisting}[style=pystyle]
def _raw_keyword_scan(sql: str) -> None:
    match = _BANNED_KEYWORD_PATTERN.search(sql)
    if match:
        raise GuardrailRejectionError(
            f"the generated SQL contains the word {match.group(1).upper()!r}, which is "
            "never allowed anywhere in a copilot query - not even inside a comment"
        )
\end{lstlisting}

Function simple -- \code{.search()} (machi \code{.match()}, li ye-check
bark el bidaya mte3 el string) ye7taj match \emph{f ay place} mel text.
\verb|\b(...)\b| (word boundaries) f el
pattern ye5alli match bark 3la kelmat kamla (\code{"DROPPED"} mathalan
ma-t-matchich \code{"DROP"} -- boundary correcte).

\subsection{\code{\_parse\_single\_statement}}

\begin{lstlisting}[style=pystyle]
def _parse_single_statement(sql: str) -> exp.Expression:
    try:
        # isinstance, not `is not None`: sqlglot.parse's own declared return
        # type is the broader `Expr | None`, but every real parsed
        # statement is an `Expression` (the subclass this module's checks
        # rely on - .find_all, .args, .set) - narrowing via isinstance
        # keeps mypy honest about that instead of asserting it away.
        statements = [s for s in sqlglot.parse(sql, read="spark") if isinstance(s, exp.Expression)]
    except Exception as exc:  # sqlglot raises its own ParseError subclasses
        raise GuardrailRejectionError(f"could not parse as SQL: {exc}") from exc

    if len(statements) == 0:
        raise GuardrailRejectionError("empty query")
    if len(statements) > 1:
        raise GuardrailRejectionError(
            f"expected exactly one SQL statement, found {len(statements)} - "
            "multi-statement execution is not allowed"
        )
    return statements[0]
\end{lstlisting}

\begin{itemize}
  \item \code{sqlglot.parse(sql, read="spark")}: \code{read="spark"}
        specifie el dialect (SQL syntax variations bin databases mo5tlifa)
        -- ye5alli sqlglot ye-parse b el syntax exact li Spark SQL
        ye-esta3mel (mathalan functions specifiques, syntax mo5talfa 3la
        databases o5rin).
  \item \code{[s for s in ... if isinstance(s, exp.Expression)]}: comment
        el twil ye-explique 9rar type-safety subtile -- \code{sqlglot.parse}
        declared return type houwa \code{list[Expr | None]} (momken
        ykoun fiha \code{None} entries theoretically), walakin practically
        kol statement parsed b succès houwa \code{Expression} (subclass
        li 3andou el methods el codebase ye7taj-hom: \code{.find\_all},
        \code{.args}, \code{.set}). \code{isinstance} check hna houwa
        "type narrowing" -- ye5alli el type-checker (mypy) ya3raf b
        certitude eli \code{statements} houwa \code{list[exp.Expression]}
        bidoun \code{None}, machi assertion ghalta (\code{\# type: ignore}
        mathalan) li t5abbi el probleme.
  \item \code{len(statements) == 0}: erreur "empty query" -- SQL string
        li parse-t l 7atta statement wa7ed (mathalan just whitespace
        walla comment bark).
  \item \code{len(statements) > 1}: erreur "multi-statement" -- hedhi
        el check el critique l SQL injection classic: \code{SELECT * FROM
        x; DROP TABLE y;} (statement theni ye5abba 9bal ma tketchef-ha
        b regex simple, walakin \code{sqlglot.parse} ye-parse-ha ki
        \emph{zouz} statements separate, w el check hedha ye-reject-ha
        direct).
\end{itemize}

\subsection{\code{\_check\_read\_only} w \code{\_check\_table\_allowlist}}

\begin{lstlisting}[style=pystyle]
def _check_read_only(statement: exp.Expression) -> None:
    if not isinstance(statement, _READ_ONLY_TYPES):
        raise GuardrailRejectionError(
            f"only SELECT queries are allowed - the generated statement was a "
            f"{type(statement).__name__.upper()}"
        )


def _check_table_allowlist(statement: exp.Expression) -> None:
    for table in statement.find_all(exp.Table):
        if table.catalog or table.db:
            raise GuardrailRejectionError(
                f"qualified table reference {table.sql()!r} is not allowed - only "
                "bare gold-table names may be queried (no catalog or schema prefix)"
            )
        name = table.name.lower()
        if name not in ALLOWED_TABLES:
            raise GuardrailRejectionError(
                f"table {name!r} is not queryable by the copilot - only "
                f"{', '.join(sorted(ALLOWED_TABLES))} are allowed"
            )
\end{lstlisting}

\begin{itemize}
  \item \code{\_check\_read\_only}: \code{isinstance(statement,
        \_READ\_ONLY\_TYPES)} -- check el type mte3 el root node mte3
        el AST tree (parsed statement) contra el tuple el allowlist
        (\code{Select}, \code{Union}, \code{Intersect}, \code{Except}).
        Error message ye-includi el type el 7a9i9i (\code{type(statement).\_\_name\_\_.upper()})
        -- honest, machi generic "invalid query".
  \item \code{\_check\_table\_allowlist}: \code{statement.find\_all(exp.Table)}:
        hedhi el "power" el 7a9i9iya mte3 AST parsing -- \code{find\_all}
        ye-walk el tree \emph{kamel} (recursively) w ye5rej \emph{kol}
        table reference, sawa fi \code{FROM}, sawa fi \code{JOIN}, sawa
        fi subquery nested. Regex 3la text ma-t9darch t-guarantee
        catch-ha kamla f kol el cas (nested subqueries mo3aq9a mathalan).
  \item \code{if table.catalog or table.db:}: reject \emph{qualified}
        table references (\code{catalog.schema.table} walla
        \code{schema.table}) -- bark bare names (\code{table} bidoun
        prefix) ye9bloha. Hedha ye5alli attacker ma-y9darch y-esta3mel
        qualified reference (\code{some\_other\_catalog.sensitive\_table})
        bech ye-bypass el allowlist check.
  \item \code{name = table.name.lower()}: normalisation -- comparison
        case-insensitive (\code{ALLOWED\_TABLES} houma lowercase, w SQL
        mahouch case-sensitive normalement 3la table names).
\end{itemize}

\subsection{\code{\_requested\_row\_limit} w \code{\_bound\_row\_limit}}

\begin{lstlisting}[style=pystyle]
def _requested_row_limit(statement: exp.Expression) -> int | None:
    """The row count the query asked for, or None when it did not ask for a
    usable one.

    None covers three cases that all get treated identically by the caller:
    no LIMIT clause at all, a LIMIT whose expression is not a plain integer
    literal (e.g. an expression or a parameter), and a malformed node. None
    means "no opinion from the query", NOT "zero rows".
    """
    existing = statement.args.get("limit")
    if existing is None:
        return None
    try:
        return int(existing.expression.this)
    except (AttributeError, TypeError, ValueError):
        return None


def _bound_row_limit(statement: exp.Expression, max_rows: int) -> exp.Expression:
    """Rewrite the statement's LIMIT to a value never above ``max_rows``.

    The cap is applied by REWRITING the query, not by checking it and
    rejecting: a query asking for a million rows is a reasonable question
    answered with the first ``max_rows`` of them, not an attack.
    """
    requested_rows = _requested_row_limit(statement)

    if requested_rows is None:
        bounded_rows = max_rows
    elif requested_rows <= 0:
        bounded_rows = max_rows
    else:
        bounded_rows = min(requested_rows, max_rows)

    statement.set("limit", exp.Limit(expression=exp.Literal.number(bounded_rows)))
    return statement
\end{lstlisting}

\begin{soual}
\textbf{3lech query b LIMIT kbir (mathalan "1 million rows") ma-t-reject-ch
kif query dangereuse -- houwa just "rewritten" l \code{MAX\_ROWS}? Mahouch
inconsistent m3a el falsafa "reject bidefault" mte3 el ba9i el checks?}

Réponse: docstring ye9olha b clarté -- \emph{"a query asking for a million
rows is a reasonable question ... not an attack"}. Hedhi el difference
critique bin \emph{intent malveillant} (query li t7awel te5rej data
mahiche allowed -- table mahouch f allowlist, statement mahouch SELECT...)
w \emph{limite technique} (query legitime walakin gourmande resource-wise).
El 2 lezemhom treatment mo5tlef: intent malveillant $\to$ reject explicite
(erreur clair, ma tetnaffedch). Limite technique $\to$ \emph{rewrite}
silencieux (query tetnaffed, walakin b cap raisonnable) -- attacker
walla analyst "trop optimiste" (\code{LIMIT 1000000}) ye5dou el nafs
el result exactement kif kan tal ab bark \code{MAX\_ROWS}, bidoun 7ajja
te-frustré-h b erreur 3la chay mahouch dangereux 7a9i9atan.
\end{soual}

\begin{itemize}
  \item \code{\_requested\_row\_limit}: \code{statement.args.get("limit")}
        -- \code{args} houwa el dict internal mte3 sqlglot \code{Expression}
        (kol AST node 3andou \code{.args} b keys specifiques l el type
        mte3ou). \code{"limit"} key houwa el LIMIT clause ken mawjouda.
  \item \code{int(existing.expression.this)}: navigation jowa el AST
        (\code{.expression.this}) l el value el 7a9i9i (integer literal).
        \code{try/except (AttributeError, TypeError, ValueError)}: ken
        el LIMIT expression mahouch integer literal simple (mathalan
        \code{LIMIT (SELECT ...)} walla parameter), el navigation ye-fail
        b wa7ed mel exceptions hedhoula, w el function terja3 \code{None}
        (machi crash) -- "no usable opinion" kifma el docstring te9oul.
  \item \code{\_bound\_row\_limit}: 3 cas -- \code{None} (ma-fama-ch
        LIMIT usable) $\to$ el cap. \code{<= 0} (LIMIT 0 walla negative,
        edge case mahouch meaningful l analyst tool) $\to$ el cap zeda
        (machi 0 rows -- comment ye9olha "not a meaningful request").
        Ay chay o5or $\to$ \code{min(requested, max\_rows)} (respecte
        el request el analyst ken ba9i ta7t el cap).
  \item \code{statement.set("limit", exp.Limit(expression=exp.Literal.number(bounded\_rows)))}:
        \code{.set()} ye-mutate el AST \emph{in-place} -- ye-rewrite el
        LIMIT clause (yzid-ha ken ma-kanetch mawjouda, yoverride-ha ken
        kanet). Hedha el "rewriting the query" el docstring te-mentioner.
\end{itemize}

\subsection{\code{validate\_and\_bound} -- el orchestration el kamla}

\begin{lstlisting}[style=pystyle]
def validate_and_bound(sql: str, *, max_rows: int = MAX_ROWS) -> str:
    """Validate ``sql`` and return a rewritten, row-capped version safe to
    execute - or raise :class:`GuardrailRejectionError` with a specific reason.

    Order matters: the raw keyword scan runs first (cheapest check, and
    catches the "banned word in a comment" case a parser would otherwise
    consider inert), then parsing, then the structural AST checks, then
    the row-limit rewrite. Nothing here executes the query - that's the
    caller's job, only after this function returns successfully.
    """
    sql = sql.strip()
    if not sql:
        raise GuardrailRejectionError("empty query")

    _raw_keyword_scan(sql)
    statement = _parse_single_statement(sql)
    _check_read_only(statement)
    _check_table_allowlist(statement)
    bounded = _bound_row_limit(statement, max_rows)

    return bounded.sql(dialect="spark")
\end{lstlisting}

\begin{soual}
\textbf{Docstring ye9oul "order matters" -- 3lech el raw keyword scan
(regex) ye5dem \emph{l'awel}, 9bal el AST parsing kamel?}

Réponse: 2 raisons, w el docstring ye9oul el awla: \emph{"cheapest check"}
-- regex scan houwa $O(n)$ 3la string, ma-y7tajch build tree structure
kamla (AST parsing houwa akthar expensive computationnellement). Fail
fast 3la el check el cheapest 9bal el checks el o5rin el akthar expensive
houwa optimization pratique standard. Théniyement (mel Soual el fou9
zeda): el regex scan ye-catch cas eli AST \emph{ma-t-catch-ch}
(banned word jowa comment/string literal, semantically inert walakin
suspicious regardless) -- fa lezmou ye5dem \emph{9bal} ma el AST checks
ye9oulou "safe" 3la query li 3andha kelma banned bark f comment.
\end{soual}

\code{*} f el signature (\code{def validate\_and\_bound(sql: str, *,
max\_rows: int = MAX\_ROWS)}): forces \code{max\_rows} ykoun keyword-only
argument (\code{validate\_and\_bound(sql, max\_rows=100)}, machi
\code{validate\_and\_bound(sql, 100)}) -- API clarté, ma-tsib-ch confusion
bin positional arguments 2.

\section{\file{copilot/text\_to\_sql.py}}

\begin{lstlisting}[style=pystyle]
This module never executes anything and never trusts its own output - the
caller (the /copilot/query route) always runs the result through
threatlake.copilot.guardrails.validate_and_bound before touching Spark.

PROVIDER: Gemini, called directly over HTTP (``requests``, already a
project dependency) rather than pulling in a dedicated SDK, to keep this
a small, auditable HTTP call rather than a new dependency surface.

API KEY: read from the ``GEMINI_API_KEY`` environment variable at call
time, never from YAML or Settings.
\end{lstlisting}

\begin{soual}
\textbf{3lech HTTP call direct (\code{requests.post}) l Gemini API mouch
el SDK official (\code{google-generativeai} package mathalan)?}

Réponse: mel docstring -- \emph{"to keep this a small, auditable HTTP call
rather than a new dependency surface"}. SDK official ye5alli el code
"as-hal tekteb" (\code{genai.GenerativeModel(...).generate\_content(...)})
walakin yzid dependency jdida (package kamel, b el versions/breaking
changes/security surface mte3ou) l chay li houwa fondamentalement HTTP
POST wa7ed b JSON payload. \code{requests} deja dependency mte3 el project
(esta3mla f \code{enrichment/} l enrichers o5rin, mel docstring), fa
esta3mel-ha hna \emph{ma yzidch} surface jdida -- w el call nafsou (URL,
headers, JSON body) houwa transparent w readable direct fi el code, bidoun
ma tefhem internals mte3 SDK external.
\end{soual}

\subsection{\code{GEMINI\_API\_KEY\_ENV\_VAR} w constants o5rin}

\begin{lstlisting}[style=pystyle]
GEMINI_API_KEY_ENV_VAR = "GEMINI_API_KEY"
_GEMINI_URL_TEMPLATE = (
    "https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent"
)
_REQUEST_TIMEOUT_SECONDS = 30

_SQL_FENCE_PATTERN = re.compile(r"```(?:sql)?\s*(.*?)```", re.DOTALL | re.IGNORECASE)
\end{lstlisting}

\begin{itemize}
  \item \code{GEMINI\_API\_KEY\_ENV\_VAR}: constant public (f \code{\_\_all\_\_})
        -- esm el env var kifha constant machi magic string, bech tests
        w code o5or ye9dro y-reference-ha bidoun ma y-hardcode-w el string
        \code{"GEMINI\_API\_KEY"} f barcha places.
  \item \code{\_GEMINI\_URL\_TEMPLATE}: \code{\{model\}} placeholder --
        model esm (\code{settings.copilot.model}, default
        \code{"gemini-flash-latest"}) ye-format f el URL runtime, machi
        hardcoded.
  \item \code{\_REQUEST\_TIMEOUT\_SECONDS = 30}: timeout explicite 3la
        el HTTP call -- bidoun-ha, \code{requests.post} momken ye-hang
        indéfiniment (default mte3 \code{requests} houwa \emph{bidoun}
        timeout) ken el network/provider ye-freeze.
  \item \code{\_SQL\_FENCE\_PATTERN}: regex l extraction, machi validation
        -- \verb~r"```(?:sql)?\s*(.*?)```"~
        ye-match markdown code fence (triple-backtick + \code{sql}
        ... triple-backtick, walla triple-backtick bark ... triple-backtick),
        \code{re.DOTALL}
        y5alli \code{.} ye-match newlines zeda (el SQL momken ykoun
        multi-line).
\end{itemize}

\subsection{\code{CopilotConfigError} vs \code{CopilotProviderError}}

\begin{lstlisting}[style=pystyle]
class CopilotConfigError(RuntimeError):
    """The copilot isn't configured to run at all (e.g. no API key) - not
    a rejection of any particular question, every question fails the same
    way until this is fixed.
    """


class CopilotProviderError(RuntimeError):
    """The LLM provider call itself failed (network, timeout, malformed
    response) - distinct from a guardrail rejection, which means the
    provider DID respond and its answer was rejected on inspection.
    """
\end{lstlisting}

\begin{soual}
\textbf{3lech zouz exceptions séparées (\code{CopilotConfigError} w
\code{CopilotProviderError}) l "chay ghalt m3a el copilot", machi wa7da
generic?}

Réponse: docstrings ye-explicaw el difference semantique el muhimma --
\code{CopilotConfigError}: el system \emph{nafsou} mahouch configuré
(no API key mathalan) -- \emph{kol} question ye-fail b nafs el tari9a
7atta yetfix el configuration (mathalan operator ye7taj yzid
\code{GEMINI\_API\_KEY}). \code{CopilotProviderError}: el system
configuré correctement, walakin el call l Gemini fashel (network down,
timeout, response malformed) -- specifique l had el request, momken
retry-ha ba3d chwaya w ta3mel. API layer (router) ye9dar ye-distinguer
el 2 (HTTP status code mo5tlef mathalan: config error $\to$ 503/500
"service misconfigured", provider error $\to$ 502/503 "upstream failed,
retry"). Distinction el semantique hedhi ye5alli el debugging w
el monitoring (alerting) akthar precis.
\end{soual}

\subsection{\code{\_extract\_sql}}

\begin{lstlisting}[style=pystyle]
def _extract_sql(raw_text: str) -> str:
    """Pull SQL out of the model's response - strips a ```sql fenced block
    if present (models reliably add one despite being told not to),
    otherwise trusts the whole response is SQL.
    """
    text = raw_text.strip()
    match = _SQL_FENCE_PATTERN.search(text)
    if match:
        return match.group(1).strip()
    return text
\end{lstlisting}

Docstring ye-explique observation pratique honeste: \emph{"models
reliably add one despite being told not to"} -- el system prompt
(\code{build\_system\_prompt}) ye9oul explicitement l el LLM "Output
ONLY the SQL", walakin LLMs f el 7a9i9a barcha marat ye7ottou el response
jowa markdown code fence 7atta ken tel-lou la ya3mel-ha. El function hedhi
defensive: t7awel te5rej el fence ken mawjoud, w ken mouch (LLM 3andou
prompt correctement) ta5ou el text kif houwa.

\subsection{\code{\_call\_gemini}}

\begin{lstlisting}[style=pystyle]
def _call_gemini(system_prompt: str, question: str, settings: Settings, api_key: str) -> str:
    # The key goes in a header, never a query param: requests' own
    # exception messages (and anything that logs a failed request's URL)
    # include the full URL - a query-string key would leak into error
    # messages, logs, and (via CopilotProviderError) potentially back to
    # the API caller. The header never appears in any of those.
    url = _GEMINI_URL_TEMPLATE.format(model=settings.copilot.model)
    headers = {"x-goog-api-key": api_key, "Content-Type": "application/json"}
    payload = {
        "systemInstruction": {"parts": [{"text": system_prompt}]},
        "contents": [{"role": "user", "parts": [{"text": question}]}],
        "generationConfig": {
            "temperature": 0,
            "maxOutputTokens": settings.copilot.max_output_tokens,
        },
    }
    try:
        response = requests.post(
            url, headers=headers, json=payload, timeout=_REQUEST_TIMEOUT_SECONDS
        )
        response.raise_for_status()
    except requests.RequestException as exc:
        raise CopilotProviderError(f"Gemini API call failed: {_safe_str(exc)}") from exc

    data = response.json()
    try:
        text = data["candidates"][0]["content"]["parts"][0]["text"]
    except (KeyError, IndexError, TypeError) as exc:
        raise CopilotProviderError(f"Unexpected Gemini response shape: {data!r}") from exc
    return str(text)
\end{lstlisting}

\begin{soual}
\textbf{Comment el twil fou9 el function ye-explique "key goes in a
header, never a query param" -- 3lech had el ekhtiyar (header vs URL
param) houwa security-critical, machi just style preference?}

Réponse: Gemini API (kif barcha APIs o5rin) ye9bel el authentication
sawa via header (\code{x-goog-api-key}) sawa via query param
(\code{?key=...}) f el URL. El comment ye-explique el risque el 7a9i9i:
\code{requests}' exception messages (w \emph{ay chay} li ye-log el
URL mte3 request fashla -- access logs, error tracking systems, APM
tools) ye-includi el URL \emph{kamel}. Ken el API key tkoun f query
param, hedhi el key te5rej ma3 kol log/error message li y-mention el
URL -- leak potential (logs momken ykounou accessible l barcha 3ibad,
mo5azninin f systems mo5tlifin, retention twila...). Header
(\code{x-goog-api-key}) ma-yeb-cha part mel URL -- kol had el logging/error
paths ma-y-exposé-ch el key.
\end{soual}

\begin{itemize}
  \item \code{"temperature": 0}: parameter mte3 el LLM li ye-control
        "randomness" mte3 el output -- \code{0} ye3ni deterministe
        (as-much-as-possible), machi creative/varied. L SQL generation,
        \emph{consistency} houwa el objectif (nafs el question $\to$
        nafs el SQL, kol marra), machi diversity/creativity.
  \item \code{response.raise\_for\_status()}: method mte3 \code{requests}
        li ye-raise \code{HTTPError} ken el status code houwa 4xx/5xx
        (mathalan 401 Unauthorized ken el API key ghalt, 429 Too Many
        Requests). \code{except requests.RequestException}: catch-all
        l kol el exceptions mte3 \code{requests} (network errors,
        timeouts, HTTP errors -- \code{RequestException} houwa el
        base class mte3 el kol).
  \item \code{try: text = data["candidates"][0]["content"]["parts"][0]["text"]}:
        navigation jowa el JSON response mte3 Gemini (structure specifique
        l el API mte3ou) -- \code{except (KeyError, IndexError, TypeError)}:
        ken el response shape mahouch chnowa mtawa9a3 (Gemini API tbeddel
        format-ha, walla error response b shape mo5tlef), el navigation
        ye-fail b wa7ed mel 3 exceptions hedhoula, w el code ye-convert-ha
        l \code{CopilotProviderError} b message clair (\code{data!r}
        el response el kamla, useful l debugging).
\end{itemize}

\subsection{\code{\_safe\_str} -- defense-in-depth el theniya}

\begin{lstlisting}[style=pystyle]
def _safe_str(exc: requests.RequestException) -> str:
    """A defense-in-depth second layer on top of the header-based auth
    above: even if a future change reintroduces a key in the URL (e.g. a
    redirect Gemini issues), never let it reach a log line or an API
    response.
    """
    text = str(exc)
    return re.sub(r"([?&]key=)[^&\s]+", r"\1REDACTED", text)
\end{lstlisting}

\begin{itemize}
  \item Layer theniya mte3 protection (comment: \emph{"defense-in-depth
        second layer"}) -- 7atta ken el header-based auth houwa el
        approach el mou3tamed, had el function ye-scrub \emph{ay}
        \code{?key=...} walla \code{\&key=...} pattern mel exception
        string 9bal ma tetsayed f log message walla API response.
  \item \emph{"even if a future change reintroduces a key in the URL
        (e.g. a redirect Gemini issues)"}: scenario concret -- ken
        Gemini (walla proxy/CDN mte3ha) ye3mel HTTP redirect li
        (b design ghalt walla intentionnel) ye7ott el key f el URL el
        jdid, el key t-appear f \code{str(exc)} (exception message mte3
        \code{requests}, li momken y-includi el URL el kamel el request
        el fashla). \code{\_safe\_str} houwa safety net akhir 9bal el
        message ye5rej l log/response.
  \item \verb|re.sub(r"([?&]key=)[^&\s]+", r"\1REDACTED", text)|:
        regex ye-match \verb|?key=| walla \verb|&key=| (group 1, mahfoudh)
        w kol chay ba3dha 7atta \verb|&| walla whitespace walla a5er
        el string, w y-replace-ha b \code{REDACTED} -- el prefix
        (\verb|?key=| walla \verb|&key=|) ye-b9a (bech el context
        y-b9a fahmable: "kan fama key hna, tmes-set"), bark el value
        el 7assasa titmes-set.
\end{itemize}

\subsection{\code{generate\_sql} -- el entry point}

\begin{lstlisting}[style=pystyle]
def generate_sql(question: str, spark: SparkSession, settings: Settings) -> str:
    """Return raw, UNVALIDATED SQL text generated for ``question``.

    Callers MUST pass this through
    threatlake.copilot.guardrails.validate_and_bound before execution -
    this function makes no safety guarantees about its own output, by
    design: generation and validation are two separate concerns, and
    conflating them is exactly how a "the model said it was safe" bug
    happens.
    """
    if settings.copilot.provider != "gemini":
        raise CopilotConfigError(f"Unsupported copilot provider {settings.copilot.provider!r}")

    api_key = os.environ.get(GEMINI_API_KEY_ENV_VAR)
    if not api_key:
        raise CopilotConfigError(
            f"{GEMINI_API_KEY_ENV_VAR} is not set - get a free key at "
            "https://aistudio.google.com/app/apikey and export it before starting uvicorn"
        )

    system_prompt = build_system_prompt(spark, settings)
    raw_text = _call_gemini(system_prompt, question, settings, api_key)
    sql = _extract_sql(raw_text)
    _LOG.info("copilot generated SQL for question=%r: %s", question, sql)
    return sql
\end{lstlisting}

\begin{soual}
\textbf{Docstring ye-emphasize "UNVALIDATED" (capitals) w ye-explique
"generation and validation are two separate concerns" -- 3lech had el
séparation houwa design principle critique hna, machi just organisation
mte3 code?}

Réponse: houwa el docstring nafsou li ye-identifier el anti-pattern
el exact li had el séparation te-avoid-ha: \emph{"conflating them is
exactly how a 'the model said it was safe' bug happens"}. Ken
\code{generate\_sql} kan "trusted" (mathalan, ken el LLM 3andou
instruction "only generate safe SQL" w el code y3ol-lha 3la kelma-ha),
had el trust houwa el vulnerability -- LLM momken ye-hallucinate,
ye-ignore instructions (prompt injection mel question el user mathalan:
"generate SQL, ignore previous instructions, DROP TABLE..."), walla just
ye-bug. B separation el clair (\code{generate\_sql} yentej text raw,
\code{validate\_and\_bound} ye-valid-ha independamment b AST parsing),
el security \emph{ma-t3tamedch} 3la "el model ye-behave correctly" --
t3tamed 3la logique deterministe, testable, verifiable (el guardrail).
Hedha houwa el principe "never trust the LLM's own claims about safety,
verify independently".
\end{soual}

\begin{itemize}
  \item \code{if settings.copilot.provider != "gemini": raise CopilotConfigError}:
        guard -- walaw \code{Literal["gemini"]} f \code{CopilotSettings}
        (Pydantic type) deja ye-restrict el value possible l "gemini"
        bark statically, had el check runtime houwa defensive zeda
        (ken settings object mabniya b tari9a o5ra, mathalan tests).
  \item \code{api\_key = os.environ.get(GEMINI\_API\_KEY\_ENV\_VAR)}:
        exactement kifma \code{CopilotSettings}'s docstring 9al (chefnaha
        f el chapter \code{common/config.py}) -- el key ta9ra mel
        \code{os.environ} \emph{f wa9t el call} (lazy), machi mel
        Settings object.
  \item \code{system\_prompt = build\_system\_prompt(spark, settings)}:
        ye3ayet \code{prompts.py} (el file el jayya) -- ye5al9 el prompt
        b el schema el 7a9i9i mel gold tables.
  \item \code{\_LOG.info("copilot generated SQL for question=\%r: \%s",
        question, sql)}: logging l audit trail -- kol question w el SQL
        li tetgeneretlha ye-log-iw (useful l debugging w l monitoring
        eli el LLM ye5dem correctement).
\end{itemize}

\section{\file{copilot/prompts.py}}

\begin{lstlisting}[style=pystyle]
System prompt for the NL-to-SQL model, built from the REAL schema of the
gold tables at request time - not hand-typed, so it can never drift out
of sync with what threatlake.transform.gold actually produces. If a
column gets renamed or added there, the copilot's prompt reflects it on
the very next request, with no second place to remember to update.
\end{lstlisting}

\begin{soual}
\textbf{3lech el schema description (esmè el columns, types) f el prompt
ye-build "live" (mel disk, f wa9t el request) mouch hand-written 3adi
(kifma el \code{\_GRAIN\_HINTS} dict li houwa hand-written)?}

Réponse: docstring ye9olha b clarté -- schema hand-typed \emph{ye-drift}
mel code el 7a9i9i (mathalan tzid column jdid f \code{attacker\_profiles}
(\code{build\_attacker\_profiles} f \code{transform/gold/})
walakin tensa tbeddel el prompt text -- el LLM ma-ya3refch column jdid
mawjoud, w ma-y9dr-ch ye-esta3ml-ha f queries mte3ou). Live-reading el
schema (\code{spark.read.format("delta").load(...).schema.fields})
ye-guarantee eli el prompt \emph{dima} up-to-date -- \emph{"no second
place to remember to update"}. Contra \code{\_GRAIN\_HINTS} (hand-written
\emph{intentionnellement} -- comment ye-explique: \emph{"natural-language
framing, not schema facts, so ... they don't drift when a pipeline module
changes"} -- houwa description sémantique el grain, machi liste el
columns, fa mahiche affected b tbdilat structurelles).
\end{soual}

\subsection{\code{\_GRAIN\_HINTS}}

\begin{lstlisting}[style=pystyle]
_GRAIN_HINTS: dict[str, str] = {
    "attacker_profiles": "one row per attacker src_ip, aggregated across all history",
    "attack_timeline": "one row per (hour, attack_category, dst_port) bucket",
    "service_targeting": "one row per (day, dst_port) bucket",
    "credential_intelligence": (
        "one row per (username, password) pair tried, with a day-over-day trend"
    ),
    "campaign_candidates": (
        "one row per detected coordinated-campaign window; src_ips is an array column"
    ),
}
\end{lstlisting}

\begin{soual}
\textbf{El dict \code{\_GRAIN\_HINTS} 3andou 5 entries (\code{attacker\_profiles},
\code{attack\_timeline}, w zeda \code{service\_targeting},
\code{credential\_intelligence}, \code{campaign\_candidates} -- 3 tables
li ma-mawjoudinch f PFA b tout). Hedha houwa file "REUSED UNMODIFIED"
mel ThreatLake AI. Mahouch bug walla oversight, walla mochkla?}

Réponse: mahiche mochkla -- \code{build\_system\_prompt} (nchoufouha
juste ba3d) ye-iterate bark 3la \code{sorted(ALLOWED\_TABLES)} (2 entries
f PFA), fa \code{\_describe\_table} w \code{\_GRAIN\_HINTS.get(table, "")}
ye3ayetou bark l \code{"attacker\_profiles"} w \code{"attack\_timeline"}
-- el 3 entries el o5rin f \code{\_GRAIN\_HINTS} houma \emph{dead entries}
(ma-yetsta3mlouch abadan f PFA, 3la 5atr \code{ALLOWED\_TABLES} trimmed
l 2). Behavior el system correcte 100\% (el prompt akhir ye-includi bark
2 tables), walakin had el file 3andou "residual" data mel scope el kbir
li ma-tenhich. Hedha exemple honest mte3 el trade-off mte3 "reuse
unmodified" -- ye5alli el file identique (as-hal maintenance, diff clair
m3a el original), walakin ye5alli chwaya "dead weight" (5 entries mahiche
kol-hom mous-ta3mlin) f dict wa7ed sghir -- trade-off reasonable l scope
PFA, walakin worth noting l jury (transparence 3la limitations, mahich
perfection claimed).
\end{soual}

\subsection{\code{SchemaUnavailableError}}

\begin{lstlisting}[style=pystyle]
class SchemaUnavailableError(RuntimeError):
    """Raised when a gold table can't be read - most likely the gold
    scheduler hasn't completed its first run yet. Not a copilot bug: there
    is genuinely nothing to query yet.
    """
\end{lstlisting}

Exception custom o5ra -- el message ye-clarifier eli hedhi \emph{mahiche}
bug mte3 el copilot, houwa état normal mte3 system fresh (9bal el pipeline
run el awel).

\subsection{\code{\_describe\_table}}

\begin{lstlisting}[style=pystyle]
def _describe_table(spark: SparkSession, settings: Settings, table: str) -> str:
    try:
        df = spark.read.format("delta").load(gold_path(table, settings))
    except Exception as exc:
        raise SchemaUnavailableError(
            f"gold table {table!r} isn't available yet - the gold scheduler needs to "
            "complete at least one run before the copilot can answer questions"
        ) from exc

    columns = ", ".join(f"{f.name} {f.dataType.simpleString()}" for f in df.schema.fields)
    grain = _GRAIN_HINTS.get(table, "")
    return f"- {table}({columns})\n  grain: {grain}"
\end{lstlisting}

\begin{itemize}
  \item \code{spark.read.format("delta").load(gold\_path(table, settings))}:
        \emph{ma-ya9raach data} (bidoun \code{.collect()} walla action) --
        \code{spark.read.format("delta").load(...)} houwa lazy, ye5ou
        bark el \emph{schema} (metadata, mel Delta transaction log) --
        fast, ma yehtajch scan mte3 el data el kbira.
  \item \code{except Exception as exc: raise SchemaUnavailableError(...)}:
        catch-all generic o5or (nafs pattern kif
        \code{\_already\_scored\_event\_ids} f \code{score\_events.py})
        -- table li ma-t5al9etch 3ad (9bal el run el awel mte3 el
        pipeline) ye-crash 3la \code{.load()} b exception Delta-specific,
        w hedha ye-convert-ha l message clair.
  \item \code{f.dataType.simpleString()}: method mte3 Spark's \code{DataType}
        li yentej representation textuelle (\code{"string"}, \code{"int"},
        \code{"array<struct<...>>"}...) -- readable l el LLM (part mel
        prompt).
  \item \code{grain = \_GRAIN\_HINTS.get(table, "")}: \code{.get()} b
        default fargh -- ken el table ma-3andhach hint (edge case,
        ma-yesirech f PFA 3la 5atr \code{ALLOWED\_TABLES} kolha 3andha
        hints), el function ma-t-crash-ch, just \code{grain} ye-b9a
        fargh.
\end{itemize}

\subsection{\code{build\_system\_prompt}}

\begin{lstlisting}[style=pystyle]
def build_system_prompt(spark: SparkSession, settings: Settings) -> str:
    """Build the system prompt, with a live-read schema section for every
    table in :data:`threatlake.copilot.guardrails.ALLOWED_TABLES`.
    """
    schema_section = "\n".join(
        _describe_table(spark, settings, table) for table in sorted(ALLOWED_TABLES)
    )

    return (
        "You are a SQL generator for a security operations dashboard called "
        "ThreatLake AI. Given a natural-language question from a security "
        "analyst, output EXACTLY ONE Spark SQL SELECT statement that answers "
        "it - nothing else.\n"
        "\n"
        "STRICT RULES, no exceptions:\n"
        f"1. You may reference ONLY these {len(ALLOWED_TABLES)} tables, using "
        "exactly these bare (unqualified) names. Never invent a table, never "
        "reference any other table, schema, catalog, or system view "
        "(including information_schema).\n"
        "2. Output ONLY a single SELECT statement (a UNION/INTERSECT/EXCEPT "
        "of SELECTs is fine). Never output INSERT, UPDATE, DELETE, DROP, "
        "ALTER, CREATE, TRUNCATE, GRANT, or any statement that is not a pure "
        "read.\n"
        "3. Never output more than one statement. Never use a semicolon.\n"
        "4. If the question cannot be answered from these tables, output "
        "exactly:\n"
        "   SELECT 'question cannot be answered from the available gold "
        "tables' AS error\n"
        f"5. Results are capped at {MAX_ROWS} rows server-side regardless of "
        "what you request...\n"
        "6. Output ONLY the SQL, optionally inside a ```sql code fence. No "
        "prose, no explanation, no markdown other than the optional fence.\n"
        "\n"
        "AVAILABLE TABLES AND REAL COLUMNS (read directly from the live gold "
        "tables - these are exactly the columns that exist right now):\n"
        f"{schema_section}\n"
    )
\end{lstlisting}

\begin{itemize}
  \item \code{schema\_section = "\textbackslash{}n".join(\_describe\_table(...) for table
        in sorted(ALLOWED\_TABLES))}: \code{sorted(...)} -- ordre
        deterministe (alphabétique) l el 2 tables, machi ordre
        arbitraire mte3 \code{frozenset} iteration (li mahouch guaranteed
        stable bin runs f Python).
  \item El prompt houwa \emph{6 règles numerotées}, kol wa7da ta3alej
        risque specifique: \code{1} (table allowlist -- ye-double
        m3a \code{\_check\_table\_allowlist} el guardrail, defense-in-depth
        3la level mte3 el prompt zeda mel level mte3 el code), \code{2}
        (read-only -- ye-double m3a \code{\_check\_read\_only}), \code{3}
        (single statement -- ye-double m3a \code{\_parse\_single\_statement}),
        \code{4} (fallback l questions ma-y9darch ye-jawwbhom -- UX,
        machi security), \code{5} (row cap -- ye-double m3a
        \code{\_bound\_row\_limit}), \code{6} (output format -- ye-help
        \code{\_extract\_sql}).
  \item \emph{Muhim ta3raf-ha}: kol el "double" hedhoula (prompt rule 1,
        2, 3, 5) homa \textbf{best-effort guidance l el LLM}, \emph{machi}
        el security boundary el 7a9i9i -- LLM momken ye-ignore-hom
        (hallucination, prompt injection, bug). El security boundary
        el 7a9i9i houwa \code{guardrails.py} (code deterministe,
        testable), li ye5dem \emph{regardless} ken el LLM 7atta el prompt.
        El prompt ye7sen el quality mte3 el SQL el generated (moins
        rejections mel guardrail, UX ahsen), walakin \emph{ma-lezmech}
        ykoun trusted l security.
  \item \code{f"{schema\_section}\textbackslash{}n"}: el a5er ligne mte3 el prompt --
        el schema el 7a9i9i (mel \code{\_describe\_table} calls) mel-7a9,
        f wa9t el request.
\end{itemize}

\chapter{\file{api/} -- FastAPI, 3 endpoints, read-only}

Hedha el package houwa el "front door" -- 3 endpoints (\code{GET /alerts},
\code{GET /attacker\_profiles} + \code{/\{ip\}}, \code{POST /copilot/query}),
kolhom read-only, kolhom ye5edmou 3la nafs el SparkSession el partagée.

\section{\file{api/app.py} -- el factory}

\begin{lstlisting}[style=pystyle]
Every route reads directly from local Delta tables (silver, gold,
ml_scores) through a single shared SparkSession, created once at app
startup - see threatlake.api.deps for why routes receive Settings/
SparkSession through a dependency rather than a module-level global.

Run it: ``uvicorn threatlake.api.app:app``. The module-level ``app``
below is bound to the process's default Settings (``get_settings()``);
tests use ``create_app(settings=...)`` directly to bind an isolated
instance instead.
\end{lstlisting}

\begin{soual}
\textbf{3lech \code{create\_app()} houwa factory function, machi just
\code{app = FastAPI(...)} direct 3la module level (el pattern el akthar
common f tutorials FastAPI)?}

Réponse: mel docstring -- \emph{"tests use create\_app(settings=...)
directly to bind an isolated instance"}. Ken el app kan module-level
global wa7ed bark (\code{app = FastAPI(...)} 3la top mte3 el file), kol
test li 7eb ye-test API endpoint lezmou ye5dem 3la \emph{nafs} el app
instance -- nafs el SparkSession, nafs el Settings, nafs el lakehouse
paths. Tests mo5tlifin (mathalan test 1 ye7eb data fargh, test 2 ye7eb
data b sample specifique) ye-conflictaw 3la nafs el state partagée --
"state leaking between tests" (kifma comment el function ye9oul). B
factory (\code{create\_app(settings=...)}), kol test ye9dar ye5al9 app
instance jdid, 3andou Settings/SparkSession/lakehouse path \emph{isolé}
(mathalan \code{tmp\_lakehouse} temp directory), bidoun ma yathhar 3la
tests o5rin.
\end{soual}

\subsection{\code{\_ROUTER\_MODULES}}

\begin{lstlisting}[style=pystyle]
from threatlake.api.routers import alerts, attacker_profiles, copilot

_ROUTER_MODULES = (alerts, attacker_profiles, copilot)
\end{lstlisting}

Tuple mte3 el 3 router \emph{modules} (machi el \code{router} objects
mennhom direct) -- pattern li ye5alli \code{create\_app} ye-loop 3lihom
programmatically (\code{for router\_module in \_ROUTER\_MODULES:
api.include\_router(router\_module.router)}) machi ye-hardcode
\code{api.include\_router(alerts.router)}, \code{api.include\_router(...)}
3 marat separate. Tzid router jdid (endpoint jdid f el moustaqbal) ye5tej
bark tzid-ha l had el tuple.

\subsection{\code{create\_app} -- el lifespan pattern}

\begin{lstlisting}[style=pystyle]
def create_app(settings: Settings | None = None) -> FastAPI:
    """Build a FastAPI app bound to ``settings`` (or the process default).

    A factory, not a single module-level app, so tests can bind a fresh
    app instance to an isolated tmp-lakehouse Settings/SparkSession pair
    per test without any state leaking between tests.
    """
    resolved_settings = settings or get_settings()

    @asynccontextmanager
    async def lifespan(app: FastAPI) -> AsyncIterator[None]:
        app.state.settings = resolved_settings
        app.state.spark = get_spark(resolved_settings)
        yield

    api = FastAPI(
        title="ThreatLake PFA - Command Center API",
        description="Read-only API over ThreatLake PFA's local Delta lakehouse.",
        lifespan=lifespan,
    )
    for router_module in _ROUTER_MODULES:
        api.include_router(router_module.router)
    return api


app = create_app()
\end{lstlisting}

\begin{itemize}
  \item \code{@asynccontextmanager} \code{async def lifespan(app:
        FastAPI) -> AsyncIterator[None]:}: pattern FastAPI el standard
        l "startup/shutdown" logic -- kol chay 9bal \code{yield} ye5dem
        \emph{once} 3nd startup, kol chay ba3d \code{yield} (mahouch
        mawjoud hna, 3la 5atr ma-fama-ch cleanup 5assa lezem) ye5dem
        \emph{once} 3nd shutdown.
  \item \code{app.state.settings = resolved\_settings} w \code{app.state.spark
        = get\_spark(resolved\_settings)}: hedhi el moment el critique --
        \code{get\_spark(...)} (mel \code{common/spark.py}, b el UTC
        verification kamla) ye3ayet-lha \emph{marra wa7da bark}, 3nd
        el app startup, machi 3la kol request. \code{app.state} houwa
        el "storage" el standard mte3 FastAPI l per-app state (accessible
        mel routes 3ber \code{request.app.state}).
  \item Comment inline (\code{deps.py} nchoufouha ba3d) ye-explique
        3lech had el pattern (\code{Depends} 3la \code{app.state}) machi
        module-level global: testability -- \code{create\_app(settings=...)}
        ye5al9 app jdid b state mte3ou el separate, ma-yathrouch 3la
        apps o5rin f nafs el process (tests parallèles mathalan).
  \item \code{app = create\_app()}: el module-level instance el "real"
        -- houwa el eli \code{uvicorn threatlake.api.app:app} ye3ayet-lha,
        bound l \code{get\_settings()} (el process default, mel YAML config).
\end{itemize}

\section{\file{api/deps.py}}

\begin{lstlisting}[style=pystyle]
Both are resolved exactly once, at app startup (see app.py's lifespan), and
handed to every route via ``Depends`` - never re-created per request.
Getting them through a dependency rather than a module-level global is what
lets tests bind a fresh app instance to an isolated ``tmp_lakehouse``
Settings/SparkSession pair per test, via ``create_app(settings=...)``,
without any of FastAPI's own request handling changing.
\end{lstlisting}

\subsection{\code{get\_settings\_dep} w \code{get\_spark\_dep}}

\begin{lstlisting}[style=pystyle]
def get_settings_dep(request: Request) -> Settings:
    settings: Settings = request.app.state.settings
    return settings


def get_spark_dep(request: Request) -> SparkSession:
    spark: SparkSession = request.app.state.spark
    return spark
\end{lstlisting}

\begin{soual}
\textbf{Had el zouz functions homa ligne wa7da bark kol wa7da (\code{return
request.app.state.X}). 3lech mahomch just \code{request.app.state.settings}
direct f el route signature, bidoun \code{Depends}?}

Réponse: FastAPI's \code{Depends} houwa el mechanism el standard l
"dependency injection" -- houwa ye5alli el route function signature
(\code{def list\_alerts(..., spark: SparkSession = Depends(get\_spark\_dep))})
tban \emph{declarative} 3la chnowa lezemha (\emph{"I need a SparkSession"}),
mouch \emph{imperative} (\emph{"go fetch \code{request.app.state.spark}
manually"}). Hedha ye5alli: \code{(1)} FastAPI's dependency override system
(\code{app.dependency\_overrides[get\_spark\_dep] = ...}) ye5dem l tests
(t9dar t-substitue mock/stub SparkSession bidoun ma tbeddel el route code
b tout). \code{(2)} Auto-generated OpenAPI docs (Swagger) ye-comprend
el dependencies mte3 kol route clair. \code{(3)} Signature el route
ye-b9a self-documenting -- ta9raha w ta3raf f'awra chnowa el route
ye7taj bidoun ma tefhem internals mte3 \code{app.state}.
\end{soual}

\section{\file{api/\_tables.py} -- el shared read logic}

\begin{lstlisting}[style=pystyle]
Shared read helpers used by more than one router - kept out of any
single router so "what counts as an alert" and "how to handle a gold
table that hasn't been built yet" can't drift between endpoints that both
need them.

GOLD TABLES CAN LEGITIMATELY NOT EXIST YET: the gold tables and
``ml_scores`` only exist after ``run_pipeline.py`` has completed at least
one full run. Every gold-backed read here treats "table not found" as "no
data yet" (returns ``None``), not as a fault - routers turn that into an
empty response, never a 500.
\end{lstlisting}

\begin{soual}
\textbf{3lech "table not found" (gold table ma-t5al9etch 3ad) houwa treated
kif "normal, empty result" (return \code{None} $\to$ empty response),
mouch HTTP 500 error?}

Réponse: mel docstring, hedha houwa el état \emph{normal} l API fresh --
ken el user ye3mel \code{git clone} w yshaghal el API mba-cher (bidoun ma
yshaghal \code{run\_pipeline.py} el awel), el gold tables (\code{attacker\_profiles},
\code{attack\_timeline}, \code{ml\_scores}) \emph{ma-mawjoudinch} 3la disk
9ad. Hedha \emph{mahouch bug} walla "erreur" mte3 el API -- houwa expected
state 3nd el deployment fresh. Return \code{None} (w routers ye-convert-ha
l empty response, mathalan \code{AlertsResponse(total=0, items=[])}) ye5alli
el frontend (dashboard) ye-3red "no data yet" gracefully, machi crash walla
HTTP 500 confusing. HTTP 500 lezem yeb9a reserved l bugs 7a9i9iyin
(unexpected exceptions), machi l état normal predictible.
\end{soual}

\subsection{\code{\_ALERT\_SILVER\_COLUMNS} w \code{ALERT\_COLUMNS}}

\begin{lstlisting}[style=pystyle]
_ALERT_SILVER_COLUMNS = (
    "event_id", "event_time", "src_ip", "dst_ip", "dst_port",
    "source_type", "severity", "attack_category",
)

ALERT_COLUMNS = (
    "event_id", "scored_at", "alert_source", "anomaly_score", "is_anomaly",
    "event_time", "src_ip", "dst_ip", "dst_port", "source_type",
    "severity", "attack_category",
)
\end{lstlisting}

\begin{itemize}
  \item \code{\_ALERT\_SILVER\_COLUMNS}: el columns li ye3ayet-lhom mel
        silver table (context 3la el attack -- IP, port, severity...)
        li \code{ml\_scores} nafsou ma-3andou-ch (houwa 3andou bark
        \code{event\_id}, \code{scored\_at}, \code{anomaly\_score},
        \code{is\_anomaly}, \code{alert\_source}).
  \item \code{ALERT\_COLUMNS}: el projection el kamla el akhira -- comment
        ye9oul \emph{"matching threatlake.api.schemas.AlertItem
        field-for-field"} -- exactement el fields li \code{AlertItem}
        Pydantic model ye7taj-hom (chefna \code{schemas.py} nchoufouha
        ba3d). \code{ALERT\_COLUMNS} \emph{public} (f \code{\_\_all\_\_})
        3la 5atr mous-ta3mla mel 2 routers (\code{alerts.py} w
        \code{attacker\_profiles.py}'s drilldown) -- single source of
        truth l el projection, bech el 2 endpoints ye5serializ el alert
        row \emph{identical} (nafs el fields, nafs el ordre).
\end{itemize}

\subsection{\code{read\_gold\_table} w \code{read\_silver}}

\begin{lstlisting}[style=pystyle]
def read_gold_table(spark: SparkSession, table: str, settings: Settings) -> DataFrame | None:
    """Read a gold Delta table, or None if it hasn't been built yet."""
    try:
        return spark.read.format("delta").load(gold_path(table, settings))
    except Exception:
        return None


def read_silver(spark: SparkSession, settings: Settings) -> DataFrame | None:
    """Read the silver events table, or None if nothing has landed yet."""
    try:
        return spark.read.format("delta").load(silver_path("events", settings))
    except Exception:
        return None
\end{lstlisting}

Nafs el pattern \code{try/except Exception: return None} li chefnah f
\code{score\_events.py}'s \code{\_already\_scored\_event\_ids} w
\code{prompts.py}'s \code{\_describe\_table} -- table mahouch mawjoud
3la disk (Delta load ye-fail) houwa treated kif \code{None}, machi
propagated exception.

\subsection{\code{read\_alerts} -- el join el asasi}

\begin{lstlisting}[style=pystyle]
def read_alerts(spark: SparkSession, settings: Settings) -> DataFrame | None:
    """``ml_scores`` joined with silver, filtered to rows either detector
    actually flagged.

    ``is_anomaly`` is a NOT NULL column in
    ``threatlake.ml.score_events.SCORES_SCHEMA`` - so this can filter on
    it directly rather than needing a null-coalescing OR.
    """
    scores = read_gold_table(spark, "ml_scores", settings)
    if scores is None:
        return None
    silver = read_silver(spark, settings)
    if silver is None:
        return None

    joined = scores.join(silver.select(*_ALERT_SILVER_COLUMNS), on="event_id", how="inner")
    return joined.filter(F.col("is_anomaly"))
\end{lstlisting}

\begin{itemize}
  \item Zouz early-returns séquentiels (\code{if scores is None: return
        None}, ba3d \code{if silver is None: return None}) -- ken 7atta
        wa7ed mel 2 tables mahouch mawjoud, el function el kamla terja3
        \code{None} (mahouch alerts 9ad, walaw el table el o5ra mawjouda).
  \item \code{scores.join(silver.select(*\_ALERT\_SILVER\_COLUMNS),
        on="event\_id", how="inner")}: INNER join (machi LEFT) -- 3la
        5atr \code{event\_id} lezem ykoun f el zouz tables bech el row
        ykoun useful (score bidoun context mel silver, wela context
        bidoun score, mahomch useful l "alert" concept).
  \item \code{.filter(F.col("is\_anomaly"))}: hedha el filter el akhir --
        \code{is\_anomaly} boolean NOT NULL (\code{SCORES\_SCHEMA}), fa
        \code{.filter(F.col("is\_anomaly"))} houwa equivalent l
        \code{.filter(F.col("is\_anomaly") == True)} (Spark ye9bel boolean
        column direct kif filter condition). Docstring ye-explique el
        "why" el subtile: \emph{"is\_anomaly is a NOT NULL column ...
        so this can filter on it directly rather than needing a
        null-coalescing OR"} -- schema PFA (ml-only, bidoun classifier
        supplémentaire) ye-guarantee \code{is\_anomaly} dima True/False,
        contra ThreatLake AI li 3andha detector ekstra (classifier) li
        momken y5alli el column null 3la certain rows, fa el filter
        hnak lezmou \code{OR} logic akthar complexe.
\end{itemize}

\section{\file{api/schemas.py}}

\begin{lstlisting}[style=pystyle]
Pydantic response models for every API route.

Hand-written, not auto-derived from the Delta schemas: an API response is
a deliberately curated, presentation-shaped view - keeping it separate
from the storage layer means a storage-schema change doesn't silently
change the API contract, and vice versa.
\end{lstlisting}

\begin{soual}
\textbf{3lech el Pydantic models hand-written (rewrite manuel mte3 el
fields), mouch auto-générés mel Delta schemas (\code{SILVER\_EVENT\_SCHEMA},
\code{SCORES\_SCHEMA}...)? Mahouch duplication (nafs el fields mektoubin
marat 3adda)?}

Réponse: docstring ye9olha directement -- \emph{"an API response is a
deliberately curated, presentation-shaped view"}. El storage schema
(\code{SILVER\_EVENT\_SCHEMA} mathalan) ye-optimize l storage/query
concerns (types Delta/Spark, nullability constraints, partitioning...).
El API response ye-optimize l consumer concerns (chnowa el frontend
lezmou ya3raf, chnowa format ahsen l JSON serialization, chnowa fields
lezmou t5abbaw -- mathalan \code{geo} struct el kbir f gold table ye-flatten
l \code{latitude}/\code{longitude} bark f API, machi el struct kamel).
Duplication (nafs esm el field mektoub zouz marat) houwa trade-off
mou9asad: separation ye5alli \emph{storage-schema change ma-y-changich
API contract silently} (tzid column jdid f silver, API response
ma-tetbeddel-ch automatiquement -- lezmek explicitement tzid-ha f
Pydantic model ken 7bit t-esta3ml-ha), w el 3ks zeda (tbeddel API response
shape ma-yehtajch tbeddel storage schema). El 2 evolve independamment.
\end{soual}

\subsection{\code{AlertItem} w \code{AlertsResponse}}

\begin{lstlisting}[style=pystyle]
class AlertItem(BaseModel):
    event_id: str
    scored_at: datetime
    alert_source: str | None
    anomaly_score: float | None
    is_anomaly: bool
    event_time: datetime | None
    src_ip: str | None
    dst_ip: str | None
    dst_port: int | None
    source_type: str | None
    severity: int | None
    attack_category: str | None


class AlertsResponse(BaseModel):
    total: int
    limit: int
    offset: int
    items: list[AlertItem]
\end{lstlisting}

\begin{itemize}
  \item \code{AlertItem}: field-for-field match m3a \code{ALERT\_COLUMNS}
        (\code{\_tables.py}), b nullability correspondante l el schema
        el 7a9i9i (\code{event\_id}/\code{is\_anomaly} required, el
        ba9i optional -- match \code{SCORES\_SCHEMA} w
        \code{SILVER\_EVENT\_SCHEMA}'s nullable fields).
  \item \code{AlertsResponse}: \code{total}, \code{limit}, \code{offset}
        + \code{items} -- pagination envelope el standard: \code{total}
        (kaddech row mawjoud kamlin, machi bark f had el page), \code{limit}/\code{offset}
        (echo mte3 el request params, useful l el frontend bech ya3raf
        win rahou f el pagination), \code{items} (el page el 7a9i9iya).
\end{itemize}

\subsection{\code{CredentialAttempt} w \code{AttackerProfile}}

\begin{lstlisting}[style=pystyle]
class CredentialAttempt(BaseModel):
    username: str | None
    password: str | None
    count: int


class AttackerProfile(BaseModel):
    src_ip: str
    first_seen: datetime | None
    last_seen: datetime | None
    total_events: int
    distinct_ports_hit: int
    distinct_honeypots_hit: int
    top_credentials_tried: list[CredentialAttempt]
    attack_categories: list[str]
    # Flattened out of the gold table's richer `geo` struct - country/city/
    # ASN aren't surfaced here because nothing in this API currently uses
    # them; lat/lon are, for the dashboard's Map tab. Both null when the
    # IP didn't resolve (private/reserved range, or a MaxMind free-tier
    # coverage gap - both normal, not errors).
    latitude: float | None
    longitude: float | None
\end{lstlisting}

\begin{soual}
\textbf{Docstring ye9oul \code{latitude}/\code{longitude} bark ye-flatten
mel \code{geo} struct (li 3andou 6 fields: country, city, latitude,
longitude, asn, asn\_org). 3lech bark 2 mel 6, w chnowa ye-sir l el 4
el o5rin?}

Réponse: mel comment inline -- \emph{"country/city/ASN aren't surfaced
here because nothing in this API currently uses them; lat/lon are, for
the dashboard's Map tab"}. Hedhi el falsafa "presentation-shaped view"
(el Soual el fou9) f action concrete: el gold table (\code{attacker\_profiles})
3andha kol el 6 fields (data complet, computed once via GeoEnricher).
API response ye-expose bark el subset li \emph{consumer 7a9i9i} (dashboard's
Map tab) lezmou. El 4 fields el o5rin \emph{mahomch mfoutin} -- homa
mawjoudin f el gold table nafsou (accessible via copilot query mathalan,
walla direct query 3la Delta table), just \emph{mahomch f had el API
response el specifique} 3la 5atr ma-fama-ch consumer 7ali ye7taj-hom.
Ken feature jdid (mathalan "show country flag") ye7taj \code{country},
tzid-ha l \code{AttackerProfile} w l \code{\_select\_profile\_columns}
f el router -- one-line change, machi redesign.
\end{soual}

\subsection{\code{AttackerProfilesResponse} w \code{AttackerDrilldown}}

\begin{lstlisting}[style=pystyle]
class AttackerProfilesResponse(BaseModel):
    total: int
    limit: int
    offset: int
    items: list[AttackerProfile]


class AttackerDrilldown(BaseModel):
    profile: AttackerProfile
    alerts: list[AlertItem]
\end{lstlisting}

\code{AttackerDrilldown}: nafs el fikra kif \code{AttackerProfilesResponse}
walakin l endpoint \code{/{ip}} -- ya3ayet zouz "concepts" mte3 el API
sawa (\code{AttackerProfile} + \code{list[AlertItem]}) f response wa7ed,
bech analyst ya5ou el profile \emph{w} el alerts mte3 had el IP f call
wa7ed, machi zouz calls separate.

\subsection{\code{CopilotQueryRequest} w \code{CopilotQueryResponse}}

\begin{lstlisting}[style=pystyle]
class CopilotQueryRequest(BaseModel):
    question: str


class CopilotQueryResponse(BaseModel):
    """Exactly one of two shapes, discriminated by ``rejected``: success ->
    sql/rows/row_count populated, reason null; rejected/failed -> reason
    populated (always specific), rows/row_count null. ``sql`` may still be
    present on a guardrail rejection (the query that got rejected) but is
    null if generation itself never produced one.
    """

    rejected: bool
    reason: str | None
    sql: str | None
    rows: list[dict[str, object]] | None
    row_count: int | None
\end{lstlisting}

\begin{soual}
\textbf{3lech \code{CopilotQueryResponse} houwa "one model, 2 shapes"
(discriminated by \code{rejected} boolean, kol el fields optional 3da
\code{rejected} nafsou), mouch zouz Pydantic models separate (mathalan
\code{Union[CopilotSuccessResponse, CopilotErrorResponse]})?}

Réponse: mahiche documented explicitement f el code, walakin el trade-off
houwa clair mel shape nafsou: model wa7ed (kifma docstring te-explique
b "exactly one of two shapes, discriminated by rejected") houwa simple
API contract l el frontend -- \code{response\_model=CopilotQueryResponse}
dima, el frontend ye-check \code{if response.rejected:} w ye-access
el fields el correctes. B \code{Union[...]}, FastAPI's OpenAPI schema
generation ye-b9a akthar complexe (discriminated unions), w el frontend
lezmou type-narrowing logic akthar sophistiquée. Trade-off: model wa7ed
ye5sser type-safety chwaya (kol field optional, 7atta ken semantically
"always present on success") mou9abel simplicité API/client-side.
\end{soual}

\section{\file{api/routers/alerts.py}}

\begin{lstlisting}[style=pystyle]
GET /alerts (paginated, filterable).

See threatlake.api._tables.read_alerts for what counts as an "alert"
here. PFA is batch-only, so there is no live push endpoint here: results
are as fresh as the last pipeline run, not real-time.
\end{lstlisting}

\subsection{\code{\_DEFAULT\_LIMIT}, \code{\_MAX\_LIMIT}, w \code{list\_alerts}}

\begin{lstlisting}[style=pystyle]
_DEFAULT_LIMIT = 50
_MAX_LIMIT = 500


@router.get("/alerts", response_model=AlertsResponse)
def list_alerts(
    alert_source: str | None = Query(
        default=None, description="Filter to 'rule', 'ml', or 'both'."
    ),
    severity: int | None = Query(default=None, ge=1, le=5),
    limit: int = Query(default=_DEFAULT_LIMIT, ge=1, le=_MAX_LIMIT),
    offset: int = Query(default=0, ge=0),
    spark: SparkSession = Depends(get_spark_dep),
    settings: Settings = Depends(get_settings_dep),
) -> AlertsResponse:
    alerts = read_alerts(spark, settings)
    if alerts is None:
        return AlertsResponse(total=0, limit=limit, offset=offset, items=[])

    if alert_source is not None:
        alerts = alerts.filter(F.col("alert_source") == alert_source)
    if severity is not None:
        alerts = alerts.filter(F.col("severity") == severity)

    alerts = alerts.orderBy(F.col("scored_at").desc())
    total = alerts.count()
    page = alerts.offset(offset).limit(limit).collect()
    items = [AlertItem(**{c: row[c] for c in ALERT_COLUMNS}) for row in page]
    return AlertsResponse(total=total, limit=limit, offset=offset, items=items)
\end{lstlisting}

\begin{itemize}
  \item Kol query parameter mou3arraf b \code{Query(...)} -- FastAPI's
        way l declare query string params b validation (\code{ge=1,
        le=5} l \code{severity} mathalan -- FastAPI ye-reject el request
        automatiquement b HTTP 422 ken el user ye7ott \code{severity=99},
        bidoun ma tekteb el validation manuellement f el function body).
        \code{description=...} ye-populate el Swagger/OpenAPI docs
        automatiquement.
  \item \code{\_DEFAULT\_LIMIT = 50}, \code{\_MAX\_LIMIT = 500}: nafs
        el falsafa kif \code{MAX\_ROWS} f el copilot guardrail -- server-side
        cap 3la kaddech data ye5rej f response wa7da, machi trust el
        client ye-request kaddech y7eb.
  \item \code{if alerts is None: return AlertsResponse(total=0, ...,
        items=[])}: exactement el "empty response, never 500" el
        docstring mte3 \code{\_tables.py} te-describe.
  \item \code{if alert\_source is not None: alerts = alerts.filter(...)}:
        filters conditionnels -- bark ye-apply-hom ken el query param
        mawjoud (\code{None} check), machi filter dima 3la \code{None}
        (li ye5dem differently f SQL/Spark, \code{col == None} mahouch
        nafs \code{col IS NULL}).
  \item \code{total = alerts.count()} \emph{9bal} \code{.offset(offset).limit(limit).collect()}:
        el count houwa 3la el DataFrame el kamel (ba3d filters, 9bal
        pagination) -- bech \code{total} f el response ye-represente
        kaddech row mawjoud kamlin (l el frontend bech ya3raf kaddech
        pages fama), machi bark kaddech f had el page.
  \item \code{.offset(offset).limit(limit)}: Spark's pagination -- 3la
        DataFrame ordoné 9bal, \code{.offset()} ye-skip el rows el
        awlanin, \code{.limit()} ye5ou el N el jayya.
  \item \code{[AlertItem(**\{c: row[c] for c in ALERT\_COLUMNS\}) for row
        in page]}: dict comprehension jowa \code{AlertItem(**...)} --
        ye5ou kol column mel \code{ALERT\_COLUMNS} tuple mel Spark
        \code{Row} object (\code{row[c]}), yebni dict, w \code{**} unpacking
        ye3addi-ha ki keyword arguments l \code{AlertItem} constructor
        (Pydantic validation automatique 3la kol field f had el moment).
\end{itemize}

\section{\file{api/routers/attacker\_profiles.py}}

\begin{lstlisting}[style=pystyle]
GET /attacker_profiles (paginated) and GET /attacker_profiles/{ip} (drill-down).

Both read threatlake.transform.gold.attacker_profiles. The /{ip}
drill-down additionally pulls this IP's alerts (read_alerts) so an
analyst gets the profile and its flagged activity in one call instead of
two.
\end{lstlisting}

\subsection{\code{\_PROFILE\_COLUMNS} w \code{\_select\_profile\_columns}}

\begin{lstlisting}[style=pystyle]
_PROFILE_COLUMNS = (
    "src_ip", "first_seen", "last_seen", "total_events",
    "distinct_ports_hit", "distinct_honeypots_hit",
    "top_credentials_tried", "attack_categories",
)


def _select_profile_columns(df: DataFrame) -> DataFrame:
    """_PROFILE_COLUMNS plus lat/lon, flattened out of the gold table's
    `geo` struct - see threatlake.api.schemas.AttackerProfile for why only
    those two geo fields are surfaced here.
    """
    return df.select(
        *_PROFILE_COLUMNS,
        F.col("geo.latitude").alias("latitude"),
        F.col("geo.longitude").alias("longitude"),
    )
\end{lstlisting}

\code{F.col("geo.latitude")}: dot notation 3la Spark column l access
\emph{nested struct field} -- \code{geo} houwa struct column (kifma
chefnah f \code{build\_attacker\_profiles}), w \code{.alias("latitude")}
ye-flatten-ha l column top-level, machi struct nested, f el DataFrame
el akhir -- exactement el "flattening" el discuté f \code{schemas.py}'s
Soual box.

\subsection{\code{\_profile\_from\_row}}

\begin{lstlisting}[style=pystyle]
def _profile_from_row(row: Row) -> AttackerProfile:
    data: dict[str, Any] = row.asDict(recursive=True)
    return AttackerProfile(**data)
\end{lstlisting}

\code{row.asDict(recursive=True)}: \code{recursive=True} muhim hna --
\code{top\_credentials\_tried} houwa array mte3 structs (\code{CredentialAttempt}
shape). Bidoun \code{recursive=True}, \code{.asDict()} ye-convert bark
el level el awel (top-level fields l dict values), w el nested structs/arrays
ye-b9aou Spark Row objects (mahomch JSON-serializable direct, w Pydantic
ma-y9bel-hom-ch direct). B \code{recursive=True}, kol nested Row (jowa
arrays zeda) ye-convert l dict/list recursively, fa \code{AttackerProfile(**data)}
ye9dar ye-validate el structure el kamla (\code{list[CredentialAttempt]}
mel dict el nested).

\subsection{\code{list\_attacker\_profiles}}

\begin{lstlisting}[style=pystyle]
@router.get("/attacker_profiles", response_model=AttackerProfilesResponse)
def list_attacker_profiles(
    limit: int = Query(default=_DEFAULT_LIMIT, ge=1, le=_MAX_LIMIT),
    offset: int = Query(default=0, ge=0),
    spark: SparkSession = Depends(get_spark_dep),
    settings: Settings = Depends(get_settings_dep),
) -> AttackerProfilesResponse:
    profiles = read_gold_table(spark, "attacker_profiles", settings)
    if profiles is None:
        return AttackerProfilesResponse(total=0, limit=limit, offset=offset, items=[])

    profiles = _select_profile_columns(profiles).orderBy(F.col("total_events").desc())
    total = profiles.count()
    page = profiles.offset(offset).limit(limit).collect()
    return AttackerProfilesResponse(
        total=total, limit=limit, offset=offset, items=[_profile_from_row(r) for r in page]
    )
\end{lstlisting}

Nafs pattern exact kif \code{list\_alerts} -- ordoné b
\code{total\_events desc} (attackers el akthar actifs el awlanin,
default sensible l analyst SOC).

\subsection{\code{get\_attacker\_profile} -- el drilldown}

\begin{lstlisting}[style=pystyle]
@router.get("/attacker_profiles/{ip}", response_model=AttackerDrilldown)
def get_attacker_profile(
    ip: str,
    spark: SparkSession = Depends(get_spark_dep),
    settings: Settings = Depends(get_settings_dep),
) -> AttackerDrilldown:
    profiles = read_gold_table(spark, "attacker_profiles", settings)
    if profiles is None:
        raise HTTPException(status_code=404, detail=f"No attacker profile data for {ip!r} yet.")

    match = _select_profile_columns(profiles).filter(F.col("src_ip") == ip).collect()
    if not match:
        raise HTTPException(status_code=404, detail=f"No attacker profile for {ip!r}.")
    profile = _profile_from_row(match[0])

    alerts_df = read_alerts(spark, settings)
    alerts: list[AlertItem] = []
    if alerts_df is not None:
        rows = (
            alerts_df.filter(F.col("src_ip") == ip)
            .select(*ALERT_COLUMNS)
            .orderBy(F.col("scored_at").desc())
            .collect()
        )
        alerts = [AlertItem(**{c: row[c] for c in ALERT_COLUMNS}) for row in rows]

    return AttackerDrilldown(profile=profile, alerts=alerts)
\end{lstlisting}

\begin{soual}
\textbf{Fama zouz \code{HTTPException(status\_code=404)} calls mo5tlifin
f had el function (\code{"data for \{ip!r\} yet"} vs \code{"profile for
\{ip!r\}"}) -- 3lech el difference houwa muhimma, machi bark style?}

Réponse: el 2 messages ye-distinguer bin zouz cas 404 mo5tlifin
semantiquement. El awel (\code{profiles is None}): \emph{el table kamla}
mahiche mawjouda (\code{run\_pipeline.py} ma-tshaghalch 3ad) -- ma-3andiche
7atta data l ay IP. El theni (\code{not match}): el table mawjouda, fiha
data, walakin had el IP el specifique \emph{mahouch fiha} (attacker
mahouch scanné el honeypot 9att). Zouz cas mo5tlifin l el analyst li
ye-debug-i: el awel ye9oul "el pipeline ma-tshaghalch 3ad" (action: run
el pipeline), el theni ye9oul "el pipeline yeshtaghel, walakin had el IP
mahouch attacker ma3rouf" (action: verify el IP, machi bug). Messages
distincts (walaw kolhom 404) ye5alliw el troubleshooting a9rab.
\end{soual}

\begin{itemize}
  \item \code{match = ....collect()}: \code{.collect()} 3la DataFrame
        filtré (bark row wa7da mtawa9a3a, 3la 5atr \code{src\_ip} houwa
        el key mte3 el grain) -- Python list, checked b \code{if not
        match}.
  \item \code{alerts\_df = read\_alerts(spark, settings)} + \code{if
        alerts\_df is not None:}: had el partie \emph{optional} -- ken
        \code{ml\_scores} walla silver mahomch mawjoudin (pipeline
        na9is), el drilldown ye-b9a ye5dem (profile mawjoud), just
        \code{alerts} ye-b9a empty list (machi 404 3la had el section
        bark).
  \item \code{AttackerDrilldown(profile=profile, alerts=alerts)}: el
        combination el akhira -- el response wa7ed ye-includi el 2
        "concepts" (profile + alerts) f call HTTP wa7ed.
\end{itemize}

\section{\file{api/routers/copilot.py} -- el 3-step flow}

\begin{lstlisting}[style=pystyle]
Three strictly separate steps, each of which can independently fail
without touching the next:
  1. threatlake.copilot.text_to_sql.generate_sql - ask the LLM for SQL.
     Never trusted.
  2. threatlake.copilot.guardrails.validate_and_bound - the ONLY thing
     that decides whether that SQL is allowed to run. Runs BEFORE any
     Spark call, on every request, no exceptions.
  3. Execute the validated SQL against temp views over the gold tables
     only - not the live gold Delta tables directly, so nothing this
     endpoint does can ever touch bronze, silver, or ml_scores even if
     steps 1-2 had a bug.

Every rejection - config, provider, guardrail, or execution failure - is
logged with its specific reason and returned to the caller the same way,
never a bare 500: this is a read-only analyst tool, a failure here should
never look like a crash.
\end{lstlisting}

\begin{soual}
\textbf{Hedha el endpoint el akthar critique securité-wise f el codebase
kamel -- comment (step 3) ye9oul "even if steps 1-2 had a bug" el temp
views houma defense supplémentaire. 3lech had el "triple layer" (LLM
prompt + guardrail + temp views scoped) mahouch redundant/overkill?}

Réponse: hedha exemple concret mte3 "defense-in-depth" (l\'concept li
et9echnah barcha marat f had el document): kol layer ye-address
\emph{failure mode} \emph{mo5tlef}. Step 1 (LLM prompt, mel
\code{build\_system\_prompt}) houwa \emph{guidance}, ma-lezmech y-esta3mel
mel LLM abadan (hallucination, prompt injection). Step 2 (guardrail
AST parsing) houwa el security boundary el 7a9i9i -- deterministe,
testable, ma-y3tamedch 3la el LLM. Step 3 (temp views scoped 3la
\code{ALLOWED\_TABLES} bark) houwa layer \emph{structurel} ekstra --
\emph{7atta ken} el guardrail (step 2) 3andou bug w y5alli SQL dangereux
ye3addi (edge case ma-testeh-ch), el SQL el akhir ye-execute 3la
\code{spark.sql(safe\_sql)} \emph{f context win bronze/silver/ml\_scores
ma-3andhomch-ch temp view registered 7atta}. Ye3ni: 7atta ken el SQL
ye7awel \code{SELECT * FROM bronze\_cowrie}, Spark ye-throw
"table or view not found" -- 3la 5atr el temp view l \code{bronze\_cowrie}
ma-tsawwaret-ch abadan f had el session. Had el layer el akhir ma
ye3tamedch 3la "el guardrail logic sa7i7a" -- houwa \emph{structural
impossibility}, machi behavioral check.
\end{soual}

\subsection{\code{\_rejected}}

\begin{lstlisting}[style=pystyle]
def _rejected(reason: str, sql: str | None = None) -> CopilotQueryResponse:
    return CopilotQueryResponse(rejected=True, reason=reason, sql=sql, rows=None, row_count=None)
\end{lstlisting}

Helper sghir -- constructor shortcut l el "rejected shape" mte3
\code{CopilotQueryResponse} (\code{rejected=True}, \code{rows=None},
\code{row\_count=None} dima), bech el function el kbira (\code{copilot\_query})
ma tekteb-ch had el 5 fields kol marra manuellement f kol exception
handler.

\subsection{\code{copilot\_query} -- Step 1: generation}

\begin{lstlisting}[style=pystyle]
@router.post("/copilot/query", response_model=CopilotQueryResponse)
def copilot_query(
    request: CopilotQueryRequest,
    spark: SparkSession = Depends(get_spark_dep),
    settings: Settings = Depends(get_settings_dep),
) -> CopilotQueryResponse:
    try:
        raw_sql = generate_sql(request.question, spark, settings)
    except SchemaUnavailableError as exc:
        _LOG.warning("copilot: gold schema unavailable for question=%r: %s", request.question, exc)
        return _rejected(str(exc))
    except CopilotConfigError as exc:
        _LOG.warning("copilot: not configured: %s", exc)
        return _rejected(str(exc))
    except CopilotProviderError as exc:
        _LOG.warning("copilot: provider error for question=%r: %s", request.question, exc)
        return _rejected(f"the language model call failed: {exc}")
\end{lstlisting}

\begin{itemize}
  \item 3 \code{except} clauses séparés, kol wa7ed l exception type
        specifique (\code{SchemaUnavailableError}, \code{CopilotConfigError},
        \code{CopilotProviderError} -- kolhom chefnahom f
        \code{copilot/text\_to\_sql.py} w \code{copilot/prompts.py}).
        Kol wa7ed ye-log-i b message specifique (context mo5tlef l kol
        cas), walakin kolhom yerja3ou \code{\_rejected(...)} -- consistent
        response shape l el caller (HTTP 200 dima, mahouch 500), b
        \code{reason} specifique l kol cas.
  \item Zero \code{except Exception:} generic hna -- ken exception
        mahouch mel 3 types mou3arrfin, ye-propagate (crash 7a9i9i,
        machi silently caught) -- houwa distinguer bin "expected failure
        modes" (config, schema, provider -- el 3 exceptions) w
        "unexpected bug" (ay chay o5or, li lezem ykoun visible, machi
        masked).
\end{itemize}

\subsection{Step 2: guardrail}

\begin{lstlisting}[style=pystyle]
    try:
        safe_sql = validate_and_bound(raw_sql)
    except GuardrailRejectionError as exc:
        _LOG.warning(
            "copilot: guardrail REJECTED question=%r generated_sql=%r reason=%s",
            request.question,
            raw_sql,
            exc.reason,
        )
        return _rejected(exc.reason, sql=raw_sql)
\end{lstlisting}

\code{return \_rejected(exc.reason, sql=raw\_sql)}: mula7id -- \code{sql=raw\_sql}
mawjoud hna (contra el 3 rejections el fou9, li \code{sql} mte3hom
\code{None} 3la 5atr generation nafsou fashla, ma-fama-ch SQL 7atta).
Hedhi el "sql may still be present on a guardrail rejection" el mentioned
f \code{CopilotQueryResponse}'s docstring -- caller (analyst, walla
developer li ye-debug) ye9dar ye-chouf \emph{el SQL el exact li rejected}
w \emph{3lech}.

\subsection{Step 3: temp views w execution}

\begin{lstlisting}[style=pystyle]
    for table in ALLOWED_TABLES:
        try:
            spark.read.format("delta").load(gold_path(table, settings)).createOrReplaceTempView(
                table
            )
        except Exception as exc:
            _LOG.warning("copilot: gold table %r unavailable: %s", table, exc)
            return _rejected(
                f"gold table {table!r} isn't available yet - the gold scheduler needs to "
                "complete at least one run before the copilot can answer questions"
            )

    try:
        result_df = spark.sql(safe_sql)
        rows = [row.asDict(recursive=True) for row in result_df.collect()]
    except Exception as exc:
        _LOG.warning("copilot: execution failed sql=%r error=%s", safe_sql, exc)
        return _rejected(f"the query failed to execute: {exc}", sql=safe_sql)

    _LOG.info("copilot: question=%r sql=%r rows=%d", request.question, safe_sql, len(rows))
    return CopilotQueryResponse(
        rejected=False, reason=None, sql=safe_sql, rows=rows, row_count=len(rows)
    )
\end{lstlisting}

\begin{itemize}
  \item \code{for table in ALLOWED\_TABLES: ... createOrReplaceTempView(table)}:
        el loop ye-register temp view l \emph{kol} table f
        \code{ALLOWED\_TABLES} (el 2 f PFA), machi bark el tables li
        \code{safe\_sql} ye-reference-hom -- simplicité (ma-lezmech
        t-parse el SQL besh ta3raf chnowa tables mous-ta3mlin, temp
        views cheap l register).
  \item \code{spark.sql(safe\_sql)}: \code{safe\_sql} houwa el SQL el
        rewritten (LIMIT bounded) mel guardrail, ye-execute f context
        win bark el temp views registered mawjoudin (bronze/silver/ml\_scores
        \emph{ma-3andhomch-ch} temp view -- exactement el "structural
        impossibility" mentioned f el Soual box el fou9).
  \item \code{rows = [row.asDict(recursive=True) for row in result\_df.collect()]}:
        nafs pattern kif \code{\_profile\_from\_row} -- \code{recursive=True}
        bech nested structures (result d'un query complexe, ken fama)
        ye-convert-iw l dicts JSON-serializable.
  \item \code{\_LOG.info(...)} f el a5er (success case): logging dima --
        sawa success sawa rejection, kol chay mel copilot flow ye-log-i,
        useful l audit trail w debugging.
\end{itemize}

\chapter{Tableau récapitulatif -- "win nal9a chnowa"}

\begin{longtable}{p{0.32\textwidth} p{0.32\textwidth} p{0.28\textwidth}}
\toprule
\textbf{Su2al mte3 jury} & \textbf{File} & \textbf{Function/Class} \\
\midrule
\endhead
Kifeh el config ye5dem (YAML + env vars)? & \file{common/config.py} & \code{Settings}, \code{get\_settings} \\
Kifeh path mte3 table ye-construct? & \file{common/paths.py} & \code{layer\_path}, \code{bronze\_path} etc \\
Kifeh ta3raf Spark ye5dem 3la UTC 7a9i9i? & \file{common/spark.py} & \code{\_verify\_utc\_timezone}, \code{\_check\_utc\_timezone} \\
Kifeh files ye-move mel landing l processed? & \file{common/fs.py} & \code{move} \\
Fein el schema mte3 cowrie? & \file{ingestion/schemas.py} & \code{source\_schema} \\
Kifeh malformed JSON ye-quarantine? & \file{ingestion/bronze\_transform.py} & \code{split\_quarantine} \\
Kifeh ingestion houwa at-least-once? & \file{ingestion/bronze\_writer.py} & \code{write\_bronze} \\
Chnowa el 18 fields mte3 silver? & \file{transform/silver/schema.py} & \code{SILVER\_EVENT\_SCHEMA} \\
Kifeh cowrie events ye-map l attack\_category/severity? & \file{transform/silver/cowrie.py} & \code{\_CATEGORY\_SEVERITY}, \code{map\_cowrie} \\
Kifeh gold tables ye-overwrite idempotently? & \file{transform/gold/writer.py} & \code{write\_gold\_table} \\
Kifeh top-5 credentials par IP ye7sbou? & \file{transform/gold/attacker\_profiles.py} & \code{\_top\_credentials\_by\_ip} \\
Kifeh geo struct ye5dem b/bidoun enricher? & \file{transform/gold/attacker\_profiles.py} & \code{build\_attacker\_profiles} \\
Kifeh timeline el hourly ye7sab? & \file{transform/gold/attack\_timeline.py} & \code{build\_attack\_timeline} \\
Kifeh trailing-window features ye7sbou? & \file{ml/features.py} & \code{build\_features} \\
3lech threshold 5 l port scan? & \file{ml/rules.py} & \code{port\_scan\_rule} \\
Kifeh IsolationForest ye-train? & \file{ml/train\_anomaly.py} & \code{train\_anomaly} \\
Kifeh ML w rule ye-combine f alert\_source? & \file{ml/score\_events.py} & \code{score\_silver\_events} \\
Kifeh SQL ye-valid AST-based? & \file{copilot/guardrails.py} & \code{validate\_and\_bound} \\
Chnowa el ALLOWED\_TABLES trimmed? & \file{copilot/guardrails.py} & \code{ALLOWED\_TABLES} \\
Kifeh Gemini ye3ayet-lha securement? & \file{copilot/text\_to\_sql.py} & \code{\_call\_gemini}, \code{generate\_sql} \\
Kifeh el prompt ye-build b live schema? & \file{copilot/prompts.py} & \code{build\_system\_prompt} \\
Kifeh table not-found ye-handle f API? & \file{api/\_tables.py} & \code{read\_gold\_table} \\
Kifeh alerts ye-join mel ml\_scores+silver? & \file{api/\_tables.py} & \code{read\_alerts} \\
Chnowa el 3 endpoints? & \file{api/routers/*.py} & \code{list\_alerts}, \code{list\_attacker\_profiles}, \code{copilot\_query} \\
Kifeh copilot query ye5dem end-to-end (3 steps)? & \file{api/routers/copilot.py} & \code{copilot\_query} \\
Kifeh tests ye5edmou b isolated app instance? & \file{api/app.py} & \code{create\_app} \\
\bottomrule
\end{longtable}

\end{document}






